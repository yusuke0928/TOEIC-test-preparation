# 進捗と引き継ぎ

最終更新: 2026-08-14 — **初版完成**

## 状態：完成。GitHub Pages へ公開できます

残っているのは、あなた（利用者側）の作業だけです。

```bash
git init && git add -A && git commit -m "朱記 — TOEIC L&R 900 対策アプリ 初版"
git branch -M main
git remote add origin https://github.com/<ユーザー名>/<リポジトリ名>.git
git push -u origin main
```

そのうえで GitHub の **Settings → Pages → Source** を **GitHub Actions** に設定してください。
`.github/workflows/pages.yml` が push を検知して自動で公開します。

## 収録内容

| 区分 | 内容 | 問題数 |
|---|---|---|
| 個別論点ドリル | 37論点（文法12・語彙7・文脈4・読解7・リスニング7） | 251 |
| 予想模試 Vol.1 | 標準回 | 200 |
| 予想模試 Vol.2 | リスニング強化回（図表・意図問題を増量） | 200 |
| 予想模試 Vol.3 | リーディング高負荷回（Part 7 の語数を上振れ） | 200 |
| 予想模試 Vol.4 | 語彙難化回（900帯の語を正解位置に） | 200 |
| 予想模試 Vol.5 | 総仕上げ回 | 200 |
| **合計** | | **1,251** |

各模試のパート構成は本番と完全一致（Part 1:6 / 2:25 / 3:39 / 4:30 / 5:30 / 6:16 / 7:54）。

## 検証済みの事項

- 整合性チェック **エラー0**（1,251問 / 712ユニット）
- 設問番号は 5回すべて 1〜200 の連番。欠番・重複なし
- 正解位置は**パート単位で均等**（Part 1 = 2/2/1/1、Part 6 = 4/4/4/4、Part 2 = 9/8/8 など）
- 実ブラウザの通しテスト **24項目すべてパス**
- GitHub Pages のサブパス配信（`/<repo>/`）を再現して動作一致を確認。絶対パス参照 0件
- README の記述（SRS間隔・推定スコアの最低問題数・話者ロール数・制限時間・問題数）をコードと機械照合し全項目一致

## 開発の経緯（3エージェント体制）

- **レビュー＋修正エージェント**：851問を1問ずつ監査して45件を検出（致命的5・要修正28・軽微12）。
  正解を入れると英文が壊れる問題2件、中断セッションの復元先が壊れる不具合、
  署名の改行が潰れる表示不具合（50か所以上）などを修正。
  監査中に既存バグ2件（`clearSession()` のデバウンス、片セクションのみの推定値混入）も発見・修正。
- **ユーザーエージェント**（40歳英語教師役）：通し体験で25件の改善提案。
  正解位置の偏りを実測で指摘（Part 6 は48問中 (D) が0回、Part 1 は (C) が56%）、
  リスニング未受験なのに推定スコアが出る問題、音声非対応環境で詰む問題などを指摘。すべて反映済み。
- **実装エージェント**：予想模試 Vol.3〜5 を作成。自作分の計算問題の誤りを出荷前に自力検算で発見・修正。

## 品質管理の道具（scratchpad 配下・セッション固有）

新セッションでは消えているので、必要なら作り直してください。

- `validate.mjs` — 整合性チェック。パート別問題数、設問番号の連番、選択肢数、`answer` の範囲、
  論点ID・場面ID、Part 6 の空所数、Part 7 の `insertAt`/`sentence`、id 重複、
  解説中の `(A)`〜`(D)` 参照が正解と一致するか
- `balance2.mjs` — 正解位置を**パート単位**で平準化する codemod。
  対象ファイル群を import してパートを特定し、acorn で構文解析した設問と出現順で突き合わせ、
  選択肢と `why` を対で入れ替える。整形とコメントは保たれる。
  数値・日付・曜日・`Bay 1` のような通し記号は順序に意味があるため自動的に除外。
  使い方: `node balance2.mjs [--write] --group <名前> <ファイル…>`
- `smoke.mjs` — Playwright による実ブラウザ通しテスト 24項目
- `shots.mjs` — 主要画面のスクリーンショット取得

## 設計上の判断メモ

- **過去問は使えない。** ETS が公開しておらず著作権保護されている。IIBC のサンプル問題ページにも
  「複製して使用することは法律で禁じられています」と明記。形式仕様のみ参照し全問を書き下ろした。
- Part 1 は写真の代わりに SVG 線画（28場面）。ただし線画では 900帯の識別
  （`is wearing` と `is putting on`、`is being stacked` に人が写っているか等）を鍛えられないという
  構造的な限界がある。**Part 1 の演習価値は低い**とユーザーエージェントも判断している。
- 合成音声には本番の連結・脱落・弱形がない。**リスニングは補助教材**と位置づけ、
  Part 3/4 はスクリプトと全体訳を精読・音読の素材として使うのが現実的。
- 端末間同期はサーバーがないため不可能。設定画面の「読み込んで合流」で代替している。
- 換算スコアは公式問題集の換算表の中央値をもとにした近似（`assets/js/score.js`）。±25点の幅で見る。

## 未決事項（次に着手するなら、まず本人に方針を確認すること）

初版は完成しており、以下はいずれも着手しなくても成立する。労力の差が大きいので方針を先に決める。

1. **論点の収録数を厚くする** — 最も費用対効果が高い。ユーザーエージェントが
   「900を目指すなら1論点あたり最低20〜30問は欲しい」と評価。現状の薄い論点は
   `p7syn` 3問 / `p7not` 4問 / `p7intent` 9問 / `cohesion` 9問 / `subj` 13問 /
   `verbal` 13問 / `inv` 13問 / `phrasal` 13問 など。薄い15論点を各20問まで補充すると約300問の追加。
2. **Part 1 の抜本改善** — 線画では 900帯の識別を鍛えられず、1場面＝1問なので2周目は
   絵で答えを思い出す。場面数を増やして1場面複数問にする／質感を描き分ける／
   「Part 1 は音声のみで訓練する」と割り切って設計変更する、などの方針判断が要る。
3. **難度目盛りの統一** — ドリルと模試で難度1〜5の尺度が揃っていない
   （「品詞の識別」は表示3〜5だが体感1〜2）。短時間で終わる。
4. **模試 Vol.6 以降の追加** — `assets/data/mocks/volN-*.js` を作り
   `registry.js` の `MOCK_META` に足すだけ。時間と費用は最もかかる。
