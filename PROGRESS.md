# 進捗と引き継ぎ

最終更新: 2026-09-12（3回目） — **否定語の軸（検査K）を是正した。**
「否定語を持つ選択肢が1本だけのとき、それが正解」が Part 2 で 85%・Part 4 で 61% だったものを、
正解を触らず誤答側に否定を配って 42%／36% に落とした（検査K の警告 12→0、113問に手が入った）。
**ただし第1波は4体中3体で内容が壊れ（致命的13本）、45本を着手前に戻して選定し直した。**
「本文に言及が無い」誤答に否定を足すと真になる、正解の根拠文を否定すると正解の裏返しになる、など**この軸に固有の失敗型が5つ**見つかり、CLAUDE.md に手順として残した。

## 状態：GitHub Pages へ公開できる状態を維持

初版からの変更点はすべて `assets/**` 内で完結しており、公開手順（`.github/workflows/pages.yml`）に
変更はない。まだ push していなければ、初版の README の手順どおりでよい。

## 収録内容

| 区分 | 内容 | 問題数 |
|---|---|---|
| 個別論点ドリル | 37論点（文法12・語彙7・文脈4・読解7・リスニング7） | 638 |
| 予想模試 Vol.1 | 標準回 | 200 |
| 予想模試 Vol.2 | リスニング強化回（図表・意図問題を増量） | 200 |
| 予想模試 Vol.3 | リーディング高負荷回（Part 7 の語数を上振れ） | 200 |
| 予想模試 Vol.4 | 語彙難化回（900帯の語を正解位置に） | 200 |
| 予想模試 Vol.5 | 総仕上げ回 | 200 |
| 予想模試 Vol.6 | 標準回その2（Vol.1 との解き比べで伸びを測る） | 200 |
| **合計** | | **1,838** |

各模試のパート構成は本番と完全一致（Part 1:6 / 2:25 / 3:39 / 4:30 / 5:30 / 6:16 / 7:54）。
ドリルは 23 ファイル・502 ユニット、模試と合わせて総ユニット数 1,120 件。

## 2026-09-12（3回目）の変更点：否定語の軸（検査K）を是正した

### 選定（メインが機械的に決めた）
128問＋予備19問。ハッシュ順＋ファイル別比例配分。本日書き直した vol2〜5 の No.161–175 は除外。
op A（正解が唯一の否定→誤答1本に否定）/ A'（誤答1本だけが否定→別の誤答にも）/ B（否定なし→誤答1本に否定）を混ぜた。
**A だけだと正解つきの設問が k=2 に流れ込み、k=2 側に新しい警告を作る**（着手前 P4 の k=2 は 9問中7）。

| Part | 着手前 k=1 | 最終 k=1 | z | 最終 k=2 |
|---|---|---|---|---|
| 2 | 26問中22 (85%) z=+5.55 | 33問中14 (42%) | +1.11 | 14問中10 (71%) |
| 3 | 38問中17 (45%) z=+2.81 | 38問中14 (37%) | +1.69 | 21問中11 (52%) |
| 4 | 31問中19 (61%) z=+4.67 | 39問中14 (36%) | +1.57 | 20問中12 (60%) |
| 7 | 86問中33 (38%) z=+2.86 | 96問中29 (30%) | +1.18 | 36問中19 (53%) |

第1波の分布は**事前計算と一致した**（正解を触らず誤答1本だけ動かす操作は分布を予測できる）が、**監査で内容が壊れていて45本を着手前に戻し**、
正しい選定基準で第2波（Part 2/4、17本）・第3波（Part 7、13本）を足して上の最終値に落ち着いた。
**最終的に手が入った設問は113問**（選定168・戻し45・飛ばし2）。工程は 実装4体 → 監査4体 → 直列修正 → 最終監査2体 → 直列修正＋第2波 → 監査 → 第3波 → 監査 → 直列修正。

| 計器 | 着手前 | 最終 |
|---|---|---|
| `validate.mjs` | エラー0・警告16（検査K 12） | **エラー0・警告4（検査K 0）** |
| 検査G〜Jの区画・数値 | — | **不変** |
| `dupcheck` 3軸 | 31/82/8/0 | **不変** |
| `setleak` | 602件 | 594件 |
| `titleleak` | 46件 | 46件 |
| `smoke.mjs` | — | **37/37 PASS**・console エラー0 |
| 否定語別（`never` / 正解） | 8本 / 0 → 第1波後 29本 / 0 | **11本 / 0** |

**監査で出た致命的は合計15本**（第1波13：ドリル4・Vol.1-2 4・Vol.3-4 4・Vol.5-6 1／最終監査1／第3波1）。すべて戻すか差し替えた。

### 渡した最重要規則と、それでも起きたこと
> **偽の文を否定すると真になる。**否定形の誤答は本文が肯定している事実を否定する形で書き、`why` にその肯定文を引用する。

- **impl2（vol3/4）は規則と逆のことをした。**「本文の事実と一致するが論点には答えていない」閉じ方を12件強で使ったと**自己申告**。
  ＝本文に照らして**真**の選択肢。`What is indicated…?` 型では第二の正解。引用した「肯定文」が新しい誤答を支持しているものを
  メインが9本拾った（`There is no minimum stay required.` ← 「No minimum」など）。
  **規則を文で渡しても、実装役は「安全策」として別の閉じ方に逃げる。**「真だが論点外」は排除根拠にならないと明記しておくべきだった。
- **impl3（vol5/6）は `never` に 39本中17本が集まった。**既存の `never` は8本すべて誤答側だったので、
  **「`never` があれば誤答」が 29/29** で成立する新しい指紋になった。分布目標（`not` 半分）を渡しても、
  「時制を問わず使える」語に収束する。**否定語の種類も、消す対象ではなく分布を目標にして、しかも実装役ごとに数えさせる**必要がある。
- **impl1（vol1/2）は `n't` に10本。**Part 2 の縮約で自然だが、担当ごとに1語に寄る癖は毎回出る。
- **新しい命題を持ち込むと相互ヒントが増える。**impl3 の方式（元の誤答を破棄し、本文の別の真実を否定する）は正しいが、
  別の箇所の語が同じセットの他の設問の正解と重なる（vol6 No.45 の `exercise` → No.44・46 の正解）。setleak の差分で23行・10組が新規。

### 運用
- 実装役の報告に「22問」と「24件」が混在するなど、数の自己申告は今回もずれた。全件を head2 と機械照合した（negverify.mjs）。
- impl1 は一括置換スクリプトのデフォルト引数のバグで「各ファイル最後の1件しか反映されていない」状態を**自分の git diff で発見**して直した。
- 数値制約の逸脱 53本（128本中）の大半は「`No`/`Not`/`There` で文を始めた」もの。否定文として自然な形なので、機械的に不合格にせずレビューに回した。

### レビューで分かった選定の穴（メインの設計ミス。CLAUDE.md に足す）
> **否定を足せる誤答は、本文と矛盾して閉じているものだけ。「本文に言及が無い」で閉じている誤答に否定を足すと、ほぼ必ず真になる。**
> 推測（`p7inf`）・意図（`p3int`）の誤答は元来「触れていないこと」で作るので、この型の設問は対象にできない。
> さらに **`p7not`（NOT 問題）は構造的に対象外**（誤答＝本文にある真の記述）、**裸の VP／NP を並べる選択肢形式**には否定語が載らない、
> **同一カテゴリの列挙**（連絡経路など）では否定が正解を名指しして矛盾対を作る。
> **機械選定は「設問の型」だけで選んだ。誤答がどう閉じているかは機械では分からないので、選定後に人が1本ずつ「矛盾で閉じているか」を見てから書かせること。**
- **Vol.5-6 では「正解の裏返し」が9問。**「本文が肯定している事実を否定せよ」だけ渡すと、実装役は最も鮮明な肯定文＝正解の根拠を選ぶ。
  → **「正解の根拠になっている文は使わない」を制約に足す。**
- **否定形の誤答を配れない stem がある**：`Why…?` `What mistake…?` `What is the stated reason…?` `What is she unable to do?`、営業時間など相互排他の一覧。
  答えの形をしていない1本か、正解の補集合になる。**選定は設問の型で除外できる**（stem の先頭語で機械的に弾ける）。
- **最終監査で見つかった4つ目の型**：「変化・理由・方法・問題」を問う設問（`What change…?` `Why…?` `How does…?` `What problem…?`）に否定を足すと、
  否定文が自動的に「変わっていない／問題ない」になって**設問の前提と衝突**し、本文なしで落とせる（25%→33%）。
  否定を足す先は**命題を述べる設問**（詳細・推測・意図）に限る。第2波の選定に stem の先頭語で除外を入れた。

### 今回やらなかったこと（残件。優先順）
1. **Vol.3/4/5 の Part 7 に、もう1本の再スキンがある（第3波の実装役が発見）。**
   `v3-p7-156`（Drummond Foods）・`v4-p7-158`（Marrow & Bean）・`v5-p7-156`（Brindlewood Insurance）は
   段落構成（「In 20XX 単一の変更」→「No other change was made」→「first six months」→「Full N-year tracking, unusual for a change this small」）まで一致し、
   3問とも設問の型と正解の装置が同じ（No.157/159 の正解はいずれも `It continued for N years rather than a shorter period`）。
   **前の巻の記憶で答えが出る。**今日直した 161–164／172–175 と同じ作業単位（1巻を残して2巻を書き直す）。
2. **Part 3 の否定語軸が z=1.69**（38問中14）。警告域ではないが閾値直下（**ドリルの Part 3 は6問中4で z=2.36**、母数8未満で検査は黙っている）。第2・3波と同じ基準で op B を10問ほど足せば 1.0 以下になる。
3. `v5q159` の誤答 B の `why` が事実誤認（書式は2区画までの保有を認めている）。解説のみの修正。
4. `v2q99` を着手前に戻した結果、`why` が「供給が止まったという記述はない」の「記載がない」型に戻っている（HEAD 由来）。
5. HEAD 由来の相互ヒント：vol6-l2a No.44/45/46 の正解3本が `exercise` を共有、vol6-l3 No.81 の4択で `copies` を持つのは正解だけ。
6. `v3q159` の誤答 B・C は「記載がない」型のまま（今回は A だけを触った）。

## 2026-09-12（2回目）の変更点

### この日にやったこと：残件1・1b・2 をまとめて閉じた（模試3巻の Part 7 を24問書き下ろし）

**残件1（`no=163` が4巻で同一挿入文・同一正解）・残件1b（`No.172`〜`175` のセット再スキン）・
残件2（巻をまたぐ内容語の重複5組）は、どれも同じ壊れ方の2スロットだった。**

測り直すと、vol2〜vol5 の4巻で **no=161–164 と no=172–175 が1本のテンプレートの4回再スキン**になっていた。

| スロット | 何が同じだったか |
|---|---|
| 161–164 | 「表面上の平均が挙動の異なる2集団を隠していた」記事。挿入文は4巻とも `The figure/number itself was unremarkable, at N percent.`、**正解は4巻とも [2]**。No.164 の stem は4巻完全一致 |
| 172–175 | 「単価は上がったが総額は下がった」報告書。No.175 は stem・正解の文言とも4巻一致、No.173 の正解も4巻一致 |

**vol2 を「残す1巻」とし、vol3・vol4・vol5 の2スロット計24問を文書ごと書き下ろした。**

正解位置の列（vol1→vol6）:
```
         着手前          →  現在
no=163   C B B B B C     →  C B D A B C
no=173   B A A A A C     →  B A D C B C
no=175   A B B B B B     →  A B D A C B    （B が5巻→2巻）
```

### 工程
書き下ろし（実装役3体・巻ごとにファイル分離）→ 監査（レビュー役3体）→ 修正 → **最終監査（3体）** → 最後の手当て（1体・直列）。
**残す1巻の vol2 も独立に監査した**（致命的0件。解説3箇所のみ是正）。

初回監査の結果は **致命的5件・要修正10件**。CLAUDE.md の「新規作問の約1/4に欠陥」「差し替えは6回中5回で新しい欠陥」がそのまま再現した。

### 機械検査（すべてメインが HEAD スナップショットと突き合わせ）
| 検査 | HEAD | 現在 |
|---|---|---|
| `validate.mjs`（着手前の版） | エラー0・警告4 | **エラー0・警告4** |
| `validate.mjs`（**検査K 追加後**） | エラー0・**警告16** | **エラー0・警告16** |
| `dupcheck`【2】巻またぎ同一スロット | 18組 | **8組** |
| `dupcheck`【3】内容語の巻またぎ重複 | 5組 | **0組** |
| `setleak`（相互ヒント） | 612件 | **602件** |
| `titleleak`（本日追加） | 47件 | **46件** |
| `smoke.mjs` | — | **37/37 PASS**・console エラー0・404も0 |

残る `dupcheck`【2】8組のうち **no=163・no=174 の4組は p7ins の位置一致**で、
6巻を4位置に配ると鳩の巣原理で必ず2組できるため**これが下限**。残り4組（no=98・102・117・121）は本件の範囲外。

### 新しく足した道具：`tools/titleleak.mjs`
**3人の実装役が、別々の題材・別々のファイルで、独立に「文書タイトルに第1問の答えを書く」事故を起こした。**
`render.js` は `title`・`head` を実際に描画するのに、**どの計器の視野にも入っていなかった**
（`setleak` も `dupcheck` も設問どうししか見ない）。わざと壊したデータで発火を確認済み（47→48→47）。
誤検知が大半の一次ふるいなので `validate.mjs` には入れていない。

### 新しく見つかった軸（既存の検査G〜J はどれも見ていない）
1. **時制**・**他動性** — vol3 No.161 は5つの計器を全部通過したうえで100%漏れていた
   （誤答3本が過去完了で正解だけ単純過去、正解だけが目的語を取る）。
2. **論理的に対立する2本** — `all tested clear` と `not all been cleared` を同じ設問に置くと
   「対のどちらかが答え」で4択が2択に落ちる。
3. **最小対** — 内容語 Jaccard が突出して高い2本があると同じことが起きる。
   全1,200問で Jaccard≥0.45 の9問中8問（89%、z=2.33）で正解がペアに入っていた。
4. **否定語**（下記の残件）。

### 3巡かけても閉じず、設問ごと作り替えた例（メインの設計ミス）
vol3 No.172 は「北面で何をすることに決めたか」を問うていたが、
**タイトル・stem・No.174 の挿入文・No.173 の stem が揃って文書の唯一の目玉事実を指していた**ため、
誤答をどう作っても閉じなかった:

| B の作り方 | 塞がる | 開く |
|---|---|---|
| 範囲を変える | 主題 | stem・タイトルが範囲を無償で与える |
| 北面で発破を続ける | 範囲 | No.174 の挿入文で落ちる |
| 別工法を出す | 範囲・174 | 主題語で落ちる |

**本文の未使用部分（苦情と保険会社）を問う設問に作り替えて決着した。**
CLAUDE.md の「型がそもそも4択に乗るかは作問を始める前に判断すること。これは指示を出す側の責任」に当たる。

### 運用上の知見
- **subagent の完了報告が、書き込みの完了より先に届くことがある。**
  vol3 の修正役の報告を受けて `grep` したところ何も変わっておらず、架空の報告と判断して別の役を再投入したが、
  **実際には最初の役が出力上限で中断したあと再開して書き込み**、2体が同じファイルを同時に編集する事態になった。
  幸い後の役が衝突に気づいて統合したが、**grep で変更が無くても、その役が完全に終了したと確認するまで再投入しないこと。**
- **共有スクラッチパッドでも衝突する。**実装役の1体が、メインの計測スクリプトと同じ名前のファイルを
  同じディレクトリに書いて上書きした。**メイン専用のディレクトリを分けること。**

### 今回やらなかったこと（残件。優先順）

1. **否定語の軸（新規発見。今回の中で最大）。`validate.mjs` の検査Kで毎回測れる。**
   **検査Kを入れると警告が 4件 → 16件**（検査Kが12件、**12件すべてが「否定側を選ぶと当たる」向き**）。
   **HEAD でも同じ12件・同じ区画**なので、今回の作業が作ったものではない。

   | 区画 | 該当 | 正解が否定側 | 偶然 | z |
   |---|---|---|---|---|
   | **模試+ドリル Part2** | 26問 | **22問 (85%)** | 33% | **+5.55** |
   | **模試+ドリル Part4** | 31問 | **19問 (61%)** | 25% | **+4.67** |
   | 模試6巻 Part7 | 69問 | 28問 (41%) | 25% | +2.99 |
   | 模試+ドリル Part3 | 38問 | 17問 (45%) | 25% | +2.81 |

   **Part 2 がいちばん重い。**正解が否定の応答になりやすく、誤答は肯定形で無関係な事実を言う書き癖。
   **検査Jの「形＝論点なら対象外」は Part 2 には当てはまらない**（Part 2 が問うているのは応答の適切さで、
   否定形そのものではない）。是正は**誤答側に否定語を配る**方向で行う（正解から否定を外すと逆の指紋になる）。
   **12区画に手を入れるので1セッションかかる。**
2. **巻をまたぐ Part 7 の残り**：`no=98` `no=102` `no=117` `no=121` の4組（HEAD から存在、本件の範囲外）。
3. **`verbal` 論点の「唯一の `-ing` を選ぶ」で 10/12（83%、z=+4.67）。**HEAD から存在。
4. **Part 6 の「述語になれる形が3/4本のとき定形側が100%正解」（17/17、z=2.38）。**
5. **`question.level` の新設。**
   最終監査で**レビュー役3体とも「新しい6ユニットは実質 lv4」**と判定したが、
   **CLAUDE.md の「部分的に直すと巻内で目盛りが割れる」に従い lv5 のまま据え置いた**
   （前セッションで33ユニットを HEAD に戻した判断と逆行するため）。設問単位で付け直すには `question.level` が要る。
6. **`titleleak` の残り46件**（HEAD から存在）。大半は「To: All 〜 staff」型の自明な偽陽性だが、上から読む価値はある。
7. **他スロットの再スキン**（本件の調査で新たに見つかった。未着手）:
   - `Two Gyms / Two Cinemas ×2 / Two Bakeries, One 〜, Two Answers` の4本
   - `Institute of 〜 — Membership Grades` の4本

## 2026-09-12 の変更点（1回目）

### この日にやったこと：文法・文脈系 Part 5/6 の一斉点検を、監査 → 修正 → 再監査3巡まで通した

**母集団は476問**（語彙系7論点を除く Part 5/6 の全数。ドリル282＋模試194）。
初回監査の内訳は **致命的61（12.8%）/ 重要72 / 軽微97 / 問題なし246**。
**着手前の見積もり「抜き取り10問で40%が致命的」は過大だった。**

工程は 初回監査16バッチ → 修正12ファイル → 再監査（第1巡）12件 → 追加修正 →
**最終再監査8件（本日）→ 手当て → 第3巡5件 → 手当て**。
HEAD 比で **stem・選択肢・正解が変わった設問は88問**、解説等のみの変更はその他多数。

### 最大の教訓：同じ型の設計を4回間違えた（すべてメインの判断ミス）

要求・提案の that 節（`It is essential that every participant ------- a signed waiver.`）の作問。

| 設計 | 何が起きたか |
|---|---|
| ① 誤答を全部非定形に（直説法の第二の正解を潰すため） | **「`to` も `-ing` も付いていないものを選ぶ」だけで7問とも当たる**（HEAD 0% → 100%） |
| ② 誤答1本を `will V` に戻す | `stipulate` `urge` `require` で**第二の正解**。**動詞ごとに割れる** |
| ③ 肯定形で4本とも定形（`will V` ＋ `have V-en` ＋ `were V-ing`） | **`have V-en` は完了仮定法として正文**。4問同時に第二の正解 |
| ④ **`not` を空所の前に置く**（最終） | 直説法・完了仮定法・助動詞が**語順で同時に落ち、トリガー語も効く** |

**③ の誤りが一番重い。**「要求提案の that 節の述語は原形」なら **HAVE の原形は `have`** なので、
`have been approved` は**完了仮定法として正しい英語**。**メインがテンプレートに書いた「数の不一致」が事実誤認**だった。
2人の監査役が独立にコーパスで確認している
（`crucial that Britain have the support of France` / `requirement that the applicant have been resident` ほか）。

**型の一覧の初版が正しかった。**「閉じているのは `subj-28`（`not` 先行）だけ」と最初から書いてあり、
**メインが肯定形に寄せたのが誤り**だった。動機（本番は肯定文が多い）は妥当だったが、
**測っている力が論点と一致していること**のほうが上位。

### 追加した検査（`tools/validate.mjs` の検査J）
**定形／非定形の軸**。既存の検査G（語数）・H（複合/先頭語）・I（受動態）は
**上の①の欠陥をどれも検出しなかった**（語数も複合も先頭語も受動態も動いていないため）。
- 軸は「定形か」ではなく **「その位置で述語として使える形か」**。
  **`be V-en`（原形受動）を非定形に数えると7問中3問を取り逃がす**（既存の `finite.mjs` が実際にそうなっていた）。
- 壊したデータで `--extra` 経路を含めて発火を確認済み（メインが独立に再現。z=5.48）。

### 機械検査の結果（すべてメインが HEAD と突き合わせて確認）
| 検査 | HEAD | 現在 | 備考 |
|---|---|---|---|
| `validate.mjs` | エラー0・**警告3** | エラー0・**警告4** | 下記 |
| `smoke.mjs`（実ブラウザ） | — | **37項目 PASS / 0 FAIL** | console エラー0・404も0 |
| `setleak.mjs`（相互ヒント） | 612件 | **612件・差分0** | 相互ヒントを1件も増やしていない |
| 選択肢集合の完全一致 | 38組 | **32組** | |
| 4本中3本一致 | 82組 | **81組** | |
| 巻をまたぐ内容語の重複 | 5組 | 5組 | **未着手。下記の残件** |

**警告が1件増えた理由**：Part 6 の「述語になれる形が3/4本のとき定形側が100%正解」。
**HEAD で 12/12・z=2.00 の既存の癖を、今回 17/17・z=2.38 に強めた**
（構造で閉じ直すときに誤答が定形寄りになるため）。抜け道は「非定形の1本を選ばない」だけで **25% → 33%**。
直すには「Part 6 で非定形が正解になる設問」を新しく書く必要があり、**新規作問は6回中5回で新しい欠陥を生む**ので見送った。

### 模試の level を HEAD に戻した（33ユニット）
CLAUDE.md が「模試の level 付け直しは方針判断により未実施」と記録しているのに、
前の巡で vol1〜5 の33ユニットだけ 5→3 / 4→3 に変えていた。**部分的に直すと巻内で目盛りが割れる**ので戻した。
**この33ユニットは、監査の判定（lv3 相当）とはずれた状態のまま**である。
設問単位で正確に付け直すには `question.level` の新設が要る（`level` はユニットにしか無い）。
**例外3件**：`v3-p5-113r2` `v4q106r` `v4q115r` は**設問そのものを差し替えた新規作問**なので、
HEAD の値を流用せず目盛りに当て直した。

### 前セッションの作業記録が消えていた
`/tmp` 配下のスクラッチパッド（監査16バッチ・修正12件・再監査12件の報告、`followups.tsv`、引き継ぎ書）が
**丸ごと無くなっていた。**トランスクリプトから台帳231行・型の一覧・申し送り49行・引き継ぎ書を復元したが、
**報告本文は失われたまま。作業記録を `/tmp` 配下だけに置くと消える。**

### 監査そのものの取りこぼしが見つかった
- **偽の規則を、規則の片面でだけ捕まえていた。**初回監査は「裸の `none` は2者でも実在」を**致命的として格上げ**し、
  「比較級＝2者」も正しく名指ししているのに、**裏面の「`neither` / `either` に数の上限は無い」へ展開しなかった。**
  全1,838問を走査して**誤答が開いているもの6件・解説だけが誤っているもの12件**を新たに検出した。
  **`topics.js` に供給源は無く、各実装役が学校文法の常識として独立に持ち込んでいた**（中央を直しても再発する）。
- **同じ設問の中でも片面だけ直していた。**`p6c-04-1` は誤答 `these` を「規則不成立」で差し替えながら、
  **まったく同じ構文の隣の `those` を残していた。**
- **`ptcp-10r` の誤答 `Secured` は HEAD から開いたまま、3巡の監査を素通り**していた。
  しかも排除根拠が2回続けて偽で（第2巡の案を実装役が正しく反証し、置き換えた新根拠も偽）、
  **アプリ自身が `vocab.js` で `She was assigned a mentor.` を正しい与格受動として教えている**のと矛盾していた。

### 運用上の知見
- **報告と実データの食い違いは今回も複数件。**引き継ぎの分類で「解説のみ」とされた6問（`pron-13` `quant-09`
  `quant-10` `comp-18` `verbal-08` `ptcp-20`）は**実際には選択肢が変わっていた。**
  採番し直しの基準も、台帳由来（全件採番）と申し送り由来（全件据え置き）で**割れていた。**
  **同じ規則を2つの経路に当てるときは、経路ごとに担当が分かれるので必ずずれる。**
- **並行する実装役は互いの成果を照合できない。**2人が同時に同じ選択肢集合を作り、
  別の2人は同じ設問の直し方として**独立に同じ `off` を思いついて**枠まで一致させた。
  **メインが、全員の作業が終わったあとに選択肢集合の照合を1回かけること。**
- **メインが型を文字列で渡すと、実装役はそのまま写す。**4問が同じ骨格になり、
  **文字列の一致ではないので機械照合には掛からなかった**（実装役の1人が自分で気づいて報告した）。
- **`assets/` を分けても `tools/` は全役が実行するので衝突する。**実装役が `validate.mjs` を編集中の状態を
  別の役が実行し、「起動不能で検査が回せない」と報告してきた（完成後は正常）。
- **判定が割れたときの決め手は毎回同じだった：ヒット件数を比べず、ヒットを1件ずつ開いて中身を読む。**
  `essential` `require` `either` の3件とも、**件数だけ見ていたら逆の結論**になっていた。
  加えて **Wikipedia の insource 正規表現は literal を前置しないと API がエラーを返し、それが「0件」に見える**
  （実装役が `urge` を「0件＝閉じる」と報告した根拠がこれ。正しく測ると真正4件）。

### 今回やらなかったこと（残件。優先順）

> **【2026-09-12（2回目）で更新】この節の残件1・1b・2 は解消した。**
> 最新の残件は冒頭の「2026-09-12（2回目）の変更点」を見ること。以下は当時の記録として残す。


1. **模試の同じスロットで、選択肢と正解が巻をまたいで一致している組が18**（HEAD では30）。
   **`node tools/dupcheck.mjs` の【2】で毎回測れる。**
   **`no=174`（Part 7 の位置選択）は 2026-09-12 に是正済み**（5巻すべて正解 `[3]` だったものを
   巻ごとに位置と閉じ方を変えた。10組 → 0組）。**残っているのは主に `no=163` の7組**で、
   vol2〜vol5 の4巻が `The figure/number itself was unremarkable, at N percent.` とほぼ同一の挿入文で、
   **4巻とも正解が `[2]`**。No.174 と同じ作業量で直せる。
   **`balance2.mjs` は `p7ins` を構造的に除外するので自動では直らない。**

1b. **No.174 を直しても、その文書のセットは直っていない。**
   `No.172`〜`No.175` は1文書4問のセットで、**vol2〜vol5 の文書が互いの再スキン**
   （語彙の重なり Jaccard 0.49〜0.59。vol6 だけ別物で 0.06〜0.09）。
   **`No.175` は vol2〜vol5 で stem・正解の文言・記号(B) まで完全一致**、`No.173` も正解の文言と記号(A)が4巻一致、
   本文の結びの一文（`…treat unit price as a poor guide to lifetime cost in this category.`）が**4巻で一字一句同じ**で、
   それが `No.175` の正解の根拠そのもの。**是正前の No.174 より強い漏れ。**
   このセットは**4問中3問が前の巻の記憶で解ける。**16問を文書ごと作り直す作業単位（監査2巡見込み）。
2. **巻をまたぐ Part 7 の内容語の重複5組（HEAD から存在）。**
   **`no=172` は vol3・vol4・vol5 の3巻で選択肢4本と正解が完全一致**（`They are part of another scheme.`）。
   **`no=175` は vol2・vol3・vol5 で3本一致＋正解同一**（`It is a misleading measure in this category.`）。
   **同じ設問番号のスロットで正解の文字列がそのまま同じ。**
   「Vol.1 と Vol.6 の解き比べで伸びを測る」という設計の根幹に当たる。Part 7 を3巻ぶん書き直す作業単位。
3. **`verbal` 論点の「唯一の `-ing` を選ぶ」で 10/12（83%、z=+4.67）。**HEAD から存在。
   ファイル単位の合計は健全なので、区画の切り方では見えていなかった。
4. **Part 6 の「述語になれる形が3/4本のとき定形側が100%正解」（17/17、z=2.38）。**
   上記のとおり今回強めた。非定形が正解になる Part 6 の設問を書けば解消する。
5. **模試の level 33ユニット**が監査の判定とずれたまま（方針により HEAD に戻した）。
   `question.level` の新設とセットで扱う。

## 2026-09-02 の変更点

### この日にやったこと：受動態の軸（前回の優先順位 #1）を閉じた

前回（2026-08-28）の是正で「受動態が1本だけ＝正解」が Part 5・6 で 69%→8% になった一方、
全パート合算では 23%→15%（z=−3.39）へ逆に振れていた。今回、模試6巻の**誤答側の受動態を能動態に書き換える**方式で是正した。

**着手前**（作り直した計測器で再測。前回の 31/209 と定義がわずかに違うが率は一致）:

| 区画 | k（受動態を含む本数） | n | 正解が受動 | z |
|---|---|---|---|---|
| 4択合算 | 1 | 156 | 23 (15%) | −2.96 |
| Part 2 | 1 | 48 | 8 (17%／偶然33%) | −2.45 |
| Part 7 | 2 | 48 | 15 (31%／偶然50%) | −2.60 |
| Part 3 | 3 | 13 | 6 (46%／偶然75%) | −2.40 |
| Part 6 語句 | 2 | 17 | 17 (100%) | +4.12 |

**k 別に分けて初めて見えた**：漏れは k=1 だけではなく、Part 7 の k=2（受動態2本→どちらも誤答）、
Part 3 の k=3（唯一の能動＝正解）にも同じ書き癖が出ていた。

**是正後**：4択合算 k=1 は 116問中23問（20%、z=−1.29）。パート×k の全区画で |z|≤1.35。巻別も |z|≤1.36。
**残したもの**：Part 6 語句（動詞活用問題）の k=2 は 14/14 のまま。`must be disclosed* / must be disclosing / was being disclosed / disclosing`
のように**受動態そのものが問われている文法論点**で、k=0 の語句問題は全部が接続詞・副詞の語彙問題だったため
「能動が正解で受動の誤答を持つ」問題を作る素材が無かった。**文法論点と交絡している区画なので、形の漏れとは数えない**（判断はメイン）。
Part 1 の k=2（14問中3問）も残置（線画の描写文で、`is being + 過去分詞` は論点そのもの）。

**工程**：実装役3体（巻2つずつ）→ メインが HEAD と機械照合（`verify.mjs`）→ レビュー役2体が1本ずつ本文と突き合わせ →
指摘を実装役に戻す、を**3巡**。最終の第4巡は第3巡の変更箇所だけを1体が監査。
レビュー指摘の推移：第1巡 致命的3・要修正32・軽微26（101本中）→ 第2巡（第2巡の変更のみ）致命的0・要修正18・軽微13 →
第3巡（第3巡の変更のみ）致命的0・要修正2・軽微5 → 第4巡で残り8箇所の言い換えを確認して確定。**致命的3件はすべて第1巡で出た**：偽の規則（vol3 No.131 の why）、HEAD 由来の第二の正解（vol2 No.200）、
HEAD の `It is closed` を叙述形容詞と見ずに書き換えて懸垂分詞を作った（vol6 No.76）。後2者は HEAD に戻すか id を採番して差し替えた。

### やり方（前回までの失敗を踏まえて変えた点）

1. **どの設問を直すかをメインが機械的に選んだ**（84問）。ハッシュ順＋「巻ごとの不足分に比例した決定的乱択」。
   単純に不足の大きい巻から取ると vol3 の Part 2・7 から受動態が全滅した（0/25）ので、重み付きにして散らした。
2. **正解は原則触らない。**誤答の受動態を能動に（M1・M3）、または誤答を受動態に（M6・M8）。
   正解の言い換え（M5）は最終的に **vol5 No.39 の1問だけ**（命題不変なので id 据え置き）。他の2問は誤答側の能動化に切り替えた。
   **id を採番し直したのは2問**——`v2q200`→`v2q200r`（HEAD 由来の第二の正解を閉じた）、`v5q139`→`v5q139r`（同）。この2問だけ復習履歴が切れる。当初計画していた「正解を受動態に言い換えて的中を増やす」（M2、9問）は
   候補が `Around half past four.` `A late-return fee` のような名詞句ばかりで**成立しないので全部やめた**。
3. **書き換えの制約を数値で渡した**：語数 ±1、正解の語数順位を変えない、and/or・フィラーを足さない、先頭語は原則維持。
   結果、語数の順位分布・最長的中率・同語数率・フィラー率は**着手前と小数点以下まで同じ**（別の軸を壊さなかった）。
4. **報告は全部 HEAD と機械照合した**（`verify.mjs`）。3体の報告（27本・36本・38本）は差分と一致。
   選定外の変更0、stem・answer・id・本文の変更0。

### それでも生まれた指紋（3つ。全部その日のうちに戻した）

| 何が起きたか | どう見つけたか | 対処 |
|---|---|---|
| 能動化した誤答の主語が **`They`** に揃った（27本中20本の巻もあった） | 先頭語別の正解率を HEAD と比較。Part 2 の `They` 始まりが 3/13 → 3/24（z=−2.17） | 3分の1以下に制限。自動詞・能格動詞（`It closes at…` `went through`）、本文にいる主語、`We` に分散 |
| 次に **`Someone` / `Nobody` / `No one`** に揃った（14本） | 同上。模試全体で Someone 始まり 20本中17本が誤答 | 全14本を書き直し |
| 1設問の中で**正解だけが先頭語で浮く**形を新たに作った（vol1 No.43、vol3 No.72） | `verify.mjs` に先頭語の孤立判定を足して検出 | No.43 は正解を HEAD に戻し誤答3本を能動化。No.72 は先頭語を変えて 2-1-1 に |

**「主語を足せ」と言えば書き手は同じ主語を使い続ける。**「They を避けよ」と言えば Someone に集まる。
**消す対象を名指しするのではなく分布を目標にする**（2026-08-25 の教訓）がここでも当てはまった。

### 計測器の誤検知（2種類。実装役が気づいた）

- **所有格の `'s` を be の縮約と誤認**：`the neighbour's shed`（`shed` は不規則過去分詞形）、`the shop's limited` が受動態に数えられた。
  vol3 No.71 が選定リストに載り、実装役が「書き換える対象が存在しない」と報告して判明。
- **`-en` / `-ed` で終わる非過去分詞**：`is open` `is between` `are even` が受動態に数えられた。
判定が変わった選択肢は13本。修正後の定義で測り直し、上の数字はすべて修正後。**ヒットが1件だけのときはまず照合条件を疑う**の再演。

### 恒久的な検査を1つ足した

`tools/validate.mjs` **検査I**：受動態の軸。検査Hと同じ粒度（1巻×1パート／合算）で、k 別に「正解が受動態側」の率を k/選択肢数と比べ、
n≥8・|z|≥2.0 で WARN。わざと壊した合成データ（14問、受動態1本が常に誤答）で発火、健全な合成データで不発火を確認。
現在の WARN は2件（Part 1 合算 k=2、Part 6 合算 k=2）で、いずれも上の「残したもの」。

### 副産物として測れた、まだ直していないもの

**Part 2 の先頭語**（着手前から存在。今回は触っていない）:

| 先頭語 | 正解率 | 偶然 | z |
|---|---|---|---|
| `Yes` | 1/59 (2%) | 33% | −5.16 |
| `It` | 6/58 (10%) | 33% | −3.71 |
| `The` | 12/88 (14%) | 33% | −3.92 |
| `I` | 14/26 (54%) | 33% | +2.22 |

「`Yes` で始まる応答は誤答」が 98% 正しい。wh 疑問文への Yes/No は本番でも定番の誤答だが、
**Yes/No 疑問文の正解が Yes で始まる例が1問しかない**のは書き癖。Part 6 の `will` 始まり（12/20=60%）も同種。

### 直さずに残したもの（今回のレビューで見つかった HEAD 由来の欠陥。次回以降の材料）

| 場所 | 内容 |
|---|---|
| vol2・vol3・vol4 No.200／vol2・vol3・vol5 No.199 | **正解の文言が3巻で逐語一致**（`It is not within the company's discretion.`／`It falls within the included allowance.`）。196–200 の3文書セット自体が4巻の再スキン。2026-08-28 の「33問」の一部だが、今回 vol2 No.200 の誤答を触っていて改めて露出した |
| vol2〜vol5 の `What does the report conclude about unit price?` | 4巻に同一 stem・同一正解 (B)・同一 exp。記憶だけで解ける4問 |
| `vol4-r3.js` 172–175 ↔ `vol5-r3.js` 172–175 | ほぼ同一文書の再スキン（噴水交換／灌水制御装置）。設問4本すべて同型 |
| vol4 No.139・No.142 | 同じ巻の別文書で正解がどちらも `must (also) be reported` |
| vol6 No.74 | 正解だけが `To …` 不定詞（他3本は前置詞句）。**ただし6巻通算で「単独の To 不定詞が正解」は21問中この1問だけ（5%）なので、揃えると逆の規則性が強まる。直さない** |
| vol5 No.147 | 正解だけが `and` を含む（検査Hの複合軸の個別事例） |
| vol4 No.167 | 先頭語 The/The/The/A で正解が A |
| vol1 No.159 (B) | `the client` が依頼者と依頼者の顧客の二通りに読める（HEAD から） |

### 次に着手するときの優先順（2026-09-02 時点）

| # | 内容 | 規模 |
|---|---|---|
| 1 | 未点検の文法系 Part 5/6 が413問（前々回からの積み残し。過去の抜き取りで40%に致命的欠陥） | 最大 |
| 2 | 設問文を変えないと閉じない漏れ10件（vol4 No.186 は文書を一切読まずに解ける） | 中 |
| 3 | 模試 Part 5 の解説が薄い（180問に語注ゼロ。オブザーバーが1位に挙げた項目、未着手） | 中 |
| 4 | 金額・時刻の選択肢が C に45%偏る（p<0.001）。自動ツールが構造的に届かない | 中 |
| 5 | **Part 2 の先頭語**（`Yes` 1/59、`It` 6/58、`The` 12/88、`I` 14/26）。今回の計測で初めて数値化。着手前から存在 | 中 |
| 6 | **巻をまたぐ再スキン**（上表の No.199/200・unit price・172–175）。2026-08-28 の33問と合わせて方針が要る | 中 |
| 7 | ドリルの図表が1セット2問のまま／listening2.js Part 3／模試 Part 1（受動態 k=2 の逆向き z=−2.14 も Part 1） | 小 |

**受動態の軸で残した2件**（Part 6 語句 k=2＝14/14、Part 1 k=2＝3/14）は `validate.mjs` 検査Iが WARN を出し続ける。
**どちらも受動態が論点そのものの区画で、形の漏れとは数えない**（判断はメイン。2026-09-02）。
「エラー0・警告0」の運用に戻すなら、検査Iから Part 1 と Part 6 の動詞活用問題を除外する改修が要る——
ただし除外すると Part 6 の文挿入（p6ins）まで見えなくなるので、`p6ins` だけ残す形にすること。

## 2026-08-28 の変更点

### この日いちばん重要なこと

**「選択肢の長さで正解が分かる」という1つの指摘を掘ったら、同じ書き癖から6種類の漏れが出てきた。**

きっかけは前回のオブザーバーの指摘（「選択肢の長さで37〜52%当たる」）。全模試1,200問を実測すると、
**「一番長い選択肢を選ぶ」だけで区画により 35〜76% 当たった**（偶然は4択25%・Part 2 の3択33%）。

原因は書き癖ひとつ——**正解は根拠に忠実に丁寧に書き、誤答は短く済ませる。**
そしてその癖は、長さ以外にも同じ形で現れていた:

| 漏れの軸 | 実測（着手前） | 是正後 |
|---|---|---|
| **語数**（一番長いのを選ぶ） | 区画により 35〜76% | 全区画 17〜37% |
| **複合構造**（`and`/`or` を含む1本だけが正解） | 74問中48問 = **65%**（z=+7.9） | 是正中 |
| **先頭語**（1本だけ他と違う） | 120問中55問 = **46%**（z=+5.3） | 是正中 |
| **受動態**（`be+過去分詞` が1本だけ） | 35問中24問 = **69%**（z=+6.0） | 12問中1問 = **8%** |
| **並べ替え不可の選択肢が C に偏る** | 60問中27問 = **45%**（p<0.001） | **未着手** |
| **セット内の相互ヒント** | 検出手段が無かった | `tools/setleak.mjs` を新設 |

**Part 5 だけは全部健全だった**（語数23%、位置も偏りなし）。選択肢が1語なので書き癖が乗らない。

### 是正するたびに新しい指紋が生まれた（この日だけで5回）

| 指示 | 生まれた指紋 |
|---|---|
| 「語数を揃えよ」 | 4本全部が同語数（vol5 Part 2 で36%） |
| 「誤答を伸ばせ」 | フィラー（`really` `actually`）が誤答に集中（vol5 Part 2 で26%、他巻の8倍） |
| 「単独最長を潰せ」 | **単独最長は絶対に正解でない**（Part 3 全6巻で7%）→ 一番長いのを消せば3択 |
| （同じ指示の別の副作用） | **正解が2位に集中・4位が枯渇**（Part 4 で 2位39%・4位13%） |
| （メインの測定式の誤り） | 健全な区画を是正させかけた（Part 6 の母数の切り方） |

CLAUDE.md には 2026-08-25 時点で「**消す対象を名指しするのではなく、偶然そうなった分布を目標として
与えること**」と既に書かれていた。**書いてあったのに同じ罠を踏んだ。**
今回はそれを、図表の行だけでなく**選択肢のあらゆる属性に同じ罠がある**という形に一般化して記録した。

**4つ目は「誤答を伸ばす」という手段そのものに内在する。**正解を削らない限り、正解は1位から2位へ
移るだけで最短にはならない。**手段を1つに固定した時点で、この結果は決まっていた。**

### 偽の文法規則が2つ、複数ファイルに伝播していた

| 偽の規則 | 広がり | 実害 |
|---|---|---|
| **選択疑問に `Yes` は不可** | 7ファイル・14箇所（供給源は `topics.js` の `pitfall`） | **3問が第二の正解**だった |
| **`Why don't we …?` に `Because` は誤り** | 3ファイル | 是正中 |

1つ目は、プロジェクト自身が **2026-08-24 に Cambridge *English Grammar Today* と
Huddleston & Pullum (2002) を根拠に誤りと確定させていた**のに、`topics.js` の `pitfall` に残っていて
そこから広がっていた。**閉じ方の規則（`or` の後ろを独立した節にしない）も `vol6-l1.js` のコメントに
しか書かれておらず、Vol.1〜5 に波及していなかった。**

2つ目は**自己矛盾している**——同じ解説が「障害を挙げる応答が正解」と書いた直後に
「Because で理由を答えるのは誤り」と続けている。`Because they start on different dates.` は
まさに障害を挙げた応答。**区別しているのは中身ではなく接続詞の有無だけ。**

> **共通の構造：応答の中身を見ず、冒頭の語（Yes / Because）だけで切っている。**

### 第二の正解が27件見つかった（うち19件は元からあったもの）

**この作業の最大の成果は、語数の是正そのものではなく、その過程で露出した第二の正解である。**

| | 件数 | 内訳 |
|---|---|---|
| **元からあったもの** | **19件** | 選択疑問の Yes 誤答6件（偽の規則が隠していた）／Part 2 の間接応答7件／その他6件 |
| **是正が新たに作ったもの** | **8件** | `already` を足して「もう済んでいる」になった／`at the bar` が音声と一致して真になった／限定句を足して命題が成立した など |

**8件はすべて監査で捕まえた**（利用者に届く前に塞いだ）。
**19件は、選択肢に手を入れるために1問ずつ読んだから見つかった。**
語数を測るだけでは絶対に出てこなかった。

**Part 2 に集中している。**選択肢が短い応答文なので、**1語足しただけで成立する応答に変わる**ため。
是正が第二の正解を作った率は、Part 7（読解）では **0件**、Part 2 では **vol5 だけで3件**だった。

代表例:
- `vol1-l1.js` No.135 (A) `issue` — **American Heritage が自動詞として
  "to be circulated or published: *books issuing from a publisher*" を立項**しており、
  `fobs issue from the management office …` が成立していた。**辞書を引いて初めて分かる型。**
- `vol3-l1.js` No.23 (C) `The printer's out of ink.` — 「今はどちらもできない」と
  **二択の前提ごと退ける応答**。選択疑問はこれを許すので成立していた。
- `drills/listening.js` `p2w-04` (C) `It moved last month.` — 「先月ずらしたばかりだ」＝
  **また動かすことへの障害**。同じファイルの `p2i-03b` では**同じ装置が正解になっている。**

### 正しい対処が既にデータの中にあり、横展開されていなかった（4件）

| 既にできていた場所 | できていなかった場所 |
|---|---|
| `vol6-l1.js` のコメント「**`or` の後ろを独立した節にしない**」 | Vol.1〜5 に波及せず、**3問が第二の正解**だった |
| `vol1-l1.js` No.24 が `It moved **to the third floor**.` と**着点を場所に固定**して閉じている | `listening.js` `p2w-04` は着点が無く、第二の正解になっていた |
| `vol2-r1.js` No.143 が**誤答にも受動態**を置いて漏れを防いでいる | 他の11問は正解だけが受動態（**69%**で当たる状態） |
| 2026-08-25 に模試の図表を「1セット1問」へ揃えた | **ドリルには適用されていない**（`listening4.js` に2問構成が残存） |

**CLAUDE.md の「バグを直したら、同型のファイル全部に波及させ、記録を残すこと」が守られていない。**
今回は、規則の記録場所を**ファイル内コメントから CLAUDE.md 本体へ引き上げる**ようにした。

### 巻をまたいで「正解の文言が完全一致」する設問が33問（測定で境界が確定した）

作業中に実装役が気づき、メインが全818問（4語以上の正解を持つもの）で照合した。

```
設問    Part  巻                    正解の文言
No. 88  P4   vol2/vol4             "Leaving a message for a same-day callback"
No. 99  P4   vol2/vol4             "It will not arrive in time without a change."
No.164  P7   vol2/vol3/vol5        "Averages can conceal distinct groups within the data."
No.169  P7   vol3/vol5             "To report on an examination and recommend limited repair…"
No.171  P7   vol3/vol5             "To warn that the repair will not last without further…"
No.172  P7   vol3/vol4/vol5        "They are part of another scheme."
No.173  P7   vol2/vol3/vol4        "The unit price rose but total spending fell."
No.175  P7   vol2/vol3/vol4/vol5   "It is a misleading measure in this category."
No.189  P7   vol2/vol3             "Return a completed approval form"
No.195  P7   vol2/vol5 ・ vol3/vol4 "(S)he cannot use the letters until the grade is confirmed."
No.199  P7   vol2/vol3/vol5        "It falls within the included allowance."
No.200  P7   vol2/vol3/vol4        "It is not within the company's discretion."
```
**13グループ・33問。すべて設問番号まで一致している。**

> **巻ごとの関与数：vol2 = 9／vol3 = 10／vol4 = 7／vol5 = 7。**
> **vol1 と vol6 は1件も関与していない。**

**これは前回の引き継ぎの推奨「利用者に渡すなら Vol.1 → Vol.3 → Vol.6 の順で」を測定で裏付ける。**
- **Vol.1 と Vol.6 は、他のどの巻とも正解文言を共有していない。**
- Vol.3 は10グループに関与するが、**相手はすべて Vol.2・4・5**。その3巻を解かなければ露出しない。
- **したがって Vol.1 → Vol.3 → Vol.6 の3巻は相互に完全に独立している。**
- 逆に**6巻すべてを解くと13問がタダで解ける**。測定器としてその分スコアが上振れする。

2026-08-18 に「Vol.1〜5 の Part 7 が互いの再スキン」と記録されていたが、
**「正解の文言そのものが一致している」という水準までは測られていなかった。**
是正には33問の正解文言の書き直しが要る（第二の正解を作るリスクを伴う）ため、**未着手。本人の判断待ち。**

### 最終状態（全1,838問）

```
【模試6巻】     n    最長選択     単独最長が正解   順位分布
  Part2       150   32%(z=-0.3)  30%(z=-0.7)   32/35/32
  Part3       211   29%(z=+1.5)  24%(z=-0.2)   29/28/20/23
  Part4       166   27%(z=+0.7)  22%(z=-0.6)   27/31/20/21
  Part6        49   28%(z=+0.4)  24%(z=-0.1)   28/31/22/20
  Part7       259   24%(z=-0.4)  23%(z=-0.6)   24/23/28/26
  Part1        36   39%(z=+2.0)  44%(z=+1.9)   39/31/14/16   ← 未着手（下記）
【ドリル23】    全パート 最長選択 21〜39%、|z| < 1.7
【形の軸】      複合・先頭語とも全パートで |z| < 1.4
【validate】    エラー0件・警告0件（巻別＋合算の両方）
```
**着手前は「一番長い選択肢を選ぶ」だけで 35〜82% 当たった。**

### 残っている2つ（判断が要る）

**1. Part 1（模試36問）が 最長選択 39%・単独最長が正解 44%（z=+2.0/+1.9）。**
1巻6問なので CLAUDE.md は「人が見ること」としており、**今回は触っていない**。
全6巻を合算すれば n=36 で機械判定できる母数だが、**Part 1 は線画の描写文**なので、
語数を動かすと絵との対応が崩れる恐れがある。**絵を見ながらの作業になる。**

**2. 受動態の軸が、是正によって逆向きに振れた（今日7回目の「別の指紋」）。**
```
着手前:  51/225 = 23%  z=-0.81   （健全）
現在:    31/209 = 15%  z=-3.39   ← 悪化
```
Part 5・6 で「受動態が1本だけ＝正解」が 69% だったのを 8% に直した。**その区画では正しい是正**だが、
**全パートを合算すると逆に振り切れた。**いま「受動態を含む選択肢が1本だけのとき、それを消す」が
**85% 安全**（偶然75%）。**着手前との比較で、我々の作業が原因だと確定している。**

閉じるには (a) Part 3・4・7 で「正解が受動態」の設問を増やす（内容の変更を伴う）か、
(b) 誤答に2本目の受動態を足して母数から外す（85問規模）。**いずれも相応の作業量。**

**この2件は、今日の作業で初めて測れるようになったもの。**

### 見落とし1件（記録）
`drills/listening2.js` の Part 3 が n=8・最長選択 69%。
**母数8で検査G（10件以上で判定）が発火せず**、メインのファイル別スクリーニングも
`z>=1.5` の閾値で拾えなかった。**閾値による足切りは、小さな母数の漏れを構造的に見逃す。**

### 恒久的な検査を3つ足した

| | 何を見るか | 検証 |
|---|---|---|
| `validate.mjs` **検査G** | 語数から正解が漏れていないか（最長側・最短側の両方） | メインが**別に作った**合成データで発火・不発火を確認 |
| `validate.mjs` **検査H** | 複合構造・先頭語から正解が浮いていないか | 同上 |
| `tools/setleak.mjs`（新設） | セット内・文書内の相互ヒント（2方向） | 同上。既知の致命的3件が上位に出ることも確認 |

**検査Hは追加直後に、元からあって誰も気づいていなかった欠陥を検出した**——`vol5-l3.js` で
`and`/`or` を含む1本が正解、HEAD 時点で**8問中8問（100%）**。
「検査が無い箇所は壊れても誰も気づかない」がその日のうちに実証された。

`tools/setleak.mjs` は CLAUDE.md が長らく「**機械照合では捕まらない／読んで気づくしかない**」と
していた型を、**「正解だけが持つ語」に限定すれば拾える**と分かって作ったもの。
実際に「**先読みだけで100%解ける**」漏れを `vol3` Part 7 で1件捕まえた。
**ただし誤検知が大半（約640件中、実害は数%）。合否判定には使えない一次ふるい。**

### 画面上で壊れて表示される設問が2件見つかった

| | 本文 | 症状 |
|---|---|---|
| `vol5-r1.js` No.143 | `loaned laptops {{1}} returned to IT` | `must be returned returned to IT` |
| `drills/context2.js` p6c2-02-1 | `the bay {{1}} closed for nine days` | **4本すべて** `closed closed for nine days` |

空所の直後に、選択肢と重複する語が残っていた。**`validate.mjs` は空所の個数は数えるが、
空所に選択肢を入れた結果までは見ていない。**全データを3方式で走査して同型はこの2件だけと確認済み。

### スクリプトの矛盾を1件是正した

`vol1-l2a.js` の No.38–40：`That's four Saturdays in a row.`（4週連続で下落）と
`The three before that were our best of the season.`（その前3週は今季最高）が両立しない。
`four` → `two` の1語修正で、**むしろ設問が良くなった**——直近2週＝雨の2週、その前3週＝今季最高、と
重なりが消えて天候と売上が一対一で対応し、No.39 の正解の根拠が強まった。

### 報告文と実データが食い違った例が3件

**すべて別の役かメインが `git diff` / `git show HEAD:` と機械照合して発見している。**

| 報告 | 実際 |
|---|---|
| 「正解を短縮したのは2問」 | 3問 |
| 「残る相互ヒントはすべて既存」 | 2件は本人が足した語が原因 |
| 「この設問を HEAD の文言に戻した」 | 戻っておらず、正解の文言まで変わっていた |

**悪意ではなく、長い作業の途中で自分が何をしたか見失うために起きる。**
CLAUDE.md に「報告文は当てにならない。差分で裏を取ること」として記録した。

### メインの検査ツールが3回誤った

1. `flat.mjs` が金額・日付の4択（`£55｜£68｜£95｜£103`）を「全部同語数」に数え、
   vol4 Part 7 を「揃えすぎ 29%」と誤判定した（実際は語彙型42問で2%）。
2. 並べ替え不可の選択肢を抽出するフィルタが粗すぎ、Part 5・6 の語の4択まで混ぜて
   **偏りを打ち消してしまった**（χ²=4.93 → 正しく絞ると 16.93、p<0.001）。
3. 受動態プローブの正規表現が**副詞を挟んだ受動態**（`are now printed`）を落とし、
   **偽陽性を出した**。実装役が計器自体を疑って発見した。

**3回とも「測ってから判断する」で救われている。**思いついた段階で一般化していたら全部誤っていた。

## 2026-08-25 の変更点

### この日いちばん重要なこと

**「1セット1問」への集約が、隠れていた欠陥を一斉に露出させた。**

本番の TOEIC は図表付きセットでも `Look at the graphic.` の設問が**1セット1問**なのに、このアプリは
**2問**載せていた。2問目は必然的に1問目と同じ行を指したがるため、**「前問の正解を先行詞で受ける鎖」か
「識別条件を書き込んだ長い設問文」のどちらかが必ず発生する。**利用者の判断で構造ごと本番仕様に寄せた。

**そして、集約したことで「残した側」の欠陥が見えた。**2問目に隠れていて誰も点検していなかったため、
**残した30問のうち12問（4割）が壊れていた。**Vol.6 の `graphic` 事故（描画されていないものは中身も
検証されていない）と同じ構図。

### 「直すと別のところが壊れる」が繰り返し起きた

| 直したこと | 生まれた欠陥 |
|---|---|
| 鎖を切るため設問文に識別条件を書いた | 設問文が本番の2倍（17〜23語 vs 中央値11語）。3件で音声が不要に |
| 極端値を避けるため表を組み替えた | **音声と矛盾**（「唯一の選択肢だ」が偽に）、料金が非現実的に |
| 語彙の連想を消すため stem を中立化した | **第二の正解**が生まれた（男性は2つの部屋に行く） |
| 「1セット1問」に集約した | **残った図表設問の正解位置が Vol.5 で9問すべて A** に |
| 定員の極端値を避けて絞り込みの軸を変えた | 同じ巻の別セットと**装置が完全に一致**（正解の行位置・選択肢位置まで） |
| 正解位置のドリフトを消した | **全区画が閾値の直下に並ぶ**という別の指紋に |

**CLAUDE.md の「差し替えた問題は6回中5回、新しい欠陥を持っていた」が、1日でほぼそのままの比率で再現した。**
監査を入れていなければ全部残っていた。

### 1. 図表問題を本番仕様に（18セット）
全6巻の18セットで2問目を通常設問に差し替え。Vol.1・3・6 は図表設問がちょうど5問（Part 3 に3問・
Part 4 に2問）で本番と完全一致。**Vol.5 は図表セットが9件と本番の倍近くあったため、利用者の判断で
9 → 5 に減らした**（品質の低い No.50・59・65・92 を通常設問に）。

### 2. 残した図表設問30問のうち12問を是正
壊れ方は5種類。**いずれも「片方の情報源だけでは演繹できない」は通っていた。**
- **音声の最上級が表の極端値を名指し**（`shuts earliest` ＝ 最小値、`the longest` ＝ 最大値）
- **正解の行が表の極端値**（座席数も料金も最大、選択肢中の最高額、定員の唯一の最大値）
- **音声が行名をそのまま発話**（`our medium unit`）／**行名に序数性**（Locker < Small < Medium < Large）
- **表の列に答えが書いてある**（`Staff — annual permit`）／**選択肢の括弧書きが音声と一致**（`(restaurants)`）
- **選択肢の形で正解が浮く**（`Oak Room / Lecture Theatre / Seminar Room B / Gallery`）

**`vol6` No.62 は 2026-08-18 に「最大値は誤答の行に置く」で直したはずが、まだ壊れていた。**
塞いだのは一段目の素朴な経路だけで、**「クレーン可で絞ってから一番大きい行」という二段目が開いていた。**
一度「設計に内在する性質だから欠陥ではない」という報告を受け入れてしまったが、正しくは
**「その設計を選んだこと自体が誤り」**だった。**メインが実装役の論拠を検証せずに通した判断ミス。**

### 3. 正解位置の「読める規則性」を2つ是正（通算4回目・5回目）
**4回目は `balance2.mjs` 自身が作っていた。**`balancePart()` が `entries.find()` で常にファイル内の
最も早い設問を動かすため、書き手が多用した文字が前半から剥がされ**末尾にだけ残る**。
実測：35区画中30区画で同じ向き、最大で前半平均 2.37 → 後半平均 0.47（4σ超）。
**Vol.6 だけきれいだったのは、この巻だけ正解位置を人手で作り直したから。**

**5回目はメインの指示が作った。**「過剰に0へ寄せないこと」とだけ書いたため、実装役が「閾値の直下で
止める」と解釈し、**36区画中21区画が |z| = 1.74〜1.96 に固まった**（同じ値が7区画・5区画…と繰り返し）。
是正後は |z|<1.0 が26区画・1.0〜1.6 が9区画・1.6以上が1区画。

`validate.mjs` に**検査F**（前半・後半のドリフトを z スコアで測る）を追加し、`balance2.mjs` に
**`--by drift`**（件数を動かさず対で交換する）を追加した。

### 4. 中断セッションの不具合（実害あり）
- **`mocks.js` の「フル受験」ボタンが、すぐ上の「中断中」バナーの中身を無言で破棄していた。**
  120分模試の経過時間ごと消える
- **`launchOrResume` が `clearSession()` の直後に旧 Run の `persist()` で書き戻していた**（捨てたはずの
  セッションが復活する）
- **ドリルの4つの起動ボタンが同じ `sessionKey`** を共有し、押したボタンが無視されていた
- **ホームに中断中の演習が1件しか出ず**、2件目以降が到達不能だった
- confirm のキャンセルが即破棄だったのを「何もしない」に変更（取り消しを意味するボタンの裏に破棄を置かない）

`tools/smoke.mjs` を27→33項目に。**追加分すべてで「わざと壊すと FAIL する」ことを確認済み。**

### 5. 検査で捕まらない穴を2つ記録
- **`node --check` は `/* … */` の中の `*/` を素通りする。**コメントに `rev-wrong-*/rev-blank-*` と
  書いたところ、そこでコメントが閉じて**アプリ全体が起動不能**になった。捕まえるには `import()` して
  `SyntaxError` を見るしかない
- **図表ユニットの非図表設問が `topics: ['graphic']` を黙って継承していた**（`t:` の明示が無い場合）。
  分析画面の論点集計が実数の数倍にずれる。全巻で多数見つかった

## 2026-08-18 の変更点（未決事項 3 — 予想模試 Vol.6）

Vol.6（200問）を作成して `registry.js` に登録した。**ただし作業の実態は「新しい模試を1回ぶん作る」ではなく、
「模試の作り方そのものを作り直す」だった。**Vol.6 の200問のうち**約140問を作り直し**、
その過程で**既存データの欠陥を10種類以上見つけて是正した**。

### この日いちばん重要なこと

> **指示は、達成のされ方まで決めてしまう。**

同じ形の失敗を3回した。3つとも担当は指示どおりに動いており、**指示が求めていないものを禁じていなかった**だけ。

| メインが出した指示 | 担当がやったこと |
|---|---|
| 「Vol.1 の鏡写しにせよ」（構造の意味で言った） | 構造だけでなく**内容まで**写した |
| 「A/B/C/D を均等にせよ」 | **輪番**（`ABCDABCD…`）で均等にした |
| 「`vol1-r4.js` を開くな」 | Vol.1 を避けて**既存ドリル**に寄った（同型が計7回） |

### 発生した欠陥と是正

**(1) 内容の鏡写し（メインの指示が原因。約100問）**
`vol6-r4` 5/5セット・`vol6-r2` 6/7セット・`vol6-r3` 8/11問・`vol6-l2a` 7/7セット・
`vol6-l3` 6/10セット・`vol6-r1` の Part 6 全4文書が Vol.1 の再スキンだった。
`why` が完全に同一文字列、`Thank you for looking into this so thoroughly, and for the suggestion.` が100%一致、
`insertAt: 2` まで一致、という水準。**全部作り直した。**

**(2) 既存5巻が元から互いの再スキンだった（今回発覚）**
`〈姓〉&〈姓〉〈専門職〉` の意見書と `Prepared for the 〈X〉 Committee` の報告書が**5巻すべて**、
`Subject: Upgrade query` と `The Provincial — 〈分野〉` が4巻（vol4 と vol5 は**日付まで同一**）。
`vol2-l2b` と `vol5-l2b` の 65–67 も互いにほぼ逐語。**Vol.6 が Vol.1 を真似たのではなく、
6巻目として同じ型を踏襲しただけだった。**→ 未決事項に積んだ。

**(3) Vol.1〜3 の図表51問が実行時に表を失っていた（うち26問は解答不能）**
ヘルパーが `graphic: o.g` で受けるのに呼び出し側は `graphic: {...}` で渡していた。
`Vol.4` `Vol.5` だけが直っており、1〜3 に遡って直されず記録も残っていなかった。
**9ファイルを修正し、`validate.mjs` に検査を3つ追加した。**
表が見えるようになって初めて中身の欠陥が2件露出（`vol2-l3` の No.89–91 は曜日が特定できず解答不能、
No.98–100 は `Thursday week` が納期計算と矛盾）。

**(4) 正解位置が完全な循環になっていた（Vol.6 で3回）**
`vol6-r1` の Part 5 が `ABCDABCDABCD…`、`vol6-l1` の Part 2 が71%、Part 1 が `ABCD…`。
**件数は A8/B8/C7/D7 で均等**なので、それまでの検査（件数のみ）を素通りしていた。
**本文を読まずに30問全部が当たる状態だった。**
→ `validate.mjs` に**循環（60%以上でERROR）と連続（6問以上でWARN）**の検査を追加。
既存の `vol4`・`vol5` にも連続の偏りが5件あり是正（`vol5` Part 2 は**9連続**）。

**(5) 線画のコメントが実装と食い違っていた**
`crosswalk` に「3人が横断歩道を渡っている」と書いてあったが実装は全員 `stand`。
それを信じた `vol3-l1` No.4 が**絵と食い違う正解文**を持っていた。
→ 全30場面を実描画して突き合わせ、**13場面のコメントを是正**（描画コードは1文字も変えず、
SVG 文字列が HEAD と完全一致することを機械確認）。
絵に根拠が無い正解文3件（`vol3-l1` No.2 / `vol1-l1` No.6 / `drills` の `p1v-03`）も是正した。

### 今日発見した「新しい欠陥の型」（CLAUDE.md に記録）

いずれも朝の時点では誰も検査していなかった観点。**基準が上がった結果、前半に「合格」とした設問が
後半に欠陥と判定されたものが複数ある。**

- **選択肢の「形」が正解を教える。**Part 4 の30問で**13問（43%）が正解だけ単独最長**だった。
  書き手は正解を丁寧に、誤答を短く書くため。**この型は機械照合では捕まらない**（語彙一致 Jaccard 0.27）
- **図表・クロス問題は「片方だけで解けないか」を実際に試すまで分からない。**1日で3件出た。
  さらに**当てずっぽうの当たりやすさ**は別の検査で、「耐荷重が最大かつクレーン可」の行が正解だと
  表を見るだけで当たった
- **後の設問が前の設問の答えを漏らす。**TOEIC は設問を先読みできるので実際に得点になる。4件出た
- **設問を鎖にしない。**`What is the load limit for **that area**?` は前問の正解が先行詞で、1問落とすと2問落ちる

### 測っているものが目的と一致していない、という共通の失敗

4件とも「数字が返ってきたので確認できたと思った」型。**ゼロが返るのがいちばん危ない。**

| 使った道具 | 出した数字 | 実際に測っていたもの |
|---|---|---|
| 正解位置の件数 | `A8/B8/C7/D7` | 均等さ。**並びの規則性ではない** |
| n-gram 照合 | 一致0件 | 語彙の重なり。**装置・命題の重なりではない** |
| 図表問題の演繹検査 | 片方だけでは解けない | 演繹可能性。**当てずっぽうの当たりやすさではない** |
| Google Books Ngrams | 0件 | **その帯では測定不能**。存在しないことではない |

Ngrams は `despite the series ending`（Wikipedia に46件の実例）でも空配列を返す。
**0件を根拠に「閉じている」と書いた解説が、根拠が無効なまま残りかけた。**

### 工程の穴（個別対処では止まらなかった）

**「既存の巻を避けた先で既存ドリルと衝突する」が7回**起きた。個別に7回直しても再発。
原因は**作問時にドリルとの機械照合をさせていなかったこと**。
7回目の担当が自分でそう報告してきて初めて工程の問題だと分かった。
→ Vol.6 全200問 × `assets/data` 全体の一括照合を実施。
**4方式（正解語・選択肢集合・stem の6-gram・本文の5-gram）で5件、読んで発見した衝突が別に4件。**
うち2件は語彙の一致がゼロで、共有していたのは**装置と正解の命題**だけだった。
**機械照合は下限であって、上限ではない。**

### ファイルの先頭に残っていた「原因」

`vol6-l2b.js` と `vol6-l3.js` の冒頭に
「標準回その2。**Vol.1 と同じ条件（難度配分・型の配分・文書種別）を鏡写しにする。**」
というコメントが残っていた。**今日の事故の原因そのものが、次に触る担当への指示として残っていた。**
→ 両ファイルの冒頭を、守るべき構造の**数値**と、この一文が何を引き起こしたかの記録に書き換えた。
**CLAUDE.md は全体の規範だが、担当が最初に読むのは触るファイルの先頭である。**

### level について

Vol.6 の level は Vol.1 から写されており、lv5 に大きく偏っていた。
CLAUDE.md の目盛りに当て直した結果、**Part 5 は lv3=14 / lv4=12 / lv5=4**。
`recommend that + 原形`・`very` は比較級を修飾できない・分詞構文の意味上の主語——
いずれも文法書の標準項目で、**表示が間違っていただけで問題自体は妥当**。

> **ドリルは 800→900 の穴を埋めるので難所に寄せるが、模試は本番の再現なので基本項目が入っていて当然。**
> 全部が lv5 の模試は本番に似ていない。

「模試なんだから難しくあるべき」という無自覚な前提が、Vol.1 の level を歪めていた可能性がある。
**vol1〜5 の模試 level は新定義で未検算のまま**（未決事項に既出）。

## 2026-08-17 の変更点

未決事項 **1'（型の偏り）・2（level）・5（線画）・8（模試 Part 1 の訳）**を消化し、
そこから派生して**既存データの一斉点検**と**結果画面の改修**を行った。総問題数は 1,638問のまま。

### この日いちばん重要なこと

**「誤答が、別の文脈でなら正しい英語」という型の欠陥が、既存データに構造的に埋まっていた。**
約250問を点検して**約57問（23%）が該当**。

原因は「辞書の一部の語義しか見ていない」だけではなかった。**もっと根の深い原因が3つある。**

1. **閉じられない型を選んでいた。**`stop doing` や `economic`/`economical` のように、
   **何巡かけても閉じられない組み合わせ**が実在する。`verbal-04`・`confuse-05`・`adv-05` は
   各4巡かかり、いずれも**型ごと差し替えて**決着した。**作問前に型を見切れば防げる。**
2. **閉じ方を間違えていた。**「意味の推論で切る」（読み手が文外に状況を補えば開く）を
   構造で切ったつもりでいた。**差し替えの6回中5回が、この間違いで新しい欠陥を作った。**
3. **論点の定義そのものが欠陥を生んでいた。**`topics.js` の `adv` は
   「意味だけで決まる」「文全体を読まないと解けない」と書いており、
   **アプリ自身が「閉じられない型」を推奨していた。**

**得た知見はすべて CLAUDE.md の「誤答の作り方」「問題を差し替えるときの決まり」に移した。**
次に作問・修正する人は**まずそこを読むこと。**ここには経緯だけを残す。

### 1. 未決事項 1' — `subj` と `inv` の残存クラスタを是正

`subj-03/06/29/31`（文頭の空所に助動詞1語）と `inv-04/05/06/10/11`（倒置後の単複一致だけ）を解消。
`subj-06`/`31` → `subj-40`/`41`、`inv-06`/`10`/`11`/`14` → `inv-20`〜`24`。**id は全部新規採番**。
`topics.js` の `subj`・`inv` の記述も、16問・20問の実態に合わせて書き直した。

- **`inv-22` に致命的欠陥**（第二の正解）。実装役の「`So thorough as it was` は非文」という判断**自体は文献と一致**
  していたが、**文頭の `So` が接続副詞「だから」と読める**ため別経路で成立していた
- **`inv-24` の誤答 `neither has ...` は実際には正しい英語**（`"I never went there." — "Neither have I."`）。
  ELT 教材の「助動詞は先行節の時制に合わせる」は**初級者向けの単純化**だった
- **削除の巻き添えで、触っていない箇所が壊れた。**`subj-39` の解説が削除済みの `subj-31` を
  「別問題で扱う」と参照していた。`topics.js` の `subj` は**前回の変更時点で既に壊れていた**
- **`inv-06` の削除で `so`/`neither`/`as` + 倒置がアプリ全体から消えていた**（全ドリル・全模試を grep）。
  `inv-24` で復活させた

### 2. 未決事項 5 — `meeting-room` / `kitchen` の線画を描き直した

どちらも未使用だったが、これで**作問可能な状態**になった（何が問えて何が問えないかは監査報告に整理済み）。

**この作業で、これまでの教訓が一段更新された。**
実装役は `scenecheck` の12項目を全部通し、**544px の light/dark を自分でレンダリングして
「全項目 読み取れる」と報告した**。レビュー役が独立に描画したところ**新たに致命的3件**
（新造したコンロが券売機に見えていた、鍋がバーナーに乗っていなかった、鍋が調理人の胴と同形だった）。
→ **目視も自己検算と同じで当てにならない。**CLAUDE.md に明記した。

### 3. 未決事項 8 — 模試 Part 1 の訳を4選択肢すべてに（30問・120文）

`quiz.js` は対応済みだったのでデータのみ。致命的0・要修正5・軽微5。

- **誤答の訳が、絵と一致して見えていた。**`A worker is climbing onto a roof.` の「屋根に上っている」は
  日本語では**状態としても読める**が、`rooftop` の絵には実際に屋根の上に作業員がいる。`why` と正面から矛盾していた
- **能動の状態動詞を受動形で訳していた**（`is standing`→「置かれている」）。
  この教材の規約では「〜されている」は完了受動の訳形なので、**誤った規則を教える**状態だった
- **`balance2.mjs` が配列形式の `ja` を見ていなかった**（オブザーバーが発見）。
  配列 `ja` は「除外もされず書き換えもされない」状態で、**入れ替えが起きると無言でデータが壊れる**穴だった。
  同時に並べ替えて記号を振り直すよう修正し、わざと壊したコピーでラウンドトリップを確認した

### 4. 未決事項 2 — `level` を付け直した（スキーマ変更なし）

**1ユニット＝1設問のもの（`single`/`p1`/`p2`）435ユニットのうち、初版で書かれた175ユニット**を付け直した。
`assets/data/` 全体から **level 1・2 は消えた**（模試に1件だけ残っていた違反も是正）。

**第1バッチは一致率62%で31件が誤りだった。原因は2つ。**
1. **軸が違った。**実装役は「誤答が文法上成立するか」で測っていた。正しい軸は「要求される知識がどの帯か」
2. **変更しなかった15件を再判定していなかった。**外れた lv5 の7問が**全部そこに集中**していた。
   → **「残す判断も判断」。**この2点を明記して第2バッチに渡したところ**一致率92.6%**になった

**私が渡した基準そのものに自己矛盾があった。**lv3 は「文法書の標準項目」、lv5 は「要求動詞の that 節内原形」を
名指ししていたが、mandative subjunctive は両方に当てはまる。決め手は「**挿入句を挟んだより難しい版が lv4 なのに、
素の形が lv5 では階段が逆立ちする**」。判定基準は CLAUDE.md に明文化した。

### 5. 「誤答が別の文脈でなら正しい英語」型の一斉点検（**この日の主作業**）

上の4件の作業中に同型の欠陥が5件出たため、利用者の判断で着手した。

**手順**：解説が「自然だ」「文脈に合わない」で誤答を切っている**兆候**を機械抽出（62問）→ 点検 →
**抽出の取りこぼしを標本32問で測定**（致命的9.4%）→ **語彙・語法系189問を全数点検**。

| 段階 | 点検数 | 致命的（第二の正解あり） |
|---|---|---|
| 機械抽出 | 62 | **16（26%）** |
| 標本（取りこぼしの測定） | 32 | 3（9.4%） |
| 第1波 | 35 | **10（29%）** |
| 第2波 | 119 | **28（24%）** |
| **合計** | **約250** | **約57（23%）** |

**語彙・語法系189問は全数点検が完了した。**

**測定でいちばん重要だったこと**：機械抽出は**この型の欠陥のごく一部しか捕まえない**。
「〜になり**にくい**」（ヘッジ）、「口語では**見るが**」（譲歩）、「**文意が変わる**」
（＝英語としては正しいと認めている）、**語義を訳しただけで排除理由が無い**——これらは
キーワードでは捕まらない。とくに最後の型は、**解説が何も主張していないので、解説の書き方からは
原理的に検出できない。**

**差し替えは6回中5回、新しい欠陥を持っていた。**`verbal-04` は4巡、`confuse-05` は4巡、
`adv-05` は4巡かかった。**再発の原因は毎回4つのどれか**で、CLAUDE.md に列挙した。

**この点検で確定した「閉じられない型」**（何巡かけても閉じないので、作問前に見切ること）:
`stop to do`/`stop doing`／emphatic do／**辞書が相互に立項している同義語の対**
（`economic`/`economical`、`sensible`/`sensitive`）／**肯定の副詞 vs 否定の副詞を単文の意味で選ばせる型**。

**新規採番された設問は80問を超えた**（差し替え元が同数消滅）。総問題数は変わっていない。

**点検の枠組み自体が、点検の途中で広がった。**当初は「解説が自然さで切っている」という1つの兆候から
始めたが、個別の設問を見ている過程で次の軸が**後から**見つかった:
- **「肯定の副詞 vs 否定の副詞」は閉じられない型**（一般則が確立して初めて、
  すでに「合格」と判定済みだった模試2問が同型だと分かった）
- **模試の設問がドリルの設問の流用**（`v1q112` を点検した役が気づいて発覚。実測で9組）
- **論点の定義そのものが欠陥を生んでいた**（`topics.js` の `adv`）

**次に点検するなら順序を逆にできる。**今回は個別を250問見ながら型が育ったので、
**序盤に見た問題ほど当時の未熟な基準で判定されている**（実際、序盤に「合格」とした2問を後から覆した）。
CLAUDE.md に型の一覧を残したのは、**先に型を当ててから個別を見る**ためである。

### 6. 結果画面から間違えた問題を読み返せるようにした

オブザーバーが「**使えない**」と判定した件。誤答一覧が
「P5　倒置・強調・省略　あなたの解答 (A) ／ 正解 (B)」だけで、**設問文も訳もなくクリックもできず**、
読み返す経路が「誤答だけもう一度」＝**再受験しかなかった**。
**このセッションで書いた模試 Part 1 の訳120文にも、そこからは辿り着けなかった。**

`quiz.js` の描画関数を純関数として切り出して `result.js` から再利用（**新しい表示形式は作っていない**ので、
演習と復習で解説の見た目が食い違わない）。あわせて Part 3/4 の全体訳・語注と音声、
Part 6/7 の本文折りたたみ（**Part 7 全行展開で 145,656px → 6,787px**）、模試の設問番号表示、
携帯の固定バーの不透明化、演習中のタブバー非表示。

**改修そのものにも穴が4件あった**（オブザーバーが実機で発見）。「語注」の見出しが2つ連続で出る、
再生中に行を閉じると停止手段が消える、など。**作ったものは必ずもう一度見せること。**

### 7. 模試とドリルの設問の複製を解消した（9組）

**模試 Part 5 の設問が、ドリルの設問の stem をほぼそのまま流用していた。**
最悪は `v1q123` × `inv-05` で、`Not until the **second/third** round of testing ------- the intermittent
fault reproduced.` と**序数以外は一字も違わなかった**（解説文まで同一）。
**利用者はドリルで解いた文に模試で再会するので、模試の点が実力より高く出る。**模試側を全部作り直した。

**原因は書き手の不注意ではなく、論点あたりの「型の在庫」が尽きていたこと**（未決事項4に記載）。

### 8. 分析画面の「迷いの指標」に最小サンプル数のガードを入れた

**誤答0問でも「誤答が速すぎます。読み飛ばしや早合点の可能性。」と表示**され、
正解0問・誤答1問でも「誤答時に時間をかけ過ぎています」と**1問のデータで断言**していた。
同じ画面の他の指標は全部「データ不足」と守っているのに、ここだけガードが無かった。

## 2026-08-16 の変更点（未決事項 1 — `subj` と `inv` の型の偏りを是正）

### 何をしたか
**`subj`**: 「要求・提案の that 節に動詞の原形」型が **14問（70%）→ 7問（35%）**。
空いた7問を仮定法の別領域に差し替えた（仮定法過去・混合仮定法・`I wish`・`It is high time`・
`would rather`・`otherwise`・`but for`）。**id は 33〜39 で新規採番し、旧 id を再利用していない**
（中身を差し替えて id を使い回すと、別問題なのに「以前正解した」履歴が SRS に引き継がれて復習間隔が壊れる）。

**`inv`**: 論点名は「倒置・強調・省略」なのに内訳が**倒置14・強調1・省略1**だった。
助動詞・be 動詞の形だけを問う5問から3問を削り、強調2問・省略1問を追加して**倒置11・強調3・省略2**に。

正解位置は `subj` A=5 B=5 C=5 D=5、`inv` A=4 B=4 C=4 D=4 で均等。問題数は両方とも変えていない。

### 3巡かかった。ここで得たものが本体（次に型を触るとき必ず読むこと）

**差し替えた10問に対し、監査で致命的2件・要修正13件・軽微17件。**最終的に3巡した。
単なる手戻りではなく、**設計上の発見**だった。

- **「その論点を4択で一意に問えるか」は型ごとに違う。** `inv` の emphatic do は2巡とも失敗した。
  空所の直後に原形を置けば「原形の前は助動詞」で解けて**強調の知識が要らなくなり**、
  誤答を助動詞（`may` `could` `would`）に揃えると**意味の程度問題になって複数正解になる**。
  **emphatic do は統語的に強制されない唯一種類の「強調」**なので、Part 5 の4択と原理的に相性が悪い。
  命令文の `Do let us know ...`（誤答が全部非文）に振り替えて決着した。
  **型を指定する側（メイン）の判断ミスであって、作問した側の問題ではなかった。**
- **単調さを直すつもりで単調さを作る。** 差し替えた `inv-17` が既存 `inv-02` と
  **選択肢の語彙・正解語・判断過程まで一致**していた。しかも削除対象だった「助動詞の形だけ」型そのものだった。
  **差し替え先を決めたら、既存問題と判断過程が重ならないかを必ず照合すること。**
- **論点の帰属がずれる。** `whatsoever` の問題は実質「語彙選択」で、`topics.js` の `adv` が
  「**全選択肢が副詞なら**文全体を読まないと解けない問題」と書いている条件にそのまま当てはまっていた
  （4語とも副詞）。**アプリが自分で「これは別論点だ」と説明している**状態だった。
  同様に、接続詞 + 過去分詞の問題は `Although` が正解に一切関与せず、態の判断だけで解けて `ptcp` と同一だった。
  **「その語を消しても同じ答えになるか」を試すと、論点が効いているかが分かる。**
- **解説に「〜できない」と書くときは反例を探すこと。** 3巡すべてで
  「`why` が存在しない規則を述べる」欠陥が出た:
  「that 節の主語が同じなら原形」（`I would rather leave` は節ではなく原形不定詞）／
  「`would have` は if 節が過去完了のときのみ」（**混合仮定法は標準的に認められた形**）／
  「`because of` は `would have` と共起しない」（`Because of the storm, it would have arrived late anyway.` は正しい）／
  「分裂文の `it` は形式主語」（形式主語構文は that 節が真主語、分裂文には真主語がない）／
  「`Were it not` は名詞を続けられない」（`Were it not a holiday, the office would be open.` は正しい）。
  **利用者は高校英語教師なので、この種の断定はそのまま見抜かれる。**
- **誤答が「条件付きで正しい」ものは置かない。** `would rather` + 異主語節の**原形は米語で実在する**
  （COCA に実例）。学習文法書は過去形しか挙げないが、唯一解が保証されない以上、誤答には使えない。
  `to submit`（`would rather` は to 不定詞を取らない）に差し替えて決着した。

## 2026-08-15 の変更点（未決事項 2・6・7・8 の消化）

消化した未決事項: 2（広告型）・6（未使用場面）・8（短縮形）・7（Part 1 の訳）。
このほか、作業中に見つかった不具合と検査の穴を2件直した。

作業体制を **CLAUDE.md の冒頭に明記した**（利用者の指示。以後も継続）。
メインの Claude は進捗・品質管理のみを担当し、実装は Sonnet 5 の実装役、監査は Opus 5 のレビュー役、
利用者目線の通し確認は Opus 5 のオブザーバーが行う。今回は**この体制を守らずメインが自分で4問書いてしまい、
指摘を受けてから監査に回した**。結果、下記のとおり自作分から要修正が続けて出た。**規模の小ささは理由にならない。**

### 1. Part 4 に広告型のセットを追加（未決事項 2 — 完了）
`listening2.js` に `u-p4t-04`（3問）。館内放送3・留守電1・ツアーガイド1・会議抜粋1 に広告が加わり、
`p4type` の型の網羅が揃った。`topics.js` の副題「留守電・館内放送・広告・ツアー」と中身がやっと一致した。
**`p4type` の正解位置がちょうど A=4 B=4 C=4 D=4 に揃った。**

### 2. 未使用場面 `warehouse-b` を使う Part 1 を追加（未決事項 6 — 部分的に完了）
`listening.js` に `p1v-09`。**有人版 `p1v-01` の直後に配置**し、1問目・2問目で見比べられるようにした。
`warehouse` と `warehouse-b` は `warehouseFixtures()` で什器座標を共有しているため、
**2枚の差は作業員1人だけ**。`is being + 過去分詞` が動作主を要することを、同じ絵の有無で示せる。

**ドリルの Part 1 に線画方式を併用する方針を確定した**（利用者の判断）。
線画でしか問えない論点があるため。CLAUDE.md に明記済み。

### 3. 携帯幅で「次へ」が押せない不具合を修正（新規発見）
オブザーバーが実機幅の通し操作で発見。**880px 以下で `.tabbar`（fixed, z-index 70）が
`.examnav`（sticky, z-index 6）を覆い、「次へ」の露出が上端 14px だけになっていた。**
ボタン中心をタップすると**演習を離脱して `#/analytics` へ飛ぶ**。390×780 / 414×896 / 600×900 /
719×900 / 760×900 で再現。`app.css` の `@media(max-width:880px)` 内に
`.examnav{bottom:calc(env(safe-area-inset-bottom) + 3.6rem)}` を追加して解消
（3.6rem = タブバー実測 48.34px ≒ 3.02rem ＋ 従来と同程度の余白 約9px）。
881px 以上は従来どおり。全幅で `elementFromPoint` がボタン自身を返すこと、実クリックで
`#/exam` に留まり次の設問へ進むことを確認済み。

### 4. CLAUDE.md の記述の誤りを2件是正
- 「個別論点ドリルの Part 1 は `desc` 方式」→ **実際は `listening.js` に scene 方式が8問残っていた**
  （`part1.js` の冒頭コメントにも書かれていたが、CLAUDE.md と PROGRESS.md には反映されていなかった）
- 「SVG 場面は模試専用」→ **実際はドリルも使っていた**

### 5. 監査で出た欠陥（メインが自分で書いた4問に対して）
レビュー役2巡・オブザーバー1巡で、**致命的0件・要修正4件・軽微14件**。要修正の中身:
- **広告の本文が自己矛盾していた。**「月額に設計相談が含まれる／別建て請求は季節の装飾だけ」と言った直後に
  「設計料を免除する」。含まれている料金は免除できない
- **`p1v-09` が絵を見ずに解けた。** 誤答3つを全部「人が写っていないと成立しない文」で揃えてしまい、
  無人と分かった時点で消去法だけで解けた。**線画方式を使う意味を自分で消していた。**
  誤答1つを「無人でも成立するが絵と合わない状態描写」（最下段が空なので `Every shelf has been filled
  with cartons.` は偽）に差し替えて、動作主の有無＋絵の照合の二段構えにした
- **解説の対比説明が事実に反していた。** 「作業員が1人入ると正解の型が完了受動に入れ替わる」と書いたが、
  有人版 `p1v-01` の正解も `Cartons have been stacked on the shelving.` で**同じ完了受動**。
  方向も逆で、そもそも入れ替わらない。「人の有無で変わるのは進行受動を選べる可能性が生じるかどうかであって、
  正解の型が自動的に入れ替わるわけではない」に書き直した。**これはメインが実装役に出した指示自体が誤っていた**
- 解説に内部 ID（`p1v-01`）と「1問目」という順序表現がそのまま出ていた。
  利用者には意味がなく、順序は SRS の復習で変わりうる。内容で指す表現に変えた
- 広告のトークが180語（本番は100〜150語、このドリル内で最長）→ 153語に短縮

### 6. 音声スクリプトの短縮形を揃えた（未決事項 8 — 完了）
**短縮形の密度が、書かれた時期によって30倍以上ばらついていた。** 実測（千語あたり）:
`listening2.js` **0** / `vol3` 1.1 / `vol2` 1.4 / `vol1` 5.0 に対し、
後から書かれた `vol4` 30.0 / `listening4` 38.5 / `vol5` 38.7。
先の4ファイルだけが不自然に硬く、同じ「予想模試」として並んでいるのに英語の自然さに差があった。

4ファイルを揃えた（**13ファイル・260行の書き換え。行の増減なし**）。結果は
`listening2.js` 35.3 / `vol3` 31.6 / `vol2` 29.1 / `vol1` 30.0 で、模試5回が 29.1〜38.7 に収束。

**一律の機械置換はしていない。** 会話・留守電・広告を主戦場にし、公式アナウンス・ニュース放送は
抑制。強調の否定（`I will not charge you`）、`not X but Y`、法的な言い回し（`has not yet been
granted`）、節末の `cannot`、修辞的な対比は全形のまま残した。
**意図問題の stem に逐語引用されている発話は据え置いた**（本文を変えると stem と食い違うため）。

#### ここで判明したこと（次に本文を触るとき必ず読むこと）
- **`'s` は後続語で危険度が変わる。** 監査で出た要修正2件はどちらも同一原因で、
  **`'s` の直後が名詞だと所有格として完全に成立してしまう**（`the recording's audio` ＝録音の音声、
  `the chef's one` ＝シェフの唯一の…）。とくに **Part 2 は一度しか聞けず前後の文脈もない**ので、
  正解選択肢でこれをやると解けなくなる。後続が形容詞・前置詞・`been` なら所有格読みは構造的に塞がれる
- **主語が長い名詞句だと `'s` が主語の頭から離れて読みにくい**（`The rota on the board's out of date`）。
  2件差し戻した
- **公式アナウンスは全形が通例。** 駅・空港放送は `It will now board` と言う。register を崩すと不自然
- **合成音声で再現が読めない形がある。** `should've` のような「助動詞 + 've」は最も強く弱化する。
  `assets/data/` 全体でこの形は1件だけだったので全形に戻した。
  残る `that'd` / `it'd` の2箇所（`vol2-l2a.js:64` / `vol3-l2a.js:139`）は、
  **どちらも設問の正解の根拠になっていない**ことを確認したうえで残してある
- **`audio.js` は短縮形に対して安全。** `chunk()` は空白の正規化と `.!?` での分割だけで、
  アポストロフィには触れず `SpeechSynthesisUtterance` にそのまま渡る。
  ソース中のアポストロフィは全件 ASCII `'`（U+0027）で、曲線アポストロフィの混入は0件
- **実際の音の確認は誰もできていない。** 合成音声がどう発音するかは聞いて確かめるしかない

### 7. `validate.mjs` に引用照合を追加した（6 の作業中に立てた申し送り — 完了）
Part 3/4 の意図問題は `What does the man mean when he says, "..."?` の形で発話を引用する。
**この引用が script と一致しないと設問が成立しない**が、検査が無かった。
短縮形の作業で本文を一括で書き換えたとき、手書きのスクリプトで照合して既存の不一致を1件見つけたので、
ツールに入れた。引用符の種類（`"` `“` `”` とネストした `'`）と空白を正規化し、
`...` を含む省略引用は分割して**順序込みで**照合する。

**この作業で検査の穴が1つ塞がった。** `listening4.js` の8件だけ引用を `'...'` で囲む独自スタイルで、
**48件中8件が検査対象から漏れていた**。データ側を標準スタイル（外側シングル・引用ダブル）に統一して
48件全部を検査対象にした。`scenecheck.mjs` が4場面を検査できていないのに気づかず事故を出した件と同じ形。
**「エラー0件」は「全部検査できた」を意味しない。**

### 8. Part 1 の訳を4選択肢すべてに付けた（未決事項 7 — 完了）
`ja` に**正解1文の訳しかなかった**。`why` は「なぜ誤りか」を述べるだけで訳ではないため、
学習者は誤答の英文の意味を確認できなかった。**Part 1 ドリル49問の `ja` を4要素の配列にし、
約147文を新規に書き下ろした**（`listening.js` 9問・`part1.js` 40問。模試の30問は対象外）。
`quiz.js` は配列なら要素ごとに `<p>` を分けて描画する（文字列のときは従来どおり。Part 2/3/4 は無変更）。
`validate.mjs` に配列用の検査（要素数の一致・記号の順序）も追加した。

**訳し分けが本体**。進行受動 `is being + 過去分詞` ＝「〜されているところだ」／
完了受動 `has been + 過去分詞` ＝「〜されている」／`is wearing` ＝状態／`is putting on` ＝動作。
**同じ訳にしたらその設問は教材として死ぬ。**

#### ここで判明したこと（次に訳を書くとき必ず読むこと）
- **「同じ訳にするな」だけでは足りない。** `p1d-19` は `Both cyclists are putting on helmets.` と
  `One cyclist is wearing a helmet.` が同一設問内で対比される看板問題だが、
  訳が「被っているところだ」と「身に着けている」で**動詞まで違ったため、差が「ところだ」の有無なのか
  動詞の違いなのか判別できなくなっていた**。正しい要件は「**動詞を揃えて『ところだ』だけを差にする**」。
  なお「身に着けている」は日本語で状態・動作のどちらにも読めるので、状態を表す文には向かない
- **最小対の設計は訳で壊れる。** `p1d-40` は `A vendor is inspecting...` と
  `A customer is inspecting...` が主語だけ違う最小対なのに、訳で片方にだけ「手に取って」を足したため
  述部が違って見え、区別が主語ではなく動作にあるように誤解させていた。**補足は `why` の仕事**
- **人を表す語を物に置き換えない。** `cyclist`（自転車利用者）を4文とも「自転車」と訳していた。
  `p1verb` は**人物の有無が正誤を決める**論点なので、これは最も置き換えてはいけない種類の誤訳
- 新規147文に対し、監査で**致命的1件・要修正6件・軽微12件**。書き下ろしの欠陥率は今回も変わらない

## 2026-08-14 の変更点

### 1. 論点別ドリルを 251問 → 634問 に補充（+383問・新規14ファイル）
新ファイル: `grammar3〜6.js` `vocab3〜5.js` `context2〜3.js` `reading3〜4.js` `listening3〜4.js` `part1.js`。
`registry.js` の `DRILL_FILES` に登録済み。**37論点すべてが増加した**（機械照合済み、下記参照）。
とくに薄かったもの: `p7ins` 1→12 / `p7not` 2→12 / `p7intent` 2→15 / `p7syn` 3→14 /
`cohesion` 4→14 / `connect` 4→16 / `ctense` 5→16。

新規383問を6本の監査（Opus）で1問ずつ検算し、**108件の欠陥（うち致命的20件）**を検出・修正した。
致命的の中身はすべて「演習として成立していない」もの:
- 誤答が実際には正しい英語だった（`has arrived` は時・条件の副詞節で使える／`helpful to doing` は
  正解より高頻度／`grant a refund` `practice discretion` `accrue expenses` はいずれも実在／
  `circumspective` は実在の形容詞）
- 狙った論点が別の規則で空振り（仮定法の問題なのに主語が複数で、主述の一致だけで誤答が切れた）
- 図表問題が音声だけで解けた
- 文挿入の正解位置が他の空所でも成立した
- チャットの意図問題で、引用文そのものが時刻計算と矛盾していた

### 2. Part 1 を描写テキスト方式に変更（利用者本人の決定）
線画では `is wearing` / `is putting on` の識別が原理的に作問できなかった（棒人間に服がないため）。
**個別論点ドリルの Part 1 は「英語の場面描写を数秒表示 → 消える → 音声4文」形式に変更**（`desc` フィールド、
40問新規作成。`p1verb` は 8→48問）。**模試5回のPart 1（30問）は SVG のまま**（本番の見た目・時間感覚の
再現が目的のため変更なし）。`quiz.js` の `renderP1()` が `scene` と `desc` を分岐する後方互換の実装。
「もう一度見る」で描写を再表示できる（音声非対応環境で解答不能にならないため）。

### 3. 模試 Part 1 の不整合是正
30問中13問に問題があった。線画と正解・解説が矛盾していたもの（人物の腕が対象に届いていない、
箱がトラックの窓に見える、人がカウンターの上に立っている等）を、`scenes.js` の場面と設問で是正。
`is being + p.p.` の解説が「動作主が写っている」と言いながら写っていなかった設問を、
ルールそのものを教える構成に作り替えた。同じ場面を使う設問の正解文がほぼ同一だった重複も解消。

### 3b. 模試 Part 1 の線画を全面的に描き直した（3 のあとに実施）
Claude Design で設計仕様を起こし、**30場面すべてを新しい部品系で作り直した**。
仕様と見本 SVG は **`design/part1/`** にある。**以後これが正。Claude Design を再度開く必要はない。**

- 部品ライブラリ（`figure` / `fixture` / `prop` / `box3` / `limbArm` / `contact` / `composeScene`）。
  **描画順は `composeScene()` の固定層で強制**する。前後関係が描画順そのものなので、
  ここが崩れると「カウンターの天面に立っている」が再発する
- **腕の長さ制限（肩→肘30 / 肘→手28）を `limbArm()` の中で毎回測り、超過時に警告する。**
  「顕微鏡まで32単位離れていた」種類の欠陥は構造的に起きなくなった
- **`laboratory-seated` を新設。** 同じ scene ID を複数の設問が別々の意図で参照していて
  （vol1「立って顕微鏡を調整」/ vol4「座って作業台に着く」）、1枚の静止画では両立できなかったため。
  **これが場面バリエーションの型**（`warehouse` / `warehouse-b` の有人・無人も同型）
- **38問（模試30＋ドリル8）を1問ずつ絵と突き合わせて再検証し、16件の不整合を修正。**
  正解位置は1問も動かしていない（A12 / B11 / C8 / D7）

**`tools/scenecheck.mjs`** を新設した。仕様の受け入れチェック12項目を機械化したもの。
正規表現ではなく実際に Chromium で描画し `getCTM()` で transform を解決して座標を測る。

#### ここで得た教訓（次に線画を触るとき必ず読むこと）
**幾何検査だけでは足りない。** `box3()` にも人物の頭部にも輪郭線が無く、
`--card-2`（塗り）と `--card`（背景）の差はわずかなので **544px では存在自体が読めていなかった**。
座標はすべて正しく、`scenecheck.mjs` の12項目を通過していた。
**「数値が正しい」と「読み取れる」は別物で、後者は実際に描画して目で見るしかない。**

実際、作成担当が目視したのは38場面中14場面のみで、**未目視の場面に重大欠陥が集中していた**
（`train-platform` の乗客が車体に溶けていた、`factory` の箱が不可視、`library` の本が不可視など）。
**線画を変えたら、全場面を 544px・light / dark でレンダリングして必ず目視すること。**

もう1点、**`frame()` の root `<svg>` が `stroke` を持つ**構造にしてある。
`design/part1/*.svg` の見本が継承前提で書かれているため。
新しい部品を足すとき `stroke` を書き忘れても線が出るのはこのため。root から外さないこと。

### 4. 難度 level の目盛りを定義（CLAUDE.md に明記済み）
既存データは712ユニット中678（**95.2%**、機械照合済み）が level 4-5 に張り付き、目盛りとして
機能していなかった。**level は表示専用**（SRS・出題順・絞り込み・推定スコアには一切関与しない）。
新しい定義は CLAUDE.md 参照（level 3=800〜860帯 / 4=860〜900帯 / 5=900+ 帯、1・2 は作らない）。
新規383問はこの定義に従って作成済み（新規分を含めた全体では level 4-5 が 1,015 ユニット中 899
〈88.6%〉に緩和）。**既存分の付け直しは未実施**（設問単位で正確にやるには `question.level` の新設が要る）。

### 5. 実バグの修正
- **SRS のはしご段が1段ずれていた。** `store.js` の `recordItem()` が初回正解時に `streak` を 0→1 に
  した直後 `LADDER[1]`（3日）を引いており、`LADDER[0]` の「1日」が永久に使われなかった。
  README が謳う「1 → 3 → 7 → 16 → 35 → 75 日」が実際は「3 → 7 → …」だった。修正済み・機械照合済み。
- 音声非対応環境で「この端末は音声非対応」の表示が、直後の `playerBox()` 更新で上書きされていた
  （表示のみ、実害なし）。`quiz.js` の該当箇所に `audio.supported` の条件を追加して修正済み。

### 6. 品質管理ツールを `tools/` に移設・強化
前セッションでは scratchpad にあったため消失した。**`tools/` に置き直し、リポジトリに含めた**
（依存は `tools/package.json`、`node_modules` は gitignore）。
- `validate.mjs` に検査を追加: `why` の隣接参照（`同上` 等）／選択肢の大小文字と空所位置の整合／
  `why[answer]` のみ「正解」始まり／選択肢の重複／空所マーカー個数／Part 1 の `scene`・`desc` 排他。
  `--extra <ファイル…>` で registry 未登録のファイルも検査できる
- `balance2.mjs` に **`--by topic`**（論点単位の平準化）を追加。ドリルはほぼ全部 Part 5 のため、
  パート単位では論点単位の偏りが解消しない。模試は従来どおりパート単位
- `smoke.mjs` は 24→**27項目**（Part 1 描写テキスト方式の3項目を追加）。サーバの配信ルートを検証し、
  ポートが他プロセスに占有されていれば自動で隣にずれる

## 2026-08-17 に実行した機械照合

| 項目 | 方法 | 結果 |
|---|---|---|
| データ整合性 | `node tools/validate.mjs` | **エラー0件・警告0件・1,638問・1,017ユニット**（総数は作業前と不変） |
| 実ブラウザ通しテスト | `node tools/smoke.mjs` | **27項目すべて PASS**・console エラー0・通信エラー0 |
| Part 1 線画の幾何 | `node tools/scenecheck.mjs`（全30場面） | エラー0・警告4（**既知の4場面のみ**。`data-part` を持たない旧実装で、退行ではない） |
| level 1・2 の残存 | `assets/data/` を grep | **0件**（作業前は模試に1件あった） |
| 新規採番された設問 | `git show HEAD:<file>` と現行の設問 id を全ファイルで差分 | **97問**（差し替え元 58問が消滅。差の39問は、同じ枠を2〜4巡書き直した中間版が消えたぶん。ドリルの `q()` ヘルパー形式も含めて抽出） |
| 模試とドリルの複製 | 模試 Part5 150問 × ドリル Part5 350問の stem を内容語で Jaccard 比較 | 作業前**9組が40%以上一致**（最悪80%）→ 作業後**1組のみ**（それも「流用ではない」と判定済みのもの） |
| 英式・米式の綴りの一貫性 | 全1,017ユニットの英文を、英米で**綴りが実際に異なる形だけ**で対にして照合 | **同一ユニット内の揺れ0件**。全体の混在は文書ごとに選ばれており健全 |
| 「自然さで誤答を切っている」解説の抽出 | 全1,638問の `why[i]`（i≠answer）を正規表現で走査 | 62問を抽出（うち34問は構造語を1つも含まず） |
| 抽出の取りこぼし率 | 抽出に**かからなかった**語彙系189問から機械的に32問を標本抽出して全点検 | **欠陥21.9%・致命的9.4%**。抽出が捕まえるのは一部だけと判明 |
| 語彙・語法系の母集団 | 論点別に集計 | `colloc` 30 / `phrasal` 23 / `vusage` 28 / `confuse` 28 / `biz` 40 / `adv` 27 / `adjprep` 30 ＝ **189問。全数点検完了** |
| 未点検の残り | 語彙系と抽出分を除く Part 5/6 を論点別に集計 | **413問**（`voice` 41 / `p6ins` 34 / `conjprep` 34 / `ctense` 33 ほか） |
| 正解位置 | `validate.mjs` の分布出力 | 全論点・全模試で**作業前と同一**（差し替え時に `answer` を1つも動かしていない） |
| 設問番号 | `validate.mjs` の専用検査 | 模試5回とも **1〜200 の連番**・欠番なし・id 重複なし |

## 機械照合した項目（2026-08-14 のセッションでの検証）

すべて `node` で実データ（`registry.js` 経由で全 `UNITS` を実際に import）を集計するか、
`node tools/validate.mjs` の出力、または `grep`/`git diff` でコードそのものを確認して照合した。

| 項目 | 照合方法 | 結果 |
|---|---|---|
| 総問題数 1,634 / 総ユニット数 1,015 | `validate.mjs` 出力と、`registry.js` の全ファイルを実際に import して合算するスクリプトの2通り | 一致 |
| ドリル 634問・500ユニット・23ファイル | 同上（ファイル別内訳も算出） | 一致 |
| 模試 1,000問・515ユニット（各回200問・103ユニット） | 同上 | 一致 |
| 新規追加 383問・14ファイル | 旧コミット（`git archive HEAD`）の集計値との差分 | 一致（251→634、712→1015ユニット） |
| 37論点すべてが増加 | 旧コミットと現行の論点別問題数を全論点で突き合わせ | 全37論点で増加を確認 |
| 薄かった論点の増分（p7ins 1→12 等7論点） | 同上 | すべて一致 |
| level 4-5 が旧712ユニット中678（95%） | 旧コミットの全ユニットを level 別に集計 | 678/712=95.2%、一致 |
| Part 1 描写方式：`putting on` が旧データに0件 | 旧コミットに対する `grep -c` | 0件、一致（現行は11件） |
| `p1verb` 8→48問・`desc` 40問新規 | 論点別集計＋ `part1.js` の質問数集計 | 一致 |
| 模試 Part 1（30問）が `scene` のまま | 各 `*-l1.js` の `scene:` 出現数（ヘルパー行を除く） | 5回×6問＝30問、一致 |
| 各模試のパート構成 6/25/39/30/30/16/54=200 | `validate.mjs` の正解位置分布（パート別問題数の合計） | 5回全てで一致 |
| SRS 間隔 `[1,3,7,16,35,75]` 日、バグ修正済み | `store.js` の `LADDER` 定義と `recordItem()` の該当行を読み、`git diff HEAD` で修正差分を確認 | 一致（修正前は初日が抜けていたことも確認） |
| 話者ロール9種（8アクセント+NARR） | `audio.js` の `ROLES` オブジェクトを読み、`M-Am/W-Am/M-Br/W-Br/M-Au/W-Au/M-Cn/W-Cn/NARR` を確認 | 一致 |
| 制限時間 120分（フル）／75分（リーディングのみ） | `assets/js/views/mocks.js` の `timeLimitMs` | 一致 |
| 推定スコアの最低問題数（セクション20問） | `assets/js/score.js` の `MIN_SECTION_N` | 一致（20） |
| SVG線画28種 | `scenes.js` の `SCENES` オブジェクトのキー数 | 一致（28） |
| `level` が表示専用（SRS・出題順・絞り込み・推定スコアに不使用） | `assets/js/*.js` `assets/js/views/*.js` 全体を `grep` で `.level` 参照箇所を洗い出し | `quiz.js`（ヘッダ表示・結果への記録）／`drills.js`（一覧表示）／`result.js`（一覧表示）の3箇所のみ。SRS・出題順・絞り込み・推定スコアのロジックに参照なし |
| `validate.mjs` の新規検査項目（隣接参照・大小文字整合・why順序・重複・空所マーカー・scene/desc排他） | `tools/validate.mjs` を `grep` で該当ロジックを確認 | すべて実装済みを確認 |
| `balance2.mjs --by topic`、`validate.mjs --extra` | 両ファイルを `grep`・`Read` | 実装済みを確認 |
| `smoke.mjs` 27項目・ポート自動ずらし | ファイル冒頭コメントと `MAIN_PORT` ロジック | 一致 |
| `tools/node_modules` が gitignore 対象 | `.gitignore` を読み確認 | 一致 |
| `node tools/validate.mjs` エラー0・警告0 | 実行 | エラー0件・警告0件（総問題数1634問・ユニット1015件） |

### 照合の過程で見つかった、記述とコードの不一致

- **今回の作業指示にあった「`p4type` に広告型・ツアーガイド型のセットが無い」は不正確。**
  `listening2.js` の `u-p4t-02`（コメント「型②：ツアーガイド」、ガラス工場見学ツアーの導入）は
  **既存のツアーガイド型セットであり、このセッションでの変更もない**（`git diff` で無変更を確認）。
  実際に欠けているのは**広告型のみ**。`p4type` の現在のラインナップは
  館内放送（announcement）× 3・留守番電話（telephone message）× 1・ツアーガイド × 1・
  会議抜粋（meeting excerpt）× 1 の6セットで、広告（ラジオCM等）だけが無い。
  **→ 2026-08-15 に広告型（`u-p4t-04`）を追加し、この穴は埋まった。**
- `validate.mjs` が NOTE として出している既知のズレ（対応不要と判断済み）:
  `doc`（Part6/7）ユニット146件はすべて全体訳（`ja`）を持たない設計だが、CLAUDE.md の
  「`ja`（訳）」という説明はユニット全体に `ja` がある前提を示唆しうる。実データは一貫しており、
  Part 6/7 は設問単位の `ja`（各設問に訳が付く設計）のため実害はない。CLAUDE.md 修正までは
  ここでは行っていない（次に触る人向けの申し送り）。

## 検証済みの事項（初版から継続）

- 実ブラウザの通しテスト **27項目すべてパス**（旧24項目 + Part 1 描写方式3項目）
- GitHub Pages のサブパス配信（`/<repo>/`）を再現して動作一致を確認。絶対パス参照 0件
- 設問番号は模試5回すべて 1〜200 の連番。欠番・重複なし
- 正解位置はパート単位・論点単位で均等（数値・日付など順序に意味がある選択肢は対象外）

### 2026-08-15 に実行した検証

| 項目 | 方法 | 結果 |
|---|---|---|
| データ整合性 | `node tools/validate.mjs` | エラー0件・警告0件（1,638問・1,017ユニット） |
| 正解位置 | 同上の分布出力 | `p4type` A=4 B=4 C=4 D=4 / `p1verb` A=12 B=13 C=12 D=12 |
| 実ブラウザ通しテスト | `node tools/smoke.mjs`（CSS 修正後） | 27項目すべて PASS・console エラー0件 |
| Part 1 線画の幾何 | `node tools/scenecheck.mjs warehouse` | エラー0件・警告0件 |
| `warehouse-b` が読めるか | 544px・340px の light/dark で実描画して目視 | 「人がいない」「最下段が空」「箱が台に載っている」いずれも断定できる |
| 有人版との什器一致 | `warehouseFixtures()` の共有と両 PNG の照合 | 差は作業員1人だけ。什器は1単位も動いていない |
| 携帯幅の「次へ」 | 390×780 / 414×896 / 600×900 / 719×900 / 760×900 で `elementFromPoint` と実クリック | 全幅でボタン自身を返し、クリックで `#/exam` に留まり次の設問へ進む |
| 広い幅での退行 | 1280×860 で `getComputedStyle(.examnav).bottom` | `8px`（`.5rem`）のまま。見た目も修正前と同一 |
| 広告スクリプトの語数 | 実データから集計 | 153語（本番の3問セットは100〜150語。電話番号の読み上げ分を含む） |

## 開発の経緯

### 初版（3エージェント体制）
- **レビュー＋修正エージェント**：851問を1問ずつ監査して45件を検出（致命的5・要修正28・軽微12）。
- **ユーザーエージェント**（40歳英語教師役）：通し体験で25件の改善提案。
- **実装エージェント**：予想模試 Vol.3〜5 を作成。

### 2026-08-14
383問の新規ドリルを作成し、6本の監査で108件（致命的20件）を検出・修正。Part 1 の方式変更、
SRS バグ修正、品質管理ツールの `tools/` への移設・機能強化を実施。

### 2026-08-15
未決事項 2（広告型）・6（未使用場面）を消化して4問追加。**メインが体制を守らず自分で書いてしまい**、
その4問からレビュー役が要修正4件・軽微14件を検出した。オブザーバーの通し操作で
携帯幅の「次へ」が押せない不具合も見つかり、修正した。**要修正のうち2件は、メインが実装役に出した
指示そのものが誤っていたもの**（存在しない型の交替を解説に書かせた／設問タグの取り違え）。
**書き手が誰であれ、別の目を通さずに確定させてはいけない。**

## 設計上の判断メモ

- **過去問は使えない。** ETS が公開しておらず著作権保護されている。形式仕様のみ参照し全問を書き下ろした。
- Part 1 は模試では SVG 線画（28場面）、ドリルでは英語描写テキスト。線画のままでは
  `is wearing`／`is putting on` のような動作の識別を鍛えられないという構造的な限界があったため、
  ドリル側のみ本人の判断で方式変更した（詳細は「このセッションでの変更点 2」）。
- 合成音声には本番の連結・脱落・弱形がない。**リスニングは補助教材**と位置づけ、
  Part 3/4 はスクリプトと全体訳を精読・音読の素材として使うのが現実的。
- 端末間同期はサーバーがないため不可能。設定画面の「読み込んで合流」で代替している。
- 換算スコアは公式問題集の換算表の中央値をもとにした近似（`assets/js/score.js`）。±25点の幅で見る。
- **level は表示専用**（機械照合済み。上表参照）。SRS・出題順・絞り込み・推定スコアの設計判断には
  一切使っていない。難度別の絞り込み機能を将来追加するなら、この前提を崩さないよう設問単位の
  `question.level` 新設とセットで設計し直す必要がある。

## 未決事項の更新（2026-08-25 時点）

**下の一覧は 2026-08-18 時点のものです。この日の作業で状態が変わった項目を先にまとめます。**

### 解決した項目
- **14（図表セットの鎖）** — 個別是正では閉じないと判明し、**構造ごと本番仕様（1セット1問）に作り直した。**
  あわせて残した図表30問のうち12問の欠陥を是正し、**19巡かけて「3経路とも25%」まで収束させた。**
  作り方は CLAUDE.md に確定形で記録済み（種別×種別の 2×2）。
- **6 のうち中断セッション2件** — `mocks.js` の無言破棄など**同型7箇所＋実バグ1件**を是正。smoke 27→33項目。
  ホームの中断セッション一覧は3件＋折りたたみに。

### この日に新しく判明した項目
1. **正解位置のドリフト（是正済み）と、その周辺の未解決分。**
   `balance2.mjs` の位置依存バグが「前半 D・後半 A」の流れを作っていた。検査Fを新設し `--by drift` で全巻是正済み。
   **ただし件数の側がまだ整いすぎている**——Part 5 が全6巻とも `A=8 B=8 C=7 D=7`、
   Part 6 が全6巻とも `A=4 B=4 C=4 D=4`、Part 7 が6巻中5巻で `A=14 B=14 C=13 D=13`。
   検査は通るが「均等」の指示が作った指紋に見える。**16問の Part 6 で「A を4つ見たらもう A は来ない」は使える。**
2. **`topics: ['graphic']` の継承もれが 32問**（vol1 8・vol2 11・vol4 4・vol5 3。担当外で未着手）。
   `set()` / `talk()` は `t:` を書かないとユニット既定を継承するため、**分析画面の論点集計が実数の数倍にずれる。**
3. **Part 2 の装置重複が 9組以上**（`vol1` No.11 ↔ `vol4` No.12 は stem・正解・誤答の3点一致、
   `vol2` No.20 ↔ `vol4` No.27 は**解説文が完全一致**）。2組だけ是正済み。
4. **Part 3/4・Part 7 の「後の設問が前問の答えを漏らす」型**が、今日直した分のほかに
   **`vol5` No.83–85 / No.96–97 / No.77–78、`vol1` No.66–67 など**で報告されている。
   **機械照合では捕まらない**（語彙の一致がゼロでも「答えの根拠」を共有していれば漏れる）。
5. **`vol5` の Part 3/4 で「正解が単独最長」が 39%**（既存の書き癖。CLAUDE.md の閾値は3割）。
   `vol4` の Part 3 も 56%。**今日書いた分はすべて閾値内。**
6. **同一ユニット内で `why` が同一文字列**のものが Vol.1・2 だけで9件。
   `validate.mjs` の隣接参照検査（`同上` 等）には掛からない。**検査の追加を検討する価値がある。**
7. **図表設問数が仕様（1巻5問）から外れている巻**：`vol2` が7問、`vol4` が6問。
   利用者の判断で Vol.5 のみ 9→5 に減らしたため、この2巻は残置。

### 今日の作業から得た、次に着手するときの申し送り
- **是正は平均して新しい欠陥を作る。**図表だけで19巡し、毎回「前の是正が作った漏れ」を直した。
  **1巡で終わる前提で見積もらないこと。**
- **「特定の形を禁じる」是正は収束しない。**禁じた形を避けた先が一意に決まると、そこが新しい指紋になる。
  **避けるべき形が存在しない構造に組み替える**しかない（CLAUDE.md に記録）。
- **収束の判定基準を先に決めてから監査に出すこと。**基準が無いと、監査は際限なく細かい指紋を報告し続ける。
  今回は「3経路とも25〜33%なら収束。複数巻を突き合わせないと気づけない指紋は不合格にしない」とした。

## 未決事項（次に着手するなら、まず本人に方針を確認すること）

1. **一斉点検の残り：文法・文脈系の Part 5/6 が 413問未点検。**
   2026-08-17 の点検は**語彙・語法系**（`colloc` `phrasal` `vusage` `confuse` `biz` `adv` `adjprep` の189問。
   全選択肢が実在語で第二の正解が生まれやすい母集団）と**機械抽出の62問**を対象にした。
   残るのは `voice` 41 / `p6ins` 34 / `conjprep` 34 / `ctense` 33 / `pron` 31 / `vform` 31 / `connect` 29 /
   `quant` 26 / `rel` 26 / `subj` 25 / `verbal` 25 / `ptcp` 24 / `pos` 22 / `comp` 22 / `inv` 20 / `cohesion` 17。
   **「文法問題は誤答が構造的に不可能だから安全」は成り立たなかった**——中核文法10問を点検したとき
   **4問（40%）が致命的**だった（`stop doing` / `regret doing` / `keep themselves informed` /
   `Passengers booking`）。着手するなら CLAUDE.md の「誤答の作り方」を先に読むこと。
2. **既知の残存リスク（判断のうえで残したもの）。**
   基準は「**利用者が辞書を引いて解説と衝突するか**」。主要辞書が立項していない形は、
   コーパスの裾に数%あっても残置した。追い続けると際限がないため。
   - `versed with`（`versed in` の2.7〜3.5%。辞書は立項なし）／`dully signed`（0.02%）／
     叙述の `minimum`（1.6%。LDOCE と Cambridge は禁じるが **Oxford は [usually before noun]** とヘッジ）／
     `separately of`（Wikipedia 1件・1892年の米法廷文に実例。現行辞書はどれも立項せず誤用形と判定）
   - `colloc3-04r` の `why` が「LDOCE・M-W・Collins」と3辞書を名指ししているが、
     **M-W と Collins は 403 で一次確認できていない**（LDOCE と Wiktionary のみ確認済み）
3. **`set`（Part 3/4）と `doc`（Part 6/7）の `level` は未実施。**
   1ユニットに複数設問が乗り `level` はユニット単位でしか持てないため、
   設問単位で正確にやるには `question.level` の新設が要る。**模試も方針判断により未実施**
   （65%が lv5 に張り付いている。ただし模試は本番の再現が目的で、難度表示の価値がドリルより低い）。
4. **模試 Vol.7 以降。Vol.6 の実績を踏まえた見積もり。**
   Vol.6 は 200問を書いたのち**約140問を作り直した**。工数の大半は作問ではなく**是正**だった。
   - **既存の巻を雛形にしないこと。**「Vol.N の鏡写しにせよ」は内容の複製を生む。
     構造は**数値で指定**し、担当に既存ファイルを開かせない（CLAUDE.md に手順を記載）
   - **作問時に `assets/data/` 全体（ドリル込み）と機械照合する工程を必ず入れること。**
     Vol.6 では「既存の巻を避けた先でドリルと衝突する」が**7回**起きた。
     照合は **stem 単位では足りず、選択肢1文ごと**にかける（この粒度で初めて出た衝突が2件）
   - **機械照合は下限であって上限ではない。**Vol.6 の一括照合では4方式で5件が出たが、
     **読んで発見された衝突が別に4件**あり、うち2件は語彙の一致がゼロだった
     （共有していたのは**装置**と**正解の命題**だけ）
   - **正解位置は「均等」ではなく「偶然に見えること」を要求する。**
     指示を出すたびに別の規則性が生まれた（均等→輪番、循環を崩せ→降順と反復回避）。
     `validate.mjs` に検査A〜Eを追加済み
   - **利用者目線で実際に通しで解く役を必ず入れること。**Vol.6 では、機械検査を全部通した状態から
     オブザーバーが**致命的4件**（正解位置の規則性・設計仕様違反の線画設問・設問間の漏れ・
     絵と食い違う解説）を出した。**データの正しさと、解く体験の正しさは別。**
   - `topics.js` の型の在庫が尽きている論点がある点は 2026-08-17 の記述のまま有効

5. **未解答を SRS に記録するかは未決（利用者の判断待ち）。**
   `quiz.js` の `finish()` は `chosen != null` のときだけ `recordItem` を呼ぶので、
   **時間切れで空欄のまま終わった設問は SRS に入らない**。
   **Part 7 の時間切れは 800点台の標準的な失敗の形**なので、いちばん復習したい設問が復習に載らない。
   一方、未解答を「不正解」として記録すると正答率と推定スコアの意味が変わる
   （**解けなかったのではなく解く時間が無かった**）。結果画面の一覧には出すようにした。
6. **画面まわりの未対応**（オブザーバーが「使いにくい／気になる」と判定。**「使えない」は無し**）:
   - 誤答一覧に**一括展開が無い**／論点チップで飛んで戻ると**開閉とスクロール位置を失う**
   - **結果画面でフラグを付けられない**（フラグは演習中の F キーのみ）
   - 390px のフル模試・R模試、360px 全般で `.exambar` が**2段のまま**（タイマーが8桁になるため）
   - **Part 1・Part 2 ドリルの一覧が全行同一表記**（`写真描写（1問）　Part 1・写真描写　難度N` が49行）。
     ネタバレ防止の設計意図は理解できるが、**難度の数字だけが唯一の識別子**になっている
   - **模試 Part 5 の30問に語注（`vocab`）が無い**（`p5()` ヘルパーが渡していない。既存仕様）。
     ドリル側は全問付いているので、模試の復習だけ手当てが薄い
   - **中断中の演習がホームに1件しか出ない**（`home.js` が `sessions[0]` だけを描画）。
     2026-08-17 に復習セッションのキーを `rev-wrong-*` / `rev-blank-*` に分離したため
     **2本同時に残せるようになったが、古い方はホームに出ず到達不能**になる。
   - **結果画面のボタンから中断した復習を再開できない**（`startReview()` が `getSession()` を見ずに
     `launch()` するので 0/N から再スタートし、保存済みセッションを上書きする）。
     **どちらも 2026-08-17 以前からの挙動だが、キー分離で踏みやすくなった。**
7. **`p7ins`（位置選択）と `graphic`（図表）は正解位置を平準化できない。**
   選択肢が `[1]`〜`[4]` や金額・時刻など順序を持つため、`balance2.mjs` が構造的に除外する。
   これは欠陥ではなく正しい挙動（CLAUDE.md に明記済み）。
8. **Part 1 の線画について、次に触るときの申し送り**（`design/part1/SPEC.md` と併読）:
   - **座位＋手前什器の組み合わせに注意。** 座位は立位より頭頂が26単位低く、天面 y=172〜182 の
     什器と干渉して腿が隠れ、立位と区別できなくなる。`laboratory-seated` は人物を
     `frontFigure` 層（手前の面より後に描く層）へ移して解決した
   - **仕様の数値より「544px で読めるか」を優先してよい。** `laboratory-seated` の腿は
     見本の約9°では座面の線と融合して「箱に腰かけた胴」に見えたため、約20°まで倒してある
   - **天面 y を人物の股関節 y にぴったり揃えないこと**（2026-08-17 に判明）。`trapezoid` は root の
     `stroke` を継承して胴の下辺にも線が出るため、人物が**「天板の上に置かれた台形」に見える**。
     6単位ほど上げると解消する。`meeting-room` / `kitchen` の描き直しで実際に踏んだ
   - **未使用の場面は無くなった。**`meeting-room` / `kitchen` は 2026-08-17 に描き直して
     **作問可能な状態**にした（`warehouse-b` は 2026-08-15 に `p1v-09` で使用開始）。
     ただし**まだどの設問からも参照されていない**ので、使うなら新規作問が要る。
     何が問えて何が問えないかは 2026-08-17 の監査で整理済み——
     `kitchen` は調理人の脚がカウンターに完全に隠れるので**立位/座位は問えない**、
     火・湯気・水を描いていないので `boiling` / `steam` / `washing dishes` は**正解にできない**（誤答には使える）。
     `meeting-room` は椅子を描いていないので `chairs` に言及する文は不可、
     着席3人の腿はテーブルに隠れるため**根拠は頭頂高の差のみ**で、細かい姿勢は問えない
   - **`warehouse` 系の細部**（オブザーバーが実描画で指摘。実害はないが、次に触るなら）:
     棚の支柱が床線に届かず**棚が宙に浮いて見える**（支柱 y=60〜210 / 床線 y=250）。
     各段の筋交いが斜線1本なので拡大すると「引っかき傷」に見える。
     パレットが板1枚＋脚3本で、低い台には見えるが**パレットとは断定しにくい**
     （模試側で「パレットか否か」を問うなら桟の補強が要る）
   - **4場面は `scenecheck.mjs` の検査対象外**（`garden` / `waterfront` / `parking-lot` / `beach`）。
     `data-part` 属性を持たない旧実装のままなので、12項目のうち手・腕・姿勢・箱に関する9項目が
     判定されず、通っているのは色・線幅・viewBox の3項目だけ。**この4場面は目視しか頼りがない**
     （`node tools/scenecheck.mjs` が WARN として毎回報告する。エラー0でも「全部検査できた」ではない）。
     いずれも使用中（`garden` 1問 / `waterfront` 3問 / `parking-lot` 2問 / `beach` 1問）
   - 線画を1つ変えると、その場面を使う**すべての設問に波及する**。必ず波及先を洗い出して再検証する
9. **合成音声が実際にどう発音するかは、誰も確認できていない。**
   Claude は音を聞けないため、`that'd` / `it'd` のような弱化の強い形が聞き取れるかは検証不能。
   気になる箇所は本人が再生して確かめるしかない（`audio.js` 側の実装に問題がないことは確認済み）。
   **オブザーバーが設定画面で確認した実測**（2026-08-17）: 検出された英語音声は4種で、
   **M-Br / W-Br / M-Au の3ロールが同じ Daniel、W-Au / W-Cn が同じ Karen** に割り当たっていた。
   アプリ自身も「実際には2〜3アクセントになることがあります」と断っている。
   つまり**英・豪・加アクセントの聞き分けは、この環境では訓練できない。**
   900帯で実際に落ちるのは豪と英の速い発話なので、ここは本アプリの守備範囲外と割り切るしかない
   （音声ファイルを持たない設計上、避けられない）。

10. **`Part 6`・`Part 7` には訳が1問も無い**（模試95・ドリル51ユニットすべて `ja` 無し）。
    設計上の判断だが、Part 1 に4文の訳を入れたことで**非対称が際立つ**ようになった。
    `validate.mjs` が NOTE として毎回報告している既知のズレでもある。

11. **既存模試 Vol.1〜5 が互いの再スキンである（2026-08-18 に判明）。**
   `〈姓〉&〈姓〉〈専門職〉` の意見書と `Prepared for the 〈X〉 Committee` の報告書が**5巻すべて**、
   `Subject: Upgrade query` と `The Provincial — 〈分野〉` が4巻（**vol4 と vol5 は日付まで同一**）、
   `bookings@〜.ie` の団体予約が4巻（すべて最終スロット）。
   Part 3 にも波及（`vol2-l2b` と `vol5-l2b` の 65–67 が互いにほぼ逐語）。
   **Vol.6 だけの問題ではないので、既存5巻をどうするかは本人の判断が要る。**
12. **544px で読み取れない線画が7件（2026-08-18 に全30場面を実描画して判明）。**
    `beach` のデッキチェア（背もたれも脚も無い台形）／`waterfront` のボート（**船体の台形が上下逆**で
    ランプシェードに見える）と橋／`laboratory` の顕微鏡（スタンドライトに見える）／
    `library` の本（`propBooks` の y に棚板 y をそのまま渡しており10下へずれ、棚板が本を貫通）／
    `crosswalk` の縞（`rect()` が必ず stroke を付けるため箱に見える）／
    `factory` の `propMachine`（床から浮いて壁掛け盤に見える）／`painting-wall` のローラー（柄の向きが逆）。
    幾何的な重なりも6件（`presentation` の脚が聴衆を貫通、`market-stall` の支柱が売り手に完全に隠れる 等）。
    **`scenes.js` のコメントには全部記録済み**（原因まで特定してある）。
    **`scenecheck.mjs` は `data-part` の無い什器を見ないので、これらを素通りする。**
    絵を直すと現在それに依存している設問が動くので、**設問の見直しとセットで行うこと。**
13. **模試の位置選択（`p7ins`）で [1] と [4] が一度も正解になっていない。**
    6巻12問がすべて [2] か [3]（ドリルには [1]×3・[4]×2 がある）。
    **利用者が模試を繰り返すと「1と4は選ばなくていい」と学習してしまう。**
    全巻を通した是正が要る。**閉じ方は照応ではなく算術・時系列・初出違反で設計すること**
    （Vol.6 では照応で閉じようとして2問とも2〜3回失敗した）。
14. **図表セットで設問が鎖になっている既存5件。**
    前問の正解を先行詞にする型（1問落とすと2問落ちる）：
    `vol1-l2b` No.63「that room」／`vol2-l2b` No.54「that model」／`vol3-l2b` No.57「that option」／
    `vol5-l2a` No.45「that route」／`vol5-l3` No.99「that unit」。**本番はこう繋がない。**
    あわせて「**後の設問の stem・選択肢が前の設問の答えを名指ししている**」型も全巻で走査すること
    （Vol.6 では7件出た。TOEIC は設問を先読みできるので実際に得点差になる）。
15. **既存模試の Part 2 で、正解と装置が重複している組。**
    `vol2-l1` No.8 の正解 `Legal is still reviewing it.` と `vol4-l1` No.22 の
    `Legal is still drafting the paperwork.` が同装置（Vol.6 でも3度目を書きかけて差し替えた）。
    `vol4-l1` No.13 と `vol5-l1` No.9 は stem・正解の装置ともほぼ同一。
    **stem 単位の照合ではすり抜けるので、選択肢1文ごとにかけること。**
16. **「Vol.1 との解き比べで伸びを測る」という Vol.6 の看板について、判断が要る。**
    利用者目線で通しで解いた役の指摘：
    > 答えは違うので記憶で当たるわけではないが、**設問の役割まで同じ**なので、
    > Vol.1 を解いた人は**読み方を丸ごと再利用できる**。伸びを測る器としては目盛りが狂う。

    該当は Part 4 の 77–79・86–88、Part 7 の 161–164・159–160、Part 2 の No.11 など。
    **今回は基準を変えなかった**（CLAUDE.md の「答えが漏れるか」で判定し、漏れないものは残した）。
    理由は (a) 追うと10セット以上の再作問になり、Vol.6 の実績では**書き直しの約半数が新しい欠陥を生んだ**、
    (b) 逆方向に振れると本番の形から外れる（実際の TOEIC も Part 4 のジャンル配分は概ね決まっている）。
    **ただし「測定器として機能するか」は「得点が漏れるか」より広い基準で、本人の判断を仰ぐべき論点。**
17. **Vol.6 に残した既知の軽微な点。**
    - `vol6-l1.js` No.14 の誤答 `The assembly line runs until six.` は閉じているが薄い
      （判定の根拠：応答として成立させるには「稼働中のみ必要」という前提が要るが、
      **その前提こそが設問の問うている内容**なので循環する）
    - `vol6-l1.js` No.23 の正解 `Twice a month, usually.` が `vol5-l1` No.7 の正解と J=0.67。
      `How often` が聞ければ頻度表現の選択肢は1つしかないので**記憶による上乗せはゼロ**と判定した
    - `vol6-r4.js` の 186–190 と 191–195 が「**見出しの数字がそのまま使える数ではない**」という
      発想で近い（答えの型も誤答の作りも別なので漏れないと判定）
