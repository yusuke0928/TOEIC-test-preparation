# 進捗と引き継ぎ

最終更新: 2026-08-15 — Part 4 広告型の追加・未使用場面の活用・携帯幅の不具合修正

## 状態：GitHub Pages へ公開できる状態を維持

初版からの変更点はすべて `assets/**` 内で完結しており、公開手順（`.github/workflows/pages.yml`）に
変更はない。まだ push していなければ、初版の README の手順どおりでよい。

## 収録内容

| 区分 | 内容 | 問題数 |
|---|---|---|
| 個別論点ドリル | 37論点（文法12・語彙7・文脈4・読解7・リスニング7） | 638 |
| 予想模試 Vol.1 | 標準回 | 200 |
| 予想模試 Vol.2 | リスニング強化回（図表・意図問題を増量） | 200 |
| 予想模試 Vol.3 | リーディング高負荷回（Part 7 の語数を上振れ） | 200 |
| 予想模試 Vol.4 | 語彙難化回（900帯の語を正解位置に） | 200 |
| 予想模試 Vol.5 | 総仕上げ回 | 200 |
| **合計** | | **1,638** |

各模試のパート構成は本番と完全一致（Part 1:6 / 2:25 / 3:39 / 4:30 / 5:30 / 6:16 / 7:54）。
ドリルは 23 ファイル・502 ユニット、模試と合わせて総ユニット数 1,017 件。

## 2026-08-15 の変更点（未決事項 2・6 の消化）

作業体制を **CLAUDE.md の冒頭に明記した**（利用者の指示。以後も継続）。
メインの Claude は進捗・品質管理のみを担当し、実装は Sonnet 5 の実装役、監査は Opus 5 のレビュー役、
利用者目線の通し確認は Opus 5 のオブザーバーが行う。今回は**この体制を守らずメインが自分で4問書いてしまい、
指摘を受けてから監査に回した**。結果、下記のとおり自作分から要修正が続けて出た。**規模の小ささは理由にならない。**

### 1. Part 4 に広告型のセットを追加（未決事項 2 — 完了）
`listening2.js` に `u-p4t-04`（3問）。館内放送3・留守電1・ツアーガイド1・会議抜粋1 に広告が加わり、
`p4type` の型の網羅が揃った。`topics.js` の副題「留守電・館内放送・広告・ツアー」と中身がやっと一致した。
**`p4type` の正解位置がちょうど A=4 B=4 C=4 D=4 に揃った。**

### 2. 未使用場面 `warehouse-b` を使う Part 1 を追加（未決事項 6 — 部分的に完了）
`listening.js` に `p1v-09`。**有人版 `p1v-01` の直後に配置**し、1問目・2問目で見比べられるようにした。
`warehouse` と `warehouse-b` は `warehouseFixtures()` で什器座標を共有しているため、
**2枚の差は作業員1人だけ**。`is being + 過去分詞` が動作主を要することを、同じ絵の有無で示せる。

**ドリルの Part 1 に線画方式を併用する方針を確定した**（利用者の判断）。
線画でしか問えない論点があるため。CLAUDE.md に明記済み。

### 3. 携帯幅で「次へ」が押せない不具合を修正（新規発見）
オブザーバーが実機幅の通し操作で発見。**880px 以下で `.tabbar`（fixed, z-index 70）が
`.examnav`（sticky, z-index 6）を覆い、「次へ」の露出が上端 14px だけになっていた。**
ボタン中心をタップすると**演習を離脱して `#/analytics` へ飛ぶ**。390×780 / 414×896 / 600×900 /
719×900 / 760×900 で再現。`app.css` の `@media(max-width:880px)` 内に
`.examnav{bottom:calc(env(safe-area-inset-bottom) + 3.6rem)}` を追加して解消
（3.6rem = タブバー実測 48.34px ≒ 3.02rem ＋ 従来と同程度の余白 約9px）。
881px 以上は従来どおり。全幅で `elementFromPoint` がボタン自身を返すこと、実クリックで
`#/exam` に留まり次の設問へ進むことを確認済み。

### 4. CLAUDE.md の記述の誤りを2件是正
- 「個別論点ドリルの Part 1 は `desc` 方式」→ **実際は `listening.js` に scene 方式が8問残っていた**
  （`part1.js` の冒頭コメントにも書かれていたが、CLAUDE.md と PROGRESS.md には反映されていなかった）
- 「SVG 場面は模試専用」→ **実際はドリルも使っていた**

### 5. 監査で出た欠陥（メインが自分で書いた4問に対して）
レビュー役2巡・オブザーバー1巡で、**致命的0件・要修正4件・軽微14件**。要修正の中身:
- **広告の本文が自己矛盾していた。**「月額に設計相談が含まれる／別建て請求は季節の装飾だけ」と言った直後に
  「設計料を免除する」。含まれている料金は免除できない
- **`p1v-09` が絵を見ずに解けた。** 誤答3つを全部「人が写っていないと成立しない文」で揃えてしまい、
  無人と分かった時点で消去法だけで解けた。**線画方式を使う意味を自分で消していた。**
  誤答1つを「無人でも成立するが絵と合わない状態描写」（最下段が空なので `Every shelf has been filled
  with cartons.` は偽）に差し替えて、動作主の有無＋絵の照合の二段構えにした
- **解説の対比説明が事実に反していた。** 「作業員が1人入ると正解の型が完了受動に入れ替わる」と書いたが、
  有人版 `p1v-01` の正解も `Cartons have been stacked on the shelving.` で**同じ完了受動**。
  方向も逆で、そもそも入れ替わらない。「人の有無で変わるのは進行受動を選べる可能性が生じるかどうかであって、
  正解の型が自動的に入れ替わるわけではない」に書き直した。**これはメインが実装役に出した指示自体が誤っていた**
- 解説に内部 ID（`p1v-01`）と「1問目」という順序表現がそのまま出ていた。
  利用者には意味がなく、順序は SRS の復習で変わりうる。内容で指す表現に変えた
- 広告のトークが180語（本番は100〜150語、このドリル内で最長）→ 153語に短縮

## 2026-08-14 の変更点

### 1. 論点別ドリルを 251問 → 634問 に補充（+383問・新規14ファイル）
新ファイル: `grammar3〜6.js` `vocab3〜5.js` `context2〜3.js` `reading3〜4.js` `listening3〜4.js` `part1.js`。
`registry.js` の `DRILL_FILES` に登録済み。**37論点すべてが増加した**（機械照合済み、下記参照）。
とくに薄かったもの: `p7ins` 1→12 / `p7not` 2→12 / `p7intent` 2→15 / `p7syn` 3→14 /
`cohesion` 4→14 / `connect` 4→16 / `ctense` 5→16。

新規383問を6本の監査（Opus）で1問ずつ検算し、**108件の欠陥（うち致命的20件）**を検出・修正した。
致命的の中身はすべて「演習として成立していない」もの:
- 誤答が実際には正しい英語だった（`has arrived` は時・条件の副詞節で使える／`helpful to doing` は
  正解より高頻度／`grant a refund` `practice discretion` `accrue expenses` はいずれも実在／
  `circumspective` は実在の形容詞）
- 狙った論点が別の規則で空振り（仮定法の問題なのに主語が複数で、主述の一致だけで誤答が切れた）
- 図表問題が音声だけで解けた
- 文挿入の正解位置が他の空所でも成立した
- チャットの意図問題で、引用文そのものが時刻計算と矛盾していた

### 2. Part 1 を描写テキスト方式に変更（利用者本人の決定）
線画では `is wearing` / `is putting on` の識別が原理的に作問できなかった（棒人間に服がないため）。
**個別論点ドリルの Part 1 は「英語の場面描写を数秒表示 → 消える → 音声4文」形式に変更**（`desc` フィールド、
40問新規作成。`p1verb` は 8→48問）。**模試5回のPart 1（30問）は SVG のまま**（本番の見た目・時間感覚の
再現が目的のため変更なし）。`quiz.js` の `renderP1()` が `scene` と `desc` を分岐する後方互換の実装。
「もう一度見る」で描写を再表示できる（音声非対応環境で解答不能にならないため）。

### 3. 模試 Part 1 の不整合是正
30問中13問に問題があった。線画と正解・解説が矛盾していたもの（人物の腕が対象に届いていない、
箱がトラックの窓に見える、人がカウンターの上に立っている等）を、`scenes.js` の場面と設問で是正。
`is being + p.p.` の解説が「動作主が写っている」と言いながら写っていなかった設問を、
ルールそのものを教える構成に作り替えた。同じ場面を使う設問の正解文がほぼ同一だった重複も解消。

### 3b. 模試 Part 1 の線画を全面的に描き直した（3 のあとに実施）
Claude Design で設計仕様を起こし、**30場面すべてを新しい部品系で作り直した**。
仕様と見本 SVG は **`design/part1/`** にある。**以後これが正。Claude Design を再度開く必要はない。**

- 部品ライブラリ（`figure` / `fixture` / `prop` / `box3` / `limbArm` / `contact` / `composeScene`）。
  **描画順は `composeScene()` の固定層で強制**する。前後関係が描画順そのものなので、
  ここが崩れると「カウンターの天面に立っている」が再発する
- **腕の長さ制限（肩→肘30 / 肘→手28）を `limbArm()` の中で毎回測り、超過時に警告する。**
  「顕微鏡まで32単位離れていた」種類の欠陥は構造的に起きなくなった
- **`laboratory-seated` を新設。** 同じ scene ID を複数の設問が別々の意図で参照していて
  （vol1「立って顕微鏡を調整」/ vol4「座って作業台に着く」）、1枚の静止画では両立できなかったため。
  **これが場面バリエーションの型**（`warehouse` / `warehouse-b` の有人・無人も同型）
- **38問（模試30＋ドリル8）を1問ずつ絵と突き合わせて再検証し、16件の不整合を修正。**
  正解位置は1問も動かしていない（A12 / B11 / C8 / D7）

**`tools/scenecheck.mjs`** を新設した。仕様の受け入れチェック12項目を機械化したもの。
正規表現ではなく実際に Chromium で描画し `getCTM()` で transform を解決して座標を測る。

#### ここで得た教訓（次に線画を触るとき必ず読むこと）
**幾何検査だけでは足りない。** `box3()` にも人物の頭部にも輪郭線が無く、
`--card-2`（塗り）と `--card`（背景）の差はわずかなので **544px では存在自体が読めていなかった**。
座標はすべて正しく、`scenecheck.mjs` の12項目を通過していた。
**「数値が正しい」と「読み取れる」は別物で、後者は実際に描画して目で見るしかない。**

実際、作成担当が目視したのは38場面中14場面のみで、**未目視の場面に重大欠陥が集中していた**
（`train-platform` の乗客が車体に溶けていた、`factory` の箱が不可視、`library` の本が不可視など）。
**線画を変えたら、全場面を 544px・light / dark でレンダリングして必ず目視すること。**

もう1点、**`frame()` の root `<svg>` が `stroke` を持つ**構造にしてある。
`design/part1/*.svg` の見本が継承前提で書かれているため。
新しい部品を足すとき `stroke` を書き忘れても線が出るのはこのため。root から外さないこと。

### 4. 難度 level の目盛りを定義（CLAUDE.md に明記済み）
既存データは712ユニット中678（**95.2%**、機械照合済み）が level 4-5 に張り付き、目盛りとして
機能していなかった。**level は表示専用**（SRS・出題順・絞り込み・推定スコアには一切関与しない）。
新しい定義は CLAUDE.md 参照（level 3=800〜860帯 / 4=860〜900帯 / 5=900+ 帯、1・2 は作らない）。
新規383問はこの定義に従って作成済み（新規分を含めた全体では level 4-5 が 1,015 ユニット中 899
〈88.6%〉に緩和）。**既存分の付け直しは未実施**（設問単位で正確にやるには `question.level` の新設が要る）。

### 5. 実バグの修正
- **SRS のはしご段が1段ずれていた。** `store.js` の `recordItem()` が初回正解時に `streak` を 0→1 に
  した直後 `LADDER[1]`（3日）を引いており、`LADDER[0]` の「1日」が永久に使われなかった。
  README が謳う「1 → 3 → 7 → 16 → 35 → 75 日」が実際は「3 → 7 → …」だった。修正済み・機械照合済み。
- 音声非対応環境で「この端末は音声非対応」の表示が、直後の `playerBox()` 更新で上書きされていた
  （表示のみ、実害なし）。`quiz.js` の該当箇所に `audio.supported` の条件を追加して修正済み。

### 6. 品質管理ツールを `tools/` に移設・強化
前セッションでは scratchpad にあったため消失した。**`tools/` に置き直し、リポジトリに含めた**
（依存は `tools/package.json`、`node_modules` は gitignore）。
- `validate.mjs` に検査を追加: `why` の隣接参照（`同上` 等）／選択肢の大小文字と空所位置の整合／
  `why[answer]` のみ「正解」始まり／選択肢の重複／空所マーカー個数／Part 1 の `scene`・`desc` 排他。
  `--extra <ファイル…>` で registry 未登録のファイルも検査できる
- `balance2.mjs` に **`--by topic`**（論点単位の平準化）を追加。ドリルはほぼ全部 Part 5 のため、
  パート単位では論点単位の偏りが解消しない。模試は従来どおりパート単位
- `smoke.mjs` は 24→**27項目**（Part 1 描写テキスト方式の3項目を追加）。サーバの配信ルートを検証し、
  ポートが他プロセスに占有されていれば自動で隣にずれる

## 機械照合した項目（このセッションでの検証）

すべて `node` で実データ（`registry.js` 経由で全 `UNITS` を実際に import）を集計するか、
`node tools/validate.mjs` の出力、または `grep`/`git diff` でコードそのものを確認して照合した。

| 項目 | 照合方法 | 結果 |
|---|---|---|
| 総問題数 1,634 / 総ユニット数 1,015 | `validate.mjs` 出力と、`registry.js` の全ファイルを実際に import して合算するスクリプトの2通り | 一致 |
| ドリル 634問・500ユニット・23ファイル | 同上（ファイル別内訳も算出） | 一致 |
| 模試 1,000問・515ユニット（各回200問・103ユニット） | 同上 | 一致 |
| 新規追加 383問・14ファイル | 旧コミット（`git archive HEAD`）の集計値との差分 | 一致（251→634、712→1015ユニット） |
| 37論点すべてが増加 | 旧コミットと現行の論点別問題数を全論点で突き合わせ | 全37論点で増加を確認 |
| 薄かった論点の増分（p7ins 1→12 等7論点） | 同上 | すべて一致 |
| level 4-5 が旧712ユニット中678（95%） | 旧コミットの全ユニットを level 別に集計 | 678/712=95.2%、一致 |
| Part 1 描写方式：`putting on` が旧データに0件 | 旧コミットに対する `grep -c` | 0件、一致（現行は11件） |
| `p1verb` 8→48問・`desc` 40問新規 | 論点別集計＋ `part1.js` の質問数集計 | 一致 |
| 模試 Part 1（30問）が `scene` のまま | 各 `*-l1.js` の `scene:` 出現数（ヘルパー行を除く） | 5回×6問＝30問、一致 |
| 各模試のパート構成 6/25/39/30/30/16/54=200 | `validate.mjs` の正解位置分布（パート別問題数の合計） | 5回全てで一致 |
| SRS 間隔 `[1,3,7,16,35,75]` 日、バグ修正済み | `store.js` の `LADDER` 定義と `recordItem()` の該当行を読み、`git diff HEAD` で修正差分を確認 | 一致（修正前は初日が抜けていたことも確認） |
| 話者ロール9種（8アクセント+NARR） | `audio.js` の `ROLES` オブジェクトを読み、`M-Am/W-Am/M-Br/W-Br/M-Au/W-Au/M-Cn/W-Cn/NARR` を確認 | 一致 |
| 制限時間 120分（フル）／75分（リーディングのみ） | `assets/js/views/mocks.js` の `timeLimitMs` | 一致 |
| 推定スコアの最低問題数（セクション20問） | `assets/js/score.js` の `MIN_SECTION_N` | 一致（20） |
| SVG線画28種 | `scenes.js` の `SCENES` オブジェクトのキー数 | 一致（28） |
| `level` が表示専用（SRS・出題順・絞り込み・推定スコアに不使用） | `assets/js/*.js` `assets/js/views/*.js` 全体を `grep` で `.level` 参照箇所を洗い出し | `quiz.js`（ヘッダ表示・結果への記録）／`drills.js`（一覧表示）／`result.js`（一覧表示）の3箇所のみ。SRS・出題順・絞り込み・推定スコアのロジックに参照なし |
| `validate.mjs` の新規検査項目（隣接参照・大小文字整合・why順序・重複・空所マーカー・scene/desc排他） | `tools/validate.mjs` を `grep` で該当ロジックを確認 | すべて実装済みを確認 |
| `balance2.mjs --by topic`、`validate.mjs --extra` | 両ファイルを `grep`・`Read` | 実装済みを確認 |
| `smoke.mjs` 27項目・ポート自動ずらし | ファイル冒頭コメントと `MAIN_PORT` ロジック | 一致 |
| `tools/node_modules` が gitignore 対象 | `.gitignore` を読み確認 | 一致 |
| `node tools/validate.mjs` エラー0・警告0 | 実行 | エラー0件・警告0件（総問題数1634問・ユニット1015件） |

### 照合の過程で見つかった、記述とコードの不一致

- **今回の作業指示にあった「`p4type` に広告型・ツアーガイド型のセットが無い」は不正確。**
  `listening2.js` の `u-p4t-02`（コメント「型②：ツアーガイド」、ガラス工場見学ツアーの導入）は
  **既存のツアーガイド型セットであり、このセッションでの変更もない**（`git diff` で無変更を確認）。
  実際に欠けているのは**広告型のみ**。`p4type` の現在のラインナップは
  館内放送（announcement）× 3・留守番電話（telephone message）× 1・ツアーガイド × 1・
  会議抜粋（meeting excerpt）× 1 の6セットで、広告（ラジオCM等）だけが無い。
  **→ 2026-08-15 に広告型（`u-p4t-04`）を追加し、この穴は埋まった。**
- `validate.mjs` が NOTE として出している既知のズレ（対応不要と判断済み）:
  `doc`（Part6/7）ユニット146件はすべて全体訳（`ja`）を持たない設計だが、CLAUDE.md の
  「`ja`（訳）」という説明はユニット全体に `ja` がある前提を示唆しうる。実データは一貫しており、
  Part 6/7 は設問単位の `ja`（各設問に訳が付く設計）のため実害はない。CLAUDE.md 修正までは
  ここでは行っていない（次に触る人向けの申し送り）。

## 検証済みの事項（初版から継続）

- 実ブラウザの通しテスト **27項目すべてパス**（旧24項目 + Part 1 描写方式3項目）
- GitHub Pages のサブパス配信（`/<repo>/`）を再現して動作一致を確認。絶対パス参照 0件
- 設問番号は模試5回すべて 1〜200 の連番。欠番・重複なし
- 正解位置はパート単位・論点単位で均等（数値・日付など順序に意味がある選択肢は対象外）

### 2026-08-15 に実行した検証

| 項目 | 方法 | 結果 |
|---|---|---|
| データ整合性 | `node tools/validate.mjs` | エラー0件・警告0件（1,638問・1,017ユニット） |
| 正解位置 | 同上の分布出力 | `p4type` A=4 B=4 C=4 D=4 / `p1verb` A=12 B=13 C=12 D=12 |
| 実ブラウザ通しテスト | `node tools/smoke.mjs`（CSS 修正後） | 27項目すべて PASS・console エラー0件 |
| Part 1 線画の幾何 | `node tools/scenecheck.mjs warehouse` | エラー0件・警告0件 |
| `warehouse-b` が読めるか | 544px・340px の light/dark で実描画して目視 | 「人がいない」「最下段が空」「箱が台に載っている」いずれも断定できる |
| 有人版との什器一致 | `warehouseFixtures()` の共有と両 PNG の照合 | 差は作業員1人だけ。什器は1単位も動いていない |
| 携帯幅の「次へ」 | 390×780 / 414×896 / 600×900 / 719×900 / 760×900 で `elementFromPoint` と実クリック | 全幅でボタン自身を返し、クリックで `#/exam` に留まり次の設問へ進む |
| 広い幅での退行 | 1280×860 で `getComputedStyle(.examnav).bottom` | `8px`（`.5rem`）のまま。見た目も修正前と同一 |
| 広告スクリプトの語数 | 実データから集計 | 153語（本番の3問セットは100〜150語。電話番号の読み上げ分を含む） |

## 開発の経緯

### 初版（3エージェント体制）
- **レビュー＋修正エージェント**：851問を1問ずつ監査して45件を検出（致命的5・要修正28・軽微12）。
- **ユーザーエージェント**（40歳英語教師役）：通し体験で25件の改善提案。
- **実装エージェント**：予想模試 Vol.3〜5 を作成。

### 2026-08-14
383問の新規ドリルを作成し、6本の監査で108件（致命的20件）を検出・修正。Part 1 の方式変更、
SRS バグ修正、品質管理ツールの `tools/` への移設・機能強化を実施。

### 2026-08-15
未決事項 2（広告型）・6（未使用場面）を消化して4問追加。**メインが体制を守らず自分で書いてしまい**、
その4問からレビュー役が要修正4件・軽微14件を検出した。オブザーバーの通し操作で
携帯幅の「次へ」が押せない不具合も見つかり、修正した。**要修正のうち2件は、メインが実装役に出した
指示そのものが誤っていたもの**（存在しない型の交替を解説に書かせた／設問タグの取り違え）。
**書き手が誰であれ、別の目を通さずに確定させてはいけない。**

## 設計上の判断メモ

- **過去問は使えない。** ETS が公開しておらず著作権保護されている。形式仕様のみ参照し全問を書き下ろした。
- Part 1 は模試では SVG 線画（28場面）、ドリルでは英語描写テキスト。線画のままでは
  `is wearing`／`is putting on` のような動作の識別を鍛えられないという構造的な限界があったため、
  ドリル側のみ本人の判断で方式変更した（詳細は「このセッションでの変更点 2」）。
- 合成音声には本番の連結・脱落・弱形がない。**リスニングは補助教材**と位置づけ、
  Part 3/4 はスクリプトと全体訳を精読・音読の素材として使うのが現実的。
- 端末間同期はサーバーがないため不可能。設定画面の「読み込んで合流」で代替している。
- 換算スコアは公式問題集の換算表の中央値をもとにした近似（`assets/js/score.js`）。±25点の幅で見る。
- **level は表示専用**（機械照合済み。上表参照）。SRS・出題順・絞り込み・推定スコアの設計判断には
  一切使っていない。難度別の絞り込み機能を将来追加するなら、この前提を崩さないよう設問単位の
  `question.level` 新設とセットで設計し直す必要がある。

## 未決事項（次に着手するなら、まず本人に方針を確認すること）

1. **演習としての単調さ**（今回の監査で判明） — 欠陥ではないが型が偏っている。
   `vform` は19問中7問が「時・条件の副詞節中の現在形」／`subj` は12問中7問が
   「X-s / X / X-ed / will X」の完全同型／`ptcp` は同型問題が複数／`conjprep` は10問中8問が
   「後ろが節か句か」の一本槍／`inv` は8問中6問が助動詞選択で、語順そのものを問う型が1問もない。
2. **既存 level の付け直しは未実施。** 設問単位で正確にやるには `question.level` の新設という
   スキーマ変更が要る（CLAUDE.md にも明記済み）。
3. **模試 Vol.6 以降は未着手。** コストの見積もりは「200問書く」ではなく
   「200問書いて、別の目で200問監査する」であること。実績: 383問に対し108件の欠陥
   （うち致命的20件）を検出・修正しており、監査を省略すると欠陥が残ったまま公開されるリスクが高い。
   **2026-08-15 にはたった4問でも要修正4件が出た**（「2026-08-15 の変更点 5」参照）。
4. **`p7ins`（位置選択）と `graphic`（図表）は正解位置を平準化できない。**
   選択肢が `[1]`〜`[4]` や金額・時刻など順序を持つため、`balance2.mjs` が構造的に除外する。
   これは欠陥ではなく正しい挙動（CLAUDE.md に明記済み）。
5. **未使用場面 `meeting-room` と `kitchen` には線画の欠陥がある。**（2026-08-15 に 544px light/dark で目視確認）
   どちらもどの設問からも参照されていないので**実害はゼロ**だが、使うなら描き直しが要る。
   - `kitchen`: **奥の棚の最下段の線が調理人の頭部を水平に貫通している。** コンロ（`propMachine`）が
     調理器具に見えず鍋もない。脚がカウンターの手前に出ていて位置関係が壊れている
   - `meeting-room`: 立っている人物の足がテーブル天面に接し、**「天面に立っている」ように見える**
     （床線 y=176 と天面 y=182 が近すぎる）。着席3人も胴が天板で切れて頭だけ浮いて見える
6. **Part 1 の線画について、次に触るときの申し送り**（`design/part1/SPEC.md` と併読）:
   - **座位＋手前什器の組み合わせに注意。** 座位は立位より頭頂が26単位低く、天面 y=172〜182 の
     什器と干渉して腿が隠れ、立位と区別できなくなる。`laboratory-seated` は人物を
     `frontFigure` 層（手前の面より後に描く層）へ移して解決した
   - **仕様の数値より「544px で読めるか」を優先してよい。** `laboratory-seated` の腿は
     見本の約9°では座面の線と融合して「箱に腰かけた胴」に見えたため、約20°まで倒してある
   - **未使用の場面は2つになった**（`meeting-room` / `kitchen`。どちらも上記5の欠陥あり）。
     `warehouse-b` は 2026-08-15 に `p1v-09` で使用開始した
   - **`warehouse` 系の細部**（オブザーバーが実描画で指摘。実害はないが、次に触るなら）:
     棚の支柱が床線に届かず**棚が宙に浮いて見える**（支柱 y=60〜210 / 床線 y=250）。
     各段の筋交いが斜線1本なので拡大すると「引っかき傷」に見える。
     パレットが板1枚＋脚3本で、低い台には見えるが**パレットとは断定しにくい**
     （模試側で「パレットか否か」を問うなら桟の補強が要る）
   - **4場面は `scenecheck.mjs` の検査対象外**（`garden` / `waterfront` / `parking-lot` / `beach`）。
     `data-part` 属性を持たない旧実装のままなので、12項目のうち手・腕・姿勢・箱に関する9項目が
     判定されず、通っているのは色・線幅・viewBox の3項目だけ。**この4場面は目視しか頼りがない**
     （`node tools/scenecheck.mjs` が WARN として毎回報告する。エラー0でも「全部検査できた」ではない）。
     いずれも使用中（`garden` 1問 / `waterfront` 3問 / `parking-lot` 2問 / `beach` 1問）
   - 線画を1つ変えると、その場面を使う**すべての設問に波及する**。必ず波及先を洗い出して再検証する
7. **Part 1 ドリルの `ja` が正解1文の訳しかない。** 誤答3文の英語も教材になるので4文とも訳がほしい。
   ただし既存49問すべてがこの設計なので、変えるなら一括で。
8. **Part 4 のスクリプトに短縮形が1つもない**（`We do not` / `You will not`）。`listening2.js` の
   8セット全部が同様。900帯が落とすのはまさに弱形・短縮だが、合成音声の明瞭さを優先する方針なら現状でよい。
   方針の確認が要る。
