# TOEIC L&R 900 対策アプリ

## これは何か
親戚（40歳・高校英語教師・800点超保持）が 900 点台へ抜けるための演習アプリ。
ビルド不要の静的サイト。GitHub Pages にそのまま置ける。

## 作業体制（利用者の指示。以後も継続して適用する）
対話しているメインの Claude は**進捗管理と品質管理のみ**を担当し、実装作業そのものは行わない。
実作業は Agent ツールで次の3役を立てて分担させる。

| 役 | モデル | 担当 |
|---|---|---|
| 実装役 | Sonnet 5 | 問題データの作成・修正、コードの実装 |
| レビュー役 | Opus 5 | 実装役の成果物を1問ずつ監査する。英語の正誤、狙った論点が成立しているか、誤答が本当に誤りか、解説との整合 |
| オブザーバー | Opus 5 | 40歳・高校英語教師・800点台の利用者目線で、実際に画面を通して使い勝手と学習効果を確認する |

メインの Claude の職務は、3役への指示・成果の突き合わせ・`tools/` の検査実行・
PROGRESS.md と CLAUDE.md の更新。

**複数の役を並行で走らせるときは、担当ファイルを重ねないこと。**指示に「触ってよいファイル」を明記し、
それ以外に問題を見つけたら**直さずに報告**させる。
**ただし `assets/` を分けても `tools/` は全役が実行するので衝突する。**2026-09-12、実装役が
`validate.mjs` に検査を書いている途中の状態を別の役が実行し、「`ReferenceError` で起動不能。
エラー0・警告0の検査が誰も回せない」と報告してきた（完成後は正常）。
**ツールを触る役がいる間は、他の役に「ツールの異常は報告だけして作業を止めるな」と伝えるか、
ツールの変更を先に終わらせてから他を走らせること。**

**重複の検出は「全ファイルが静止した状態」でしか意味を持たない。**2026-09-12、監査中の34分間に
`grammar3.js` が3回・`vol5-r1.js` が2回・`vol2-r1.js` と `grammar.js` が各1回変わり、
**同じ設問を複数の役が短時間に上書きして、誰も自分の変更の最終状態を照合できていなかった。**
実際、ある設問は10分間に3回変わり、その途中で別巻の設問と3/4一致＋正解一致になり、また離れた。
**最後の手当ては1体に直列でやらせ、締め切ってからメインがまとめて1回照合すること。**
**`git stash` / `git checkout` / `git reset` は絶対に使わせないこと。**作業ツリーには他の役の未コミット変更が
同時に載っているため、他の役の成果を巻き込んで消す（2026-08-17 に実際に使われた。復旧を確認したが危険）。

**報告文は当てにならない。差分で裏を取ること（2026-08-28）。**
実装役・レビュー役の完了報告と実データが食い違った例が、1日で**3件**出た。
いずれも**別の役かメインが `git diff` / `git show HEAD:` と機械照合して**発見している。

| 報告 | 実際 |
|---|---|
| 「正解を短縮したのは2問」 | 3問（1問は報告に無かった） |
| 「残る相互ヒントはすべて既存」 | 2件は本人が足した語が原因だった |
| 「この設問を HEAD の文言に戻した」 | 戻っておらず、正解の文言まで変わっていた |

**悪意ではなく、長い作業の途中で自分が何をしたか見失うために起きる。**
だから**報告を読んで納得するのではなく、HEAD と突き合わせて数えること。**
`tools/` のスクリプトで「変えてはいけないものが変わっていないか」を機械検査できる。

**ただし「差分に何も無い」＝「その役が何もしていない」ではない（2026-09-12）。**
実装役の完了報告を受けて `grep` したところファイルが1バイトも変わっておらず、
**架空の報告と判断して別の役を再投入した。**ところが実際には、最初の役は**出力上限で中断していただけ**で、
そのあと再開して書き込み、**2体が同じファイルを同時に編集する事態**になった
（後の役が衝突に気づいて統合したので事故にはならなかった）。
**報告の直後に差分が無くても、その役が完全に終了したと確認できるまで再投入しないこと。**
**再投入するなら、先の役を止めてからにすること。**

**共有のスクラッチパッドでも衝突する（2026-09-12）。**
実装役の1体が、メインの計測スクリプトと**同じ名前のファイルを同じディレクトリに書いて上書きした。**
メインが結果を読み違えかけた。**メイン専用のディレクトリを分けること。**

**自己検算は当てにならない。** 過去の実績では新規作問の約1/4に欠陥が出た（383問に対し108件、
うち致命的20件）。**書き下ろしは必ず別の目の監査を通してから確定させる。**

**レビュー役が入れた修正も「誰も見ていない文」である。** 2026-08-17、致命的欠陥を直した当人が書いた解説に
**直した理由と逆の記述**が入っていた例が複数出た。ただし無限に検算を重ねられないので、止め時を決めてある:

> **修正が stem・選択肢に及んだらもう1巡かける。解説文だけの修正なら通す。**
> **例外（2026-09-12 に追加）：その解説修正が「ヘッジを断定に書き換えた」ものなら、解説のみでも1巡かける。**

ヘッジは第二の正解の**徴候であって原因ではない**（2026-08-28 に確立）。
**徴候を消す修正は、穴を残したまま「解説のみ」の札を貼る。この札が検算の網から設問を落とす。**
1日で2件出た——`p6c3-01-1`（誤答 `the same` が第二の正解。**同じ文書の別設問が同じ照応用法を使っていた**）と
`subj-36`（誤答 `replaces` は `It is high time + 現在形` としてコーパスに実在）。
どちらも「解説のみ」に分類されて最終巡の対象から外れかけていた。

**指示を出す側（メイン）の判断ミスも欠陥になる。** 過去に、メインが指定した型が Part 5 の4択と
原理的に相性が悪く2巡失敗した例、メインが渡した level 判定基準そのものが自己矛盾していた例がある。
**2026-09-12 には、メインが渡した誤答のテンプレートが4問同時に第二の正解を作った**
（「`have V-en` は数の不一致」と書いたが、要求提案の that 節では HAVE の原形が `have` なので**完了仮定法として正文**）。
**メインが型を文字列で渡すと、実装役はそれをそのまま写す。**4問が同じ骨格になり、
**文字列の一致ではないので機械照合には掛からなかった**（実装役の1人が自分で気づいて報告した）。
**CLAUDE.md の「構造は数値で指定して渡す」は、メインが型を渡すときにも当てはまる。**
**レビュー役が「型の設計自体を変える必要がある」と報告してきたら、それはメインの担当。**

## 収録内容の原則
- **TOEIC® の過去問・公式サンプルは一切使わない。** ETS 著作物であり公開・再配布されていない。
- 収録しているのは、公開されている**出題形式の仕様のみ**に合わせた完全オリジナルの書き下ろし。
- 形式仕様（本番と同じ）: Part 1:6 / 2:25 / 3:39 / 4:30 / 5:30 / 6:16 / 7:54 = 200問。
- リスニング音声は Web Speech API による合成。音声ファイルは持たない。

## 構成
```
index.html
assets/css/app.css        デザイン（紙/藍墨/朱の3色、light+dark）
assets/js/                app.js(ルータ) quiz.js(出題エンジン) store.js(localStorage/SRS)
                          analytics.js score.js audio.js charts.js render.js ui.js runtime.js
assets/js/views/          home drills mocks review analytics settings result exam
assets/data/topics.js     論点マスタ（37論点）
assets/data/scenes.js     Part 1 の SVG 場面（30種。模試30問とドリル9問が使う）。部品系は design/part1/SPEC.md に従う
design/part1/             Part 1 線画の設計仕様と見本 SVG。線画を触る前に SPEC.md を必ず読む
assets/data/registry.js   目録と遅延読み込み
assets/data/drills/*.js   論点別ドリル（23ファイル）
assets/data/mocks/volN*.js 予想模試（volN.js が集約、volN-xx.js が中身）
tools/                    品質管理ツール（下記）。依存は tools/package.json、node_modules は gitignore
```

## 動かす
```
python3 -m http.server 8777     # file:// では ES モジュールが動かない
```

## 品質管理ツール（`tools/`）
- `tools/validate.mjs` — 整合性チェック（パート別問題数・設問番号連番・選択肢数・`answer` 範囲・論点ID・
  場面ID・Part 6 空所数・Part 7 `insertAt`/`sentence`・id 重複、`why` の隣接参照〈`同上`等〉、
  選択肢の大小文字と空所位置の整合、`why[answer]` のみ「正解」始まりか、選択肢の重複、
  Part 1 の `scene`・`desc` 排他、**正解位置の規則性A〜F**、**選択肢の形から正解が漏れていないか**——
  語数（検査G）・複合 and/or と先頭語（検査H）・**受動態を含む本数 k 別の正解率（検査I。2026-09-02 追加）**・
  **述語になれる形の本数 k 別の正解率（検査J。定形／非定形の軸。2026-09-12 追加）**・
  **否定語を含む本数 k 別の正解率（検査K。2026-09-12 追加）**など）。
  `--extra <ファイル…>` で registry 未登録のファイルも検査できる。
  **検査を足すと、HEAD の状態でも警告が増えることがある**（検査Jで2件→3件）。
  **合格線は「警告0」ではなく「着手前から増えていないこと」。**
- `tools/balance2.mjs` — 正解位置の codemod。`--by part`（件数の平準化。既定、模試向け）／
  `--by topic`（論点単位、ドリル向け。ほぼ全部 Part 5 のドリルはパート単位では偏りが解消しないため）／
  **`--by drift`（前半・後半のドリフト是正。件数を動かさず対で交換する。2026-08-25 追加。複数巻を一度に渡すこと）**。
  **配列形式の `ja`（Part 1 の4選択肢分の訳）にも対応済み**——選択肢と `why` を入れ替えるとき `ja` も
  同時に並べ替え、各要素先頭の `(A)`〜`(D)` を位置に合わせて振り直す。要素数が合わない・記号が
  位置とずれている設問は安全側で入れ替え対象から除外する（2026-08-17 まで配列 `ja` は
  「除外もされず書き換えもされない」状態で、**無言でデータが壊れる穴**だった）
- `tools/setleak.mjs` — **セット内・文書内の相互ヒント検出（2026-08-28 追加）。**
  「ある設問の**正解だけが持つ内容語**が、同じセット／文書の別設問の stem・選択肢に出ていないか」を
  一語ずつ照合し、**疑わしい順に並べる**（漏れ先が別設問の正解 > stem > 誤答／固有名詞・数値は加点／
  本文に頻出する語は減点）。`set`（Part 3・4）と `doc`（Part 6・7）の両方が対象。
  **CLAUDE.md は長らく「この型は機械照合では捕まらない／読んで気づくしかない」と書いていたが、
  「正解だけが持つ語」に限定すれば機械で拾えることが分かった**（既知の致命的3件が22件中 #1・#2・#15 に出る）。
  **ただし誤検知が大半（全体で約400件）。合否判定には使えない一次ふるいであり、
  上から順に人が読むための道具。**この精度のため `validate.mjs` には入れていない
  （入れると全巻で警告が数百件になり「エラー0・警告0」の運用が壊れる）。
- `tools/titleleak.mjs` — **文書の見出し（`title` / `head`）から正解が漏れていないか（2026-09-12 追加）。**
  「ある設問の**正解だけが持つ内容語**が、その文書の `title` / `head` に出ていないか」を照合する。
  `setleak.mjs` も `dupcheck.mjs` も設問どうししか見ず、検査G〜Jは1設問の中しか見ないため、
  **見出しはどの計器の視野にも入っていなかった。**`node tools/titleleak.mjs <ルート>` で別ツリーとも比べられる。
  **誤検知が大半の一次ふるい**（HEAD で47件。「To: All 〜 staff」型が多い）。この精度のため
  `validate.mjs` には入れていない。
- `tools/dupcheck.mjs` — **設問どうしの重複を3軸で測る（2026-09-12 追加）。**
  ①選択肢集合の重複（完全一致／4本中3本一致かつ正解も同一）②**巻をまたぐ同一スロットの衝突**
  （模試で同じ `no` の設問が選択肢を共有し正解も同じ＝前の巻の記憶で解ける）
  ③内容語の選択肢（平均4語以上）の巻またぎ重複。
  `validate.mjs` の検査G〜J が「1つの設問の中で正解が浮いていないか」を見るのに対し、
  これは**設問と設問のあいだ**を見る。`node tools/dupcheck.mjs <ルート>` で別ツリー（HEAD 等）とも比べられる。
  **並行して複数の役が直しているあいだは意味を持たない。**全員が止まってから1回かけること
- `tools/smoke.mjs` — Playwright による実ブラウザ通しテスト（**37項目**）。ポートが他プロセスに占有されて
  いれば自動で隣にずれる
- `tools/shots.mjs` — 主要画面のスクリーンショット取得
- `tools/scenecheck.mjs` — Part 1 線画の幾何検査（12項目）。Chromium で実描画し `getCTM()` で
  transform を解決して座標を測る。`--ref` で `design/part1/*.svg` の見本を検査
- **選択肢の漏れは複数の計器で同時に測ること（2026-08-28。2026-09-12 に軸が増えた）。**
  軸を1本ずつ名指しして直すと、**直す作業が別の軸を壊す。**今回3巡かけても、各巡が新しい指紋を1つずつ作った。
  ①順位分布（語数）②揃えすぎ（4本の語数の幅）③水増し（フィラー・-ly 副詞・語数差）
  ④形の漏れ（複合・先頭語）⑤セット内の相互ヒント
  ⑥受動態（検査I）⑦定形／非定形（検査J）⑧**否定語（検査K）**
  ⑨**時制・他動性**⑩**論理的に対立する2本／最小対**⑪**文書の見出し（`titleleak.mjs`）**。
  **⑨〜⑪はどれも 2026-09-12 に、既存の計器を全部通過した設問から見つかった。**
  **書いてから測るまで分からないという性質は、巡を重ねても変わらない。毎回全軸を回す。**
- 作業前後に `node tools/validate.mjs` を実行し、エラー0・警告0を確認すること
- **`node --check` は `/* … */` の中の `*/` を素通りする。**コメントに `rev-wrong-*/rev-blank-*` のような
  表記を書くと**そこでコメントが閉じてコード扱いになり、アプリ全体が起動不能になる**（2026-08-25 に実際に発生）。
  捕まえるには `import()` して `SyntaxError` を見るしかない（`ReferenceError` は `window` 等が無いだけなので正常）。
  `tools/smoke.mjs` の 01・02 が結果的に落ちるが、Playwright を回すまで分からない。
  **`identifier-*` のような表記をブロックコメントに書かないこと。**
- **線画を変えたら `tools/scenecheck.mjs` に加えて、必ず 544px・light / dark で
  レンダリングして目視すること。**幾何検査を通っても「読み取れる」とは限らない
  （輪郭線が無く塗りだけだと `--card-2` と背景の差がわずかで、存在自体が読めない事故が実際に起きた）
- **その目視も、書いた本人では当てにならない。必ず別の役が独立にレンダリングして見ること。**
  2026-08-17、実装役が幾何検査12項目を通し「544px light/dark で全項目 読み取れる」と報告した2場面を
  レビュー役が独立に描画したところ、**新たに致命的3件**が出た（新造したコンロが券売機に見えていた、
  鍋がバーナーに乗っていなかった）。**描いた本人には意図が見えてしまう。**
- **什器の天面 y を人物の股関節 y にぴったり揃えないこと。**`trapezoid` は root の `stroke` を継承して
  胴の下辺にも線が出るため、人物が「天板の上に置かれた台形」に見える。6単位ほど上げると解消する

## 問題データの書式
各ファイルは `export const UNITS = [...]`（模試の分割ファイルは `L1` `R2` などを export し `volN.js` で集約）。

ユニット: `{ id, part, kind, topics:[], level:1-5, questions:[...] }`
- `kind`: `p1`(写真) / `p2`(応答) / `set`(Part3,4) / `single`(Part5) / `doc`(Part6,7)
- `set` は `script:[{role,text}]`、`doc` は `docs:[...]` を持つ
- `p1` は `scene`（SVG場面ID）と `desc`（英語の場面描写テキスト）のどちらか一方を持つ。
  **模試の Part 1（30問）は `scene` のまま**（本番の見た目・時間感覚を再現するため変更しない）。
  **個別論点ドリルの Part 1 は原則 `desc` 方式**（`part1.js` の40問。数秒表示→消去→「もう一度見る」で再表示可）。
  線画では `is wearing` と `is putting on` のような動作の識別を作問できないための変更（`quiz.js` の `renderP1()` が両方式を分岐して描画する後方互換の実装）。
  **ただし線画でしか問えない論点はドリルでも `scene` 方式を使う**（`listening.js` の9問）。
  `is being + 過去分詞` の可否は「動作主が写っているか」で決まるため、絵がないと成立しない。
  `warehouse`（有人）と `warehouse-b`（無人）は什器座標を共有しており、同じ倉庫で人の有無だけを
  変えた対比が作れる（`p1v-01` と `p1v-09`）
- **図表ユニットの非図表設問に `t:` を明示すること。**`set()` / `talk()` ヘルパーはユニット既定の
  `topics` を各設問に継承させるので、**`t:` を書かないと図表を使わない設問まで `['graphic']` を黙って持つ。**
  分析画面の論点集計が実数の数倍にずれる（2026-08-25 に全巻で多数見つかった）
- 模試の設問には通し番号 `no:1-200` が必須
- Part 6 の空所は本文に `{{1}}`〜`{{4}}`（ユニット内の何問目か）
- Part 7 の文挿入位置は本文に `[[1]]`〜`[[4]]`、設問に `insertAt` と `sentence`
- 話者ロール: `M-Am W-Am M-Br W-Br M-Au W-Au M-Cn W-Cn NARR`

解説は日本語。`exp`（なぜそうなるか）、`why`（選択肢ごとの検討・選択肢と同数）、`ja`（訳）、`vocab`（語注）。

## 難度 level の定義
`level` は **表示専用**（`quiz.js` のヘッダ、`drills.js`・`result.js` の一覧に出るだけ）。
SRS の間隔・出題順・絞り込み・推定スコアには一切関与しない。

新規に問題を作るときはこの目盛りに従うこと（level 1・2 は作らない。このアプリは 800→900 帯専用）:
- **level 3** = 800〜860帯。文全体・複数文の統合が要る／基本コロケーション
- **level 4** = 860〜900帯。900帯特有の語彙・紛らわしい語・間接応答など「知らなければ解けない」知識
- **level 5** = 900+。構造的トラップ／複数情報源の統合

### 判定のしかた（2026-08-17 に確立。これがないと人によって軸がぶれる）

> **唯一の軸：「800点台の高校英語教師が、この1問を落とすとしたら、何を知らなかったからか」を
> 言葉にする。その知識がどの帯のものかが level。**
> **誤答が文法的に成立するか／しないかは判定材料にしない**（それは難度ではなく作問の型）。

- **lv3** — 決め手が**文法書・単語帳の標準項目**。基本コロケーション、教科書レベルの語法、
  高頻度の形容詞＋前置詞。文法なら**空所の近傍だけで決まる単一規則**。
  誤答3つが同方向に偏っていて**一つの規則で一括消去できる**問題も lv3。
- **lv4（既定値。迷ったらここ）** — **知らなければ解けないが覚えれば解ける**もの。
  中頻度のビジネス語、紛らわしい派生語の対、自明でないコロケーション、Part 2 の間接応答。
  文法なら**空所の外の情報を1つ拾えば決まる**もの。
- **lv5** — **(a)** 語彙の帯が 900+（法務・財務の書き言葉）、または
  **(b) 構造的トラップ**＝**誤答の少なくとも1つが単独では正しい英語**で、
  **離れた位置**の構造・論理でのみ排除される。
  **(b) の自己点検：「この誤答は、別の文に置けば正しい英語か？」が No なら lv5 ではない。**
  トリガーが空所を直接支配する隣接語なら（要求動詞の that 節など）、それは lv3 か lv4。

**閉じ方を①構造に寄せると、設問はやさしくなる。直したら level を必ず付け直すこと**（2026-09-12）。
意味の推論で閉じるのをやめて構造で閉じ直すと、**誤答が「一つの規則で一括消去できる」形に寄る**——それは lv3。
2026-09-12 の是正では**4問中3問が lv5 のまま**だった（`p6c3-05-2r` は誤答3本が
`Every`（単数専用）`Each`（単数専用）`Much`（不可算専用）で、限定詞と名詞のクラス不一致の一発で全部落ちる）。
**同じ設計の設問が 3/4/5 に割れていないかも確認すること**（`not` 先行型の要求提案が実際に割れた）。

**`level` はユニットにしか無い。**`quiz.js` は `unit.level` しか参照せず、`question.level` はデータ全体に0件。
**Part 6/7 の `doc` ユニットは1問だけ動かせない**（同じ文書の他の設問も一緒に動く）。
設問単位で付け直したいなら `question.level` の新設が先。

**分布のノルマは置かない。**素材が基礎寄りのファイルで lv5 を作るのは目盛りの捏造。
中核ファイル（`grammar.js` `grammar2.js` `vocab.js` `vocab2.js`）は教科書1周分なので lv5 が1割前後、
増補ファイル（`grammar3`〜`6` `vocab3`〜`5`）は900帯の難所に寄せているので高め——という
**役割の違いがあるだけで、目標値ではない**。実測は増補ファイルでも 13〜34% と幅がある。
**分布を見て level を動かさないこと。**個々の設問を目盛りに当てた結果として分布が出る、という順序を守る。

### 付け直しの状況
2026-08-17 に、**1ユニット＝1設問のもの（`single` / `p1` / `p2`）435ユニットのうち、
初版で書かれた175ユニットを付け直した**（`grammar.js` `grammar2.js` `vocab.js` `vocab2.js` `listening.js`）。
2026-08-14 以降のファイルは新定義準拠。`assets/data/` 全体に level 1・2 は残っていない。

**`set`（Part 3/4）と `doc`（Part 6/7）は未実施。**1ユニットに複数設問が乗り `level` はユニット単位でしか
持てないため、設問単位で正確に付け直すには `question.level` の新設が要る。模試も方針判断により未実施。

## 品質の決まり
- 正解位置は A/B/C/D をほぼ均等にする。偏ったら平準化ツールで直す（選択肢と `why` を対で入れ替える）。
- 数値・日付など順序に意味がある選択肢は並べ替えない。
- 解説文中で `(A)` のように記号を参照するときは、入れ替えで壊れないか確認する。
- 図表問題は、図表と音声スクリプトの数値が矛盾していないか必ず突き合わせる。
- **英式・米式の綴りは、データ全体では混在していてよい**（TOEIC は英・豪・加英語を含む試験で、
  実際 `centre` 39 / `center` 13、`programme` 39 / `program` 57 のように文書ごとに選ばれている）。
  **揃えるべきなのは1つの文書の中だけ。**2026-08-17 に全1,017ユニットを実測したところ
  **同一ユニット内の揺れは0件**だった。**「全体で混在している」を欠陥と数えないこと。**
  - **綴りを機械照合するときは、英米で共通の形を対から外すこと。**
    `enrolled` `enrolling`（`enrol`/`enroll` の共通形）、`cancel` `cancels`（差が出るのは
    `cancelled`/`canceled` だけ）を英式側に入れると、**米式で統一された文書が誤検知される**。
    実際にこれで「1件混在」という誤った結果を出し、実装役の指摘で判明した。
    **検査ツールが誤りを生むこともある。ヒットが1件だけのときはまず照合条件を疑う。**
- `p7ins`（位置選択）と `graphic`（図表）は選択肢が `[1]`〜`[4]` や金額・時刻など順序を持つため、
  `balance2.mjs` が正解位置の入れ替え対象から構造的に除外する。これは正しい挙動。

## 誤答の作り方（最重要。2026-08-17 の一斉点検で確立した）

**この型の欠陥が最も多く、最も見つけにくい。**抽出した62問のうち**16問（26%）が「第二の正解あり」**だった。

- **誤答は「不可能」でなければならない。「頻度が低い」「不自然」で切ってはいけない。**
  別の文脈を想像すれば正しくなる誤答は使えない。
- **解説は「自然だ」「文脈に合わない」で誤答を切らないこと。**必ず
  **構造・共起制限・語義のどれかを名指しする**（「この語は自動詞なので直接目的語を取れない」など）。
  次の書き方はすべて「第二の正解がある」の徴候:
  - ヘッジ（「〜になり**にくい**」）／譲歩（「口語では**見るが**」）／
    「**文意が変わる**」（＝英語としては正しいと認めている）／語義を訳しただけで排除理由が無い
- **誤答を作ったら主要辞書の全語義を当たること**（LDOCE / Oxford / Cambridge / Merriam-Webster /
  Collins。句動詞・イディオムは Farlex も）。**一部の語義だけ見て済ませない。**
  - **Merriam-Webster・Collins・Cambridge・OALD は Cloudflare で 403 になることが多い。**
    LDOCE と Wiktionary は比較的通る。TheFreeDictionary は WebFetch では 403 だが
    **curl ＋ Safari の User-Agent なら通る**（AHD・Collins 12th・Random House をここから引ける）。
  - **「引けなかった」と「存在しない」を混同しないこと。**2026-08-17 に、ある役が
    「Cambridge にこの文法ラベルは現行版に存在しない」と報告したが、**実際には存在しており、
    403 で取得できなかっただけ**だった。次の役が直接取得して判明した。
    **取得に失敗したら「取得できなかった」と書く。**「無い」と書くと、後の役がそれを事実として引き継ぐ。
  - **用例で当たる手段**：Google Books Ngrams の JSON API（`books.google.com/ngrams/json`）と
    英語版 Wikipedia の insource 正規表現検索（`en.wikipedia.org/w/api.php`）。
    WebSearch には回数上限があるので、**この2つを直接叩くほうが確実**。
  - **Ngrams の「0件」を「存在しない」と読まないこと。**低頻度帯では、
    **実在が確認できる表現でも空配列が返る**。2026-08-18 に `despite the series ending`
    （Wikipedia に46件の実例あり）で空配列が返ることを確認した。
    `before the end of` のような高頻度 4-gram は返るので、**API が壊れているのではなく帯の問題**。
    **0件を根拠にするときは、必ず別の手段（Wikipedia insource）で裏を取ること。**
    実際に「Ngrams で0件だから閉じている」と書かれた解説が、根拠が無効なまま残りかけた。
    Europe PMC は `of` `from` をストップワードとして落とすので**前置詞の比較には使えない**。
  - **実例が見つかったら「なぜこの文では起きないか」まで詰める。**`very few N V that` は実在するが、
    それは**動詞が that 補部を取る場合**（`very few scientists stated that …`）。
    当の設問の動詞がその型を持たないことまで確認して初めて閉じたと言える。
  - **ヒット件数を比べるのではなく、ヒットを1件ずつ開いて中身を読むこと**（2026-09-12。1日で3回、結論がひっくり返った）。
    - `is essential that the [N] will` は全9件中1件だけ `will` だったが、それは**スペイン語からの翻訳引用**だった。
      件数だけ見た役は「1:12 だから開いている」、中身を読んだ役は「閉じている」と、**逆の結論**を出した。
    - `insource:/either of the four/` は **`Neither of the four` の内部にも一致**して件数が水増しされる。
    - `both of the three/four` のヒット3件は**全部が偽陽性**（`fours finals` / `four-mile heats` /
      `three-compartment type`）。件数だけ見ていたら「`both` も2者に限られない」という**逆の結論**を出していた。
  - **Wikipedia の insource 正規表現検索は、literal を前置しないと API がエラーを返し、それが「0件」に見える。**
    実装役が `urge` を「0件＝閉じる」と報告した根拠がこれで、正しく測ると7件・真正4件だった。
    **0件が出たら、同じ条件で「出るはずのクエリ」を投げて、計測が生きていることを確かめること。**
  - **register（「口語に限る」）を排除根拠にしないこと。位置でも救えない。**
    `on account of` ＋ 定形節は英語版 Wiktionary が**接続詞として独立の品詞項**（colloquial ラベル）を立て、
    出版物からの引用が3件ある。**「文頭では使えない」も成り立たなかった。**
    確かめ方：**文頭に立てることが確実な同形の表現と、大文字で始まる比率を Ngrams で比べる。**
    `On account of I/he/it was` の 9.4〜11.8% に対し、前置が確実な `On account of the fact` が 11.7% で差が無い。
  実際に事故になった例: `put off` の無期限棚上げ／`take off` の「（番組・運行を）打ち切る」／
  `hold up` の「遅らせる」／`afford` の「提供する」／`creditable` の「信用できる」／
  `confused` の「not clear」／`delegate 人 to do`／`indicated`＝「必要とされる」。
- **ヘッジを断定に書き換えて済ませないこと（2026-08-28）。**ヘッジ（「明示されていない」「〜になりにくい」）は
  **第二の正解の徴候であって原因ではない。**書き換えると**徴候だけが消えて欠陥が見えなくなる。**
  実例：`vol1-l2a.js` No.40 の `why` が「担当は明示されていない」だったのを
  「二人目の男性の担当ではない」という断定に差し替えたが、音声の `Find out the exact figure while you're at it.` は
  **`while you're at it` が「すでに作業を抱えている相手」に向ける言い方**で、該当するのは設問の対象である
  二人目の男性だけ。**穴は残ったまま、徴候だけ消えていた。**
  **ヘッジを見つけたら、まず誤答が本当に閉じているかを確かめる。文章の直しは最後。**
- **「応答の形」だけで誤答を切る解説は、たいてい偽の規則である（2026-08-28）。**
  1日で**2つの偽の規則**が見つかり、どちらも複数ファイルに伝播していた。
  - 「**選択疑問に Yes は不可**」（7ファイル・14箇所。うち**3問が実際に第二の正解**だった）
  - 「**`Why don't we …?` に Because で理由を答えるのは誤り**」——**自己矛盾している。**
    同じ解説が「それに対する**障害を挙げる応答が正解**」と書いた直後にこれを書いており、
    `Because they start on different dates.` はまさに障害を挙げた応答。
    区別しているのは中身ではなく**接続詞の有無だけ**で、規則になっていない。
  **共通の構造**：応答の**中身**を見ずに、**冒頭の語（Yes / Because）**だけで切っている。
  > **応答の冒頭の語を排除根拠にしないこと。**切るなら
  > 「その内容が問われている命題と論理的に独立している」「時制が合わない」
  > 「指示語の先行詞が定まらない」のように、**中身を名指しする。**
  **偽の規則は1箇所では終わらない。**見つけたら `assets/data/` 全体を走査し、
  **`topics.js` に供給源が無いかを必ず確認すること**（1つ目は `pitfall` が供給源だった）。
  **ただし供給源が無いこともある。**数の規則（下記）は `topics.js` のどこにも無く、
  **各実装役が学校文法の常識として独立に持ち込んでいた。中央を直しても再発するので、作問のたびに確認するしかない。**
  **そして規則の片面だけ直すと、もう片面が残る。**初回監査は「裸の `none` は2者でも実在」を
  **致命的として捕まえながら**、裏面の「`neither` / `either` に数の上限は無い」へ展開しなかった。
  同じ設問の中でも、`these` を差し替えて**まったく同じ構文の隣の `those` を残していた。**
  **偽の規則を見つけたら、その規則の裏面と、同じ設問の他の選択肢にも同じ検査を掛けること。**
- **数の規則は `both` だけが本物**（2026-09-12 に全1,838問を走査し、AHD・Collins・Random House で確定）。

  | 規則 | 判定 | 根拠 |
  |---|---|---|
  | `both` = 2者 | **本物。使ってよい** | 辞書・コーパスとも裏が取れた |
  | `either` = 2者 | **偽** | AHD「reputable writers have often violated this rule」／RH が `either of the three children` を立項 |
  | `neither` = 2者 | **偽** | AHD は `none` を "preferred" とするだけ（required ではない）。`neither of the three` は実在 |
  | `none` = 3者以上 | **偽** | 辞書に数の下限の記述なし |

  **とくに `neither` を「2者だから」で切っている設問は、正解が `none` のとき意味まで同じになるので第二の正解。**
  該当する誤答は**構造で不可能な形**（主語位置に単独で立てない `every`、partitive の `of` を取れない `no` など）に差し替える。
- **選択疑問（`A or B?`）で `Yes` 始まりの誤答を使うときの規則（2026-08-28 に確立）。**
  **「選択疑問に Yes は不可」は誤り**（Cambridge *English Grammar Today*、Huddleston & Pullum (2002) とも
  統語的な禁止としていない）。**排除根拠に使ってはいけない。**
  この誤った規則は `topics.js` の `pitfall` を供給源として **7ファイル・14箇所**に伝播しており、
  そのうち **3問が実際に第二の正解**だった（2026-08-28 に是正）。
  - **`or` の後ろを独立した節にしないこと。**独立節にすると `Yes` が前半に掛かり、
    「はい、〈前半〉です」と読めて成立してしまう。
    不可：`Do you want me to reprint the labels, **or will you handle it?**`
    不可：`Is the workshop mandatory, **or can employees opt out?**`
    可　：`Should we visit the mill on Thursday, **or wait until Friday?**`（共通の `Should we` に掛かる動詞句）
  - **`Yes` の後ろの内容が、二択のどちらとも論理的に独立していること。**
    `Yes` を取り除いても一方を選んでいる文（`Yes, we'll record it next week.`）は第二の正解。
  - 排除根拠は **`Yes だけではどちらを選んだのか決まらない。〈語〉の反復で、〈内容〉も二択に触れていない。`**
    と肯定形で書く。
  - **wh 疑問文（`What…` `When…`）の「Yes は不可」は別物で、統語的に正しいので温存する。**
- **学習者が辞書を引いたときに解説と衝突してはいけない。**実在する語義を「存在しない」と書かない。
  実在を認めたうえで、**別の理由（目的語の型・共起・構造）で切る**。
  **辞書間で語義が割れることがある**（`creditable`＝「信用できる」は M-W が現役、Collins は廃用扱い）。
  **学習者が引きうる辞書に載っていれば、実在の語義として扱う。**
- **辞書に立項が無くても実在する型がある。辞書を引くだけでは足りない。**
  `integral to` しか辞書に無いが `integral in creating …` `integral for bone health` は編集された英文に広く実在し、
  `proficient with`（道具について）は M-W が明示している。**前置詞の選択問題では実際の用例まで当たること。**
- **発話時点に依存する語（`recent` `current` `present` `already` `still`）を誤答に置くときは、
  発話時点を文中で固定すること。**`recent` は「いつ読むか」で真偽が変わるので、
  `in 2018` のような絶対年を書いても閉じない（1999年に読めば真）。
  **`more than two decades ago` のような発話時点基準の表現**にして初めて閉じる。
  同じ理由で `figures` `estimates` のような「報告された数値」を head にすると
  `current figures`＝現時点で採用されている版、と読めてしまう。**確定した実数を指す head を選ぶ。**
- **副詞・形容詞の選択問題は「単文に手がかりがあるか」を必ず確認する。**
  `Volunteers ------- gave up two weekends to help …` は**肯定・否定どちらの文かを決める材料が
  文中に一つも無く**、`rarely` も `readily` も等しく成立していた。
  解説の「肯定的な内容なので readily」は**循環論法**。
  **模試でも3件出て、いずれも設問ごと差し替えて決着した**
  （`scarcely declined` → 限定用法の形容詞 `then`／`scarcely risen` → 後置詞 `notwithstanding`／
  `rarely gave up` → `thereby` + 分詞句）。**いずれも「位置で切る」型に作り替えている。**
- **論点の定義そのものが欠陥を生んでいることがある。**`topics.js` の `adv` は
  「品詞は同じなので**意味だけで決まる**」「全選択肢が副詞なら**文全体を読まないと解けない**」と書いており、
  **この「閉じられない型」をアプリ自身が推奨していた**（2026-08-17 に是正済み）。
  前セッションにも、`whatsoever` の問題が `adv` の旧定義にそのまま当てはまり
  「アプリが自分で『これは別論点だ』と説明している」状態だった例がある。
  **ある論点で欠陥が集中したら、個々の作問ではなく `topics.js` の定義を疑うこと。**
- **ELT 教材・学習文法書の記述は初級者向けの単純化であることが多く、根拠として弱い。**
  「so/neither 倒置の助動詞は先行節の時制に合わせる」は教材の記述だが実際は自由
  （`"I never went there." — "Neither have I."`）。**利用者は高校英語教師なので必ず見抜かれる。**
- **閉じられない型がある。** `regret doing` / `stop doing` のように**文脈非依存に常に成立する**誤答は、
  stem に情報を足しても閉じられない。その場合は誤答か設問ごと差し替える。
  - **`stop to do` / `stop doing` は Part 5 の4択にできない**（2026-08-17 に3巡かけて確定）。
    `stop doing` が要求するのは「その行為が進行中だった」ことだけで、stem で何を否定しても
    **別の手段でその行為が進行中だった読みが残る**。
  - **`remember doing` / `remember to do` も4択にできない**（2026-09-12 に確定）。
    **CLAUDE.md は長らく「`remember` は `verbal-18` のように時制矛盾で閉じられる」と書いていたが、
    その根拠にしていた `verbal-18` 自体が4本中2本開いていた。**
    原因は `stop` と同じ構造にある——**文末の裸の時の副詞句は、主節にも補部にも掛かれる。**
    `The facilities manager remembered ------- the emergency lighting **the previous week**` では
    `the previous week` が主節の `remembered` に掛かって「先週、忘れずに点検した」が成立し、
    誤答 `to check` が第二の正解になる。`to have checked` も `remember to have seen` が
    `remember having seen` と同水準で実在する（回想用法）。
    **副詞句の掛かり先を文の側から強制できない型は、`stop` も `remember` も同じく閉じられない。**
  - **肯定形の「要求・提案の that 節」も4択にできない**（2026-09-12 に4巡かけて確定）。
    **トリガー（`essential` / `require` / `urge`）を効かせる唯一の誤答は直説法**（`submits`）だが、
    **直説法は英式で第二の正解**になる。代替はどれも閉じない:
    `will V` は**動詞ごとに割れる**（`stipulate` `urge` `require` は開き、`essential` `crucial` `imperative` は閉じる）。
    **`have V-en` は数の不一致ではなく完了仮定法として正文**（HAVE の原形は `have`。
    `crucial that Britain **have** the support of France` / `requirement that the applicant **have been resident**`）。
    誤答を全部非定形にすると**「`to` も `-ing` も付いていないものを選ぶ」だけで当たる。**
    **閉じるのは `not` を空所の前に置く型だけ**（`subj-28`）——
    `keys not are stored` / `keys not have been stored` / `keys not will be stored` が
    **すべて語順で同時に落ち、トリガー語も効く。**誤答3本が一つの規則で落ちるので **level は 3**。
    **`have V-en` を「数の不一致」で切らないこと。**仮定法現在は**屈折を持たない原形の構文**で、
    **「数の一致」という概念が無い**（CGEL「the plain form of the verb… the only linguistic property by which it is defined」）。
    同じ理由で `were V-ing` も「数の不一致」ではない（仮定法の `were` は1・3人称単数の形そのもの）。
    **`were + 過去分詞` を誤答に使うときは主節を現在形にすること。**過去形の主節だと
    英式の covert mandative（`the owners demanded that any bones **were removed**`）が成立して開く。
  - **統語的に強制されない「強調」も4択に乗らない**（emphatic do。2026-08-16 に2巡失敗）。
    誤答を助動詞に揃えると意味の程度問題になって複数正解になり、原形を置くと強調の知識が要らなくなる。
  - **型がそもそも4択に乗るかは、作問を始める前に判断すること。**これは指示を出す側の責任。
  - **「肯定の副詞 vs 否定の副詞」を単文の意味で選ばせる型は閉じられない**（`readily` 対
    `rarely` / `hardly` / `scarcely`）。**3巡かけて失敗した。**否定副詞を入れた文もそれ自体は
    完全に正しい英語で、後半に何を足しても「めったにしないが、したときは〜」で吸収される。
    分詞構文で主語を固定しても駄目（**同一になるのは主語であって量化の領域ではない**）。
    副詞を問うなら**位置で切れる型**にする（例：前置詞の目的語の内側に置いて
    数量前置修飾ができる `well over` だけを残す）。
  - **同義語を辞書が相互に立項している対は作問不能。**`economic` の語義に ECONOMICAL があるので
    `economic` / `economical` の対は、正解が `economical` 側になる限りどんな文脈でも第二の正解になる。
    `sensible` にも `sensitive`（感度が高い）の語義があり、機器の反応性を問う限り閉じられない。
- **閉じ方は3種類ある。上2つを使い、3つ目は使わない。**
  1. **構造で切る（最良）**：目的語の有無、前置詞の要否、定形/非定形、限定詞の語順、
     間接疑問を取れるか、可算性、叙述用法の可否、空所の位置。辞書で裏が取れ、文脈に依存しない。
  2. **語彙で切る（可）**：**どんな文脈を補っても成立しない**組み合わせ
     （`collect the displeasure` は文脈を何も救わない）。単一動詞のコロケーション問題には
     構造的な取っ手が無いので、この型で閉じるしかない。**コーパスで0件であることを確かめること。**
  3. **意味の推論で切る（不可）**：「調査したときにしか findings は生じない」「代替日を設けないなら
     延期ではない」。**読み手が文外に主体や状況を補えば成立してしまう。**
     判別法：**`collect the expenses` は徴収先を補えば開くが、`collect the displeasure` は開かない。**
     「誰が」「どこで」「いつ」を補って復活するなら、それは 3 であって 2 ではない。
  - 実例：`assume/assert the feasibility` は `findings` を足しても閉じなかったが、
    目的語を `whether …` 節に替えると **`assert whether` / `assume whether` が構造的に不可**になって閉じた。
  - **語義の争いになったら、枠を変えて構造判定に変換する。**`distinctive` の語義（M-W の
    "serving to distinguish"）を争うのをやめ、枠を `be distinct from` に移して
    **「from 補語を取れるか」**にしたら閉じた。
- **差し替えた問題は、6回中5回、新しい欠陥を持っていた**（2026-08-17 の実績）。**必ず別の目を通すこと。**
  再発した原因は毎回このどれか:
  1. **辞書を引かずに文法規則を推定した**（「`stop` と動名詞の間に副詞句を挟めない」
     「`nor` は先行節が否定であることを要求する」「同格名詞句の前に副詞は置けない」——**全部偽**）
  2. **意味の推論で閉じようとした**（足した情報が受動態で動作主が無く、別の主体を補えば開いた）
  3. **辞書に無くても実在する型を見落とした**（`bring a vote`、`hit the right balance`、`versed with`）
  4. **既に「作問不能」と確定した対を再投入した**（`economic` / `economical`）
- **解説に「〜できない」「〜のみ」と断定を書くときは、必ず反例を探してから書く。**
- **解説に挙げる例文が、その解説の主張の反例に見えないか確認する。**
  「`permit` は目的語の後ろにしか to 不定詞を置けない」と説明しながら、実例に受動文
  `Visitors are not permitted to take photographs.`（permit の直後に目的語が無い）を挙げていた。
  **辞書を引く前に、解説の中だけで矛盾して見える。**
- **排除の根拠は「主要辞書のどこにも立項が無い」という書誌的な否定形ではなく、
  肯定形の構造規則で書く。**否定形は反例1つで崩れるうえ、学習者には検証できない。
- **狙った論点が効いているかは「その語を消しても同じ答えになるか」で確かめる。**

## 既存を雛形にするときの決まり（2026-08-18 に確立）

**「既存のものを真似る」という指示は、既存が正しいことを前提にしている。**その前提が崩れると、
**作業が増えるほど欠陥が広がる。**

**実際に起きたこと**：模試の `set` / `talk` ヘルパーが **`graphic: o.g`** と受け取るのに、
呼び出し側は **`graphic: {...}`** で渡していた。`o.g` は常に `undefined` になり、
**Vol.1〜3 の図表付き17ユニット・51問（うち26問は表が無いと解答不能）で表が描画されていなかった。**
`Vol.4` `Vol.5` だけが `o.graphic` に直されていたが、**1〜3 に遡って直されず、記録も残っていなかった。**
そして **Vol.6 の作成で、壊れた雛形をコピーした2ファイルが欠陥を再生産しかけた**
（1人だけ「コピーすると表が消える」と気づいて回避していた）。

- **雛形をコピーする前に、その雛形が実際に動いているかを確かめること。**
  データを `import` して、期待するフィールドが実行時に存在するかを見る。
- **同じ役割のファイルが複数あるなら、全部を突き合わせること。**
  片方だけ直っている場合、直っていない側が雛形にされる。
- **バグを直したら、同型のファイル全部に波及させ、記録を残すこと。**
- **`validate.mjs` に検査が無い箇所は、壊れても誰も気づかない。**
  直したら**同じ事故を捕まえる検査を足すこと**（今回 `graphic` 論点なのに表が無い／
  `tag` が「図表」なのに表が無い／表の列数が揃っていない、の3つを追加した）。

**表が見えるようになって初めて、中身の欠陥が1件露出した**（`vol2-l3.js` の No.89–91 は
表に Saturday/Sunday の2列があるのに、音声のどこにも曜日が無く一意に決まらなかった）。
**描画されていないものは、中身も検証されていない。**

### 模試を新しく作るときは、既存の巻を雛形にしてはいけない（2026-08-18 に判明）

Vol.6 の作成で「Vol.1 の鏡写しにする」と指示したところ、**構造だけでなく内容まで鏡写しになった。**
`why` が完全に同一の文字列、`Thank you for looking into this so thoroughly, and for the suggestion.`
が100%一致、`insertAt: 2` まで一致、という水準。**Vol.1 の記憶で解ける設問は、測定器として無価値。**

さらに調べると、**元から Vol.1〜5 の Part 7 が互いの再スキンだった。**

| 雛形 | 使われている巻 |
|---|---|
| `〈姓〉 & 〈姓〉 〈専門職〉 / 〈住所〉, 〈都市〉 / 〈日付〉` の意見書 | **1・2・3・4・5**（姓も Fenwick / Vasey / Voss を使い回し） |
| `Prepared for the 〈X〉 Committee` の中間報告書 | **1・2・3・4・5** |
| `Subject: Upgrade query`（資格等級の格上げ照会、`admissions@〜.org` 宛、翌日返信） | 2・3・4・5（すべて 186–190 スロット） |
| `The Provincial — 〈分野〉, 〈日付〉` ＋ `Published 〈日付〉` | 2・3・4・5（**4と5は日付まで同一**） |
| `bookings@〜.ie` 宛の団体予約 | 2・3・4・5（すべて最終スロット） |
| `Subject: Enquiry — two days in 〈月〉` | 2・3・5 |
| `TO: All 〜 staff / SUBJECT: 新しい機材の導入` の社内メモ | 3・4・5 |
| 設計事務所 ⇄ 事業会社の見積・提案メール | 1・2・3・4・5 |

**したがって「直前の巻を真似るな」では足りない。既存の全巻で使った型を目録にして、全部避ける。**

- **構造（問題数・`tag` の並び・`level`・文書数・語数の水準）は数値で指定して渡すこと。**
  「Vol.N と同じにして」と言わない。**担当に既存ファイルを開かせないこと。**開くと必ず引きずられる。
- **語数の基準を Vol.1 に取らない。**Vol.1 は Part 3 も Part 7 も既存で最短の巻で、
  実際 Vol.5 の時点で Part 3 は 1,758語（平均135語）に移っている。
  「Vol.1 より多い＝上振れ」は2回とも誤判定だった。**基準は全巻の分布で取る。**

### 複数文書問題（Part 7 の クロス）で繰り返す2つの欠陥（2026-08-18）

**1. 鍵になるデータを stem に書くと、クロスが1文書で解ける問題になる。**
3回起きている:
- `The 3D printers have no limit …` と後続文書が機材名を明示 → 表を引かずに解けた
- stem に「14時間のうち8時間」と書いた → 文書1の規定だけで答えが決まった
- stem に連番 `CK210-51500` を書いた → 文書1だけで解けた

**作ったら必ず、文書を1つずつ隠して実際に解いてみること。**「クロスのつもりで書いた」は当てにならない。
とくに**stem に数値・固有名・型番を書いた瞬間に、その情報は「もう1つの文書」から消える**。

**同じ検査は Part 3・4 の図表問題にも要る。**2つの情報源を突き合わせる型は全部同じ壊れ方をする。
1日で3件出た:
- 音声に `I assume we're in the earliest slot` と書いたら、選択肢が時刻の昇順なので**表を見ずに解けた**
- 表に `Reorder Point` 列を置いたら、**表だけで「水準を下回っているのは1行だけ」と分かった**
- stem に型番を書いたら**文書1だけで解けた**

> **図表問題・クロス問題は、「音声だけ」「図表だけ」「文書1だけ」「文書2だけ」で
> 実際に解いてみるまで、成立しているかどうか分からない。**

**表の列を1つ足すたびに「この列だけで答えが出ないか」を確かめること。**
`Reorder Point` `Capacity` `Status` のような**判定を含む列**は、それ単独で正解を決めてしまいやすい。
必要なのは**照合の材料になる列**（`Used For` `Destination` のような、音声と結びつけて初めて効く列）。

**2. 「既存の巻を開くな」と禁じると、担当は別の既存問題に寄る。**
Vol.6 の作成で `vol1-r4.js` を開くことだけを禁じたところ、
**`assets/data/drills/reading3.js` の求人票とほぼ同一のもの**が書かれた（職種名が完全一致、国も一致、
応募資格もほぼ逐語）。**禁止の範囲を1ファイルに限ると、避けた先で衝突する。**
新規作問では**書いたあとに `assets/data/` 全体と機械照合する**工程を必ず入れること。

### 「骨格が同じ」をどこまで欠陥と数えるか（2026-08-18 に確立）

内容の鏡写しを潰しにかかると、今度は**逆方向に振れて TOEIC の形式から外れる**。
Part 4 の「館内放送で設備故障 → 代替案内 → 再開見込み」や Part 7 の
「料金表 → 問い合わせ → 返信」は**ジャンルの型**であり、本番も同じ形をしている。
骨格が同じというだけで全部書き直すと、本番に似ていない模試ができる。

> **判定の基準は「前の巻を解いた記憶から、この設問の答えが手に入るか」。**

- **漏れる（＝作り直す）**：**答えの根拠そのものが同じ装置**であるとき。
  意図問題の鍵になっている修辞が同型／推測問題の推論の踏み台が同じ／
  「誰が何をすべきか」の結論が同じ命題。
  実例：`"That is not a X. That is Y."` という修辞で切り捨てる型が2巻に出て、
  **その修辞が両方とも意図問題の正解の根拠**になっていた。
- **漏れない（＝そのままでよい）**：骨格・ジャンルの型は同じでも、
  **各設問の答えが別の事実（部屋番号・時刻・数値・固有名）に依存**していて、
  前の巻の記憶からその事実が出てこないとき。

**判定は「セット単位」ではなく「設問単位」で行う。1問でも漏れるならそのセットを作り直す。**
逆に、表層が似ていても答えが漏れないなら、書き直さずに残してよい。

### 図表は「当てずっぽうで当たらない」形に組むこと（2026-08-18）

「片方の情報源だけでは**演繹できない**」を通しても、まだ穴がある。**当てずっぽうの当たりやすさ**は別の検査。

実例：`Load limit / Crane access` の4行表で、正解の行が**耐荷重が最大 かつ クレーン可**だった。
音声の「2.8トン」が無いと演繹はできないが、**表を見て「一番大きくてクレーンが使える行」を選べば当たる。**

> **表の各列について、極端値（最大・最小）や唯一値の行が正解になっていないか確かめること。**
> **2軸とも最良の行を作らないこと。**最大値は誤答の行に置く。

直した結果：最大 3.6t はクレーン不可の行、正解は 3.0t で下から2番目。
「一番大きい行」を選んだ人は**誤答に落ちる**。音声を聞いた人だけが「2.8t以上 かつ クレーン可」で絞れる。

**図表問題の設問どうしを鎖にしないこと。**`What is the load limit for **that area**?` のように
前問の正解を先行詞にすると、1問落とすと2問落ちる。本番はこう繋がない。設問ごとに自己完結させる。

#### 第二軸に「大きいほど良い量」を置いてはいけない（2026-08-25。上の是正が不十分だった）

2026-08-18 に「最大値は誤答の行に置く」で直したはずの同じ設問が、**まだ表と設問文だけで解けていた。**
塞いだのは**一段目の素朴な経路**（表全体の最大値 3.6t）だけで、**二段目が開いていた**——
`Crane access = Yes` で2行に絞り、そのうち耐荷重の大きい行を採れば当たる。
**「屋上の重量物はクレーンで揚げる」は常識であって音声由来の情報ではない。**音声の 2.8t は要らない。

> **真偽条件で集合を絞り、その中を単一の数値閾値で1行だけ通す設計は、
> 構造的に必ず「通った行＝その集合の最大」になる**（そうでなければ判定が成立しない）。
> **だから「絞ってから一番大きいのを選ぶ」で100%当たる。行を増やしても解消しない。**

**第二軸は種別（categorical）にするか、極端値が失格になる向きに組むこと。**
是正例：`Crane access` を `Full / Restricted (2 t max) / None` の三値にする。
「クレーンが使えるうち耐荷重が最大の行」は誤答に落ち、正解は「閾値を超える3行のうち一番小さい行」になる。

**「これは設計に内在する性質だから欠陥ではない」という反論に注意。**実際にそう報告されて一度は受け入れたが、
正しくは**「内在する性質だから直せない」ではなく「その設計を選んだこと自体が誤り」**だった。
**メインが実装役の論拠を検証せずに通したのも判断ミス。**

#### 「極端値を誤答に置く」を徹底すると、補集合が指紋になる（2026-08-25）

上の規則を全巻に一律適用したら、**量を持つ列がある図表23問のうち22問（96%）で
正解が最大でも最小でもない**状態になった。**「最大の行と最小の行を捨てて2択」で 25% → 50%。**
**指示そのものが作った指紋**で、今日3例目（他は正解位置のドリフトと閾値直下への集中）。

**4行×2列では、これが100%の抜け道になる。**「最大は誤答・最小は誤答・唯一値は誤答」を全部満たすと、
**印の付かない行がただ1つ残り、それが正解だと表から逆算できる。**実例:
```
Area 1  2.5 t  Yes   ← 最小        Area 3  3.0 t  Yes   ← 正解／印なし
Area 2  3.6 t  Yes   ← 最大        Area 4  3.4 t  No    ← Crane 列の唯一値
```
行の値をいじる代案を3通り検算したが、**数値の閾値を判別軸にする限り4行の表では閉じない**
（1行を同値にしても唯一値の行が残る／2×2 の数値にすると「可の中で大きいほう」が再び開く／
第3の数値軸を足すと極端値のスロットが3行に散ってまた1行だけ無印になる）。

> **判別軸を「量」ではなく「種別」にすること。**各値がちょうど2回ずつ出る **種別×種別の 2×2** なら、
> 極端値も唯一値もそもそも存在しない。各行が2値の組み合わせとして一意なので、
> **行を決めるには音声から2つの属性を両方取るしかない。表だけでも音声だけでもちょうど25%。**

```
Machine 1  Worktops     Monday        Machine 3  Door frames  Thursday
Machine 2  Door frames  Monday  ←正解  Machine 4  Worktops     Thursday
```
**種別の値に優劣を持たせないこと。**`Reinforced / Standard` のような対にすると
「良いほう×良いほう」が正解になり、「2軸とも最良の行を作らない」に触れる。
`Concrete slab / Steel deck`、`East side / West side` のように**優劣を含まない対**にする。

**金額・時刻・数量を答えさせる設問は、量の列を避けられない。**その場合は上の規則
（正解を極端値にしない）を守ったうえで、**その巻の中で「正解が極端値でない」が全問に揃わないよう
散らす**しかない。**全部を中庸に置くと、中庸を選ぶだけで当たる。**

#### 音声の最上級は、表の極端値を名指しする（2026-08-25）

`shuts earliest of all of them` が `until` 列の唯一の最小値と、
`the longest of our four routes` が `Length` 列の唯一の最大値と、一対一で対応していた。
**「片方だけでは演繹できない」は通っているのに、その一語を拾うだけで表から行が引ける。**

> **音声で対象を指すときに最上級を使わないこと**（`largest` `earliest` `longest` `cheapest`）。
> **行名をそのまま言わないのと同じ理由で禁じ手。**表に無い属性で指すこと。

#### 括弧書き・行名の序数性・列見出しでも漏れる（2026-08-25）

- 選択肢 `'City centre (restaurants)'` の**括弧書きが音声の `restaurants only` と一致**していた。
  補足は選択肢に書かず、表の列に置く。**route 名から業態が連想できるのも駄目**（`Eastside` に改称して解消）。
- 行名が `Locker < Small < Medium < Large` と**それ自体に大小を含んで**いた。
  音声が「小さすぎるものと大きすぎるものの中間」と言えば、表を引かずに `Medium` が選べる。
  **行名から序数性を消し**（`Unit 4 / Unit 11 / Unit 2 / Unit 19`）、**行を数値順に並べない。**
- 選択肢 `Oak Room / Lecture Theatre / Seminar Room B / Gallery` で**正解だけが1語**だった。
  行名の一覧でも「形で浮く」は起きる。
- **表の列見出しをそのまま言い換えた選択肢を正解にしない。**見出しが `Load limit | Crane access` で
  正解が `The roof's load capacity and crane access.` だった。
  「音声だけで解ける設問」を作ろうとして**図表だけで解ける設問**になっていた。
- **設問文が行を名指しし、表がその行の答えを持っていないか。**
  `What is the new rate for the 3–6 hour ticket?` ＋ 表に `New rate` 列、で音声が要らなくなっていた。

#### 図表の作り方は、これ1つに揃える（2026-08-25。18巡かけて収束させた結論）

**量の表は4行では閉じない。**単一の数値閾値を判別軸にする限り、正解行は
「①全体で最大」「②全体で最小」を避けるために必ず非極値になり、その結果
**「⑦どの列でも印が付かない唯一の行」になって表だけで100%当たる。**
数値を動かす案を4通り検算しても、**派生列（前期比・伸び率・合計）のどれかで必ず極値が生まれる。**

> **判別軸は「種別×種別」にし、各値がちょうど2回ずつ出る 2×2 にすること。**
> 4行が2値の全組み合わせを1回ずつ持つので、**極端値も唯一値も無印の行も構造的に存在しない。**
> 行を決めるには音声から2つの属性を両方取るしかなく、**表だけ・音声だけ・先読みだけのどれもが厳密に25%。**

```
Room 12  Rectangular  Glass          Room 9  Rectangular  Solid
Room 5   Round        Glass          Room 8  Round        Solid   ← 正解
```
**実測で収束が確認できたのはこの形だけ**（`vol2` No.65・`vol4` No.53 が3経路とも25%）。
量の表を残した設問は、同じ日の是正でどれも 50〜100% の抜け道が残った。
**金額・時刻を答えさせる設問でどうしても量が要るなら、量の列でも各値をちょうど2回ずつ出す**
（例 `252 / 252 / 165 / 165`）と、実質的に種別列になって同じ性質が得られる。

**2×2 にしたあとも、次の4つは別途つぶすこと。**すべて実際に踏んだ:
- **軸に優劣を持たせない。**`Full height / Low`、`Climate control: Yes / No`、`Floor: Ground / Upper` は
  優劣がある。**優劣軸の有利側に正解を置くと、それだけで50%。**
  `Concrete slab / Steel deck`、`North block / South block`、`Roller shutter / Double doors` は可。
- **1条件だけで絞れないようにする。**2条件のうち片方の一語（`core`）で
  「絞った2行のうち動いているほう」が決まってしまった例がある。**片方だけ聞き取ったとき何択残るかを数えること。**
- **行番号に序数性を戻さない。**行名から `Small/Medium/Large` を消して番号にした3表で、
  **正解が3つとも「最小番号」**になっていた（うち2つは同じ「3」）。
- **表のセル値を他の設問の stem・選択肢に出さない。**`Rectangular` が誤答に、`doors` が別設問の stem に出て、
  それぞれ先読みだけで2択に落ちた。**一語ずつ照合すること。**

**収束の判定基準**：**(a) 音声だけ／(b) 図表だけ／(c) 設問文と選択肢の先読みだけ**で、
**3通りとも25〜33%なら収束。50%以上の経路が1つでも残っていれば未収束。**
**「複数巻を突き合わせないと気づけない」水準の指紋は、収束の判定では不合格にしない**（際限がない）。

#### 規則の文字を守って趣旨を破る（2026-08-25。今日いちばん多かった失敗）

**禁じた形を避けた先で、同じ漏れが別の場所に再出現する。**是正のたびに3回起きた。

| 守った規則 | どこへ移動したか |
|---|---|
| 「行名から序数性を消す」（`Locker<Small<Medium<Large` を `Unit 14/9/3/6` に） | **列に `Full height / Low` を置いて優劣を復活させた。**行名から列へ移っただけ |
| 「音声で行名をそのまま言わない」 | **`the anniversary mailing` という同義語で行を一意に指した。**4つの Source のうち Newsletter しか該当しない |
| 「最大値は誤答の行に置く」 | **正解が96%で中庸になり、中庸を選ぶだけで当たる**ようになった |
| 「安いほうを選ぶ経路を塞ぐ」（金額を組み替え） | **対称な「高いほうを選ぶ経路」が開いた。**2軸とも「高いほう」が正解に |

> **検査は「その語を使っていないか」ではなく「音声だけで／表だけで何択に絞れるか」を数えること。**
> 実際に解いて数えるまで、閉じたかどうかは分からない。

**当てずっぽうは7通り全部試すこと**：①全体で最大 ②全体で最小 ③絞り込んだ集合の中で最大 ④その中で最小
⑤列の唯一値 ⑥中庸の行・毛色が違う行 ⑦**どの列でも印が付かない唯一の行**。
**⑦は「①〜⑥を全部潰した」ことの副作用として生まれる。**潰すほど残る行が一意に近づく。

**セット内の相互ヒントは「一語ずつ」照合すること。**今日の差し戻し4件中3件がこの型だった。
実例：No.69 の正解だけが `booking` を含むのに、**No.70 の stem と選択肢3つに `booking(s)` が出ていた**。
設問を先読みするだけで正解が浮く。**stem 単位の照合では絶対に見つからない。**

### 図表設問は1セット1問・全体で5問（2026-08-25 に本番仕様へ揃えた）

**`Look at the graphic.` の設問は1セットに1問。**Part 3 に3セット・Part 4 に2セットで**計5問**。
2問載せると、2問目は必然的に1問目と同じ行を指したがるため、
**「前問の正解を先行詞で受ける鎖」か「識別条件を書き込んだ長い設問文」のどちらかが必ず発生する。**
実際、全巻で鎖が12件見つかり、個別に直そうとした5件では**3件で新しい欠陥を作り込んだ**。

**設問文に識別条件を書かないこと。**本番は `Look at the graphic. How much will the speaker pay?` 程度で、
**どれについて答えるかは音声側で特定させる。**識別条件を stem の関係節に移すと、課題が
「聞き取り＋照合」から**「キーワード待ち伏せ」**に変質する（音声とほぼ逐語一致の語を待てば済む）。
実測：既存51問の設問文は中央値11語・最大18語。書き換えた5問は17〜23語で**どれよりも長かった。**

**そして、集約すると「残した側」の欠陥が露出する。**2問目に隠れていて誰も点検していなかったため、
**残した30問のうち12問（4割）が壊れていた。**Vol.6 の `graphic` 事故（描画されていないものは中身も
検証されていない）と同じ構図。**構造を変えたら、残った側を全部点検し直すこと。**

**集約は正解位置の偏りも作る。**削った設問がたまたま特定の位置に偏っていると、残った側が一色になる。
実際 **Vol.5 は残った図表9問すべてが A**（削除した5問が A 以外を全部含んでいた）。
**`graphic` は `balance2.mjs` が構造的に除外するので、この偏りは自動では直らない。**

### 選択肢の「形」が正解を教えてしまう（2026-08-18 に発見）

内容の複製とは別の漏れ方がある。**4択のうち正解だけが構造的に目立つ**と、本文を読まずに選べる。

実例（Part 4 の目的問題）：4択のうち **`explain … and …` という複合構造を持つのは正解1つだけ**で、
他3つは単文だった。**同じ形が Vol.5 と Vol.6 の同スロットの両方で起きており**、
「71 は〈説明＋選択肢提示〉の一番長いやつ」というパターン記憶だけで正解できた。

- **正解だけが長い／正解だけが2要素／正解だけが限定句つき、になっていないか確認すること。**
  語数を揃え、複合構造を2つ以上の選択肢に持たせる。
- **これは書き手の癖なので、指示しないと必ず再発する。**Part 4 の30問を測ったところ
  **13問（43%）で正解が単独最長**だった。正解は丁寧に書き、誤答は短く済ませるため。
  **選択肢の語数を並べて数えること。**「正解が最長」が3割を超えたら書き癖が出ている。
- **正解だけが品詞・形式で浮いていないか。**誤答3つが `How to …` で正解だけ名詞句、
  誤答3つが具体的な主語で正解だけ `It` で始まる、といった形も同じ漏れ方をする。
- **同じことがセット内でも起きる。**上の例では正解の `two options` が
  同じセットの別設問の `two repair options` と語彙的に共鳴し、**相互ヒント**になっていた。
- **後の設問が前の設問の答えを漏らす型に注意。TOEIC は設問を先読みできるので実際に得点になる。**
  実例：Q66 の stem が `the discrepancy in **Electronics**` と書いており、表を引くだけで
  Q65（どのゾーンか）が音声なしで確定した。別の例では Q94 の stem に含まれる引用だけで Q93 が解けた。
  **同じセットの設問文・選択肢を並べて、前問の正解に当たる語が出ていないか確認すること。**
- **この型は機械照合では捕まらない。**上の事例の語彙一致は Jaccard **0.27** で、
  n-gram 照合にも類似度照合にもかからなかった。**読んで気づくしかない。**
  機械照合が0件でも「重複なし」とは言えない、ということでもある。
- **2026-08-18 の実測**：Vol.6 全200問を既存全データ（模試5巻＋ドリル23ファイル）と
  4方式（正解語・選択肢集合・stem の6-gram・本文の5-gram）で照合したところ**5件**が出たが、
  **読んで発見された衝突が別に4件あった**。うち2件は語彙の一致がゼロで、
  共有していたのは**装置**（図表の作り・故障→代案という運び・結びの一文の役割）と
  **正解の命題**だけだった。**機械照合は下限であって、上限ではない。**

#### 全1,200問で実測した（2026-08-28）。漏れているのは3つの軸だけ

2026-08-18 の記述は個別事例に基づいていた。**全模試1,200問で測ると、どの軸が本当に漏れているかが
はっきりした。**「4本のうち**ちょうど1本だけ**が他と違う形をしているとき、それが正解である割合」:

| 形の軸 | 該当設問 | うち正解 | 偶然 | z | 判定 |
|---|---|---|---|---|---|
| **複合（and / or を含む）** | 74件 | **48件 (65%)** | 25% | **+7.92** | 漏れ |
| **先頭語** | 120件 | **55件 (46%)** | 25% | **+5.27** | 漏れ |
| 終止符の有無 | 1件 | 0件 (0%) | 25% | −0.58 | 漏れていない |
| 読点の有無 | 93件 | 19件 (20%) | 25% | −1.02 | 漏れていない |
| 語数が1本だけ違う | 194件 | 45件 (23%) | 25% | −0.58 | 漏れていない |

**終止符・読点・語数の「1本だけ違う」は、個別事例としては存在するが系統的な漏れではない。
これを直すために文言をいじらないこと**（今日、直そうとして別の指紋を作った例がある）。

**複合軸のパート別**：Part3 **91%**(10/11)／Part4 **76%**(13/17)／Part6 67%(4/6)／Part7 58%(18/31)。
**長さの漏れより命中率が高い区画がある。**

**巻をまたいでスロットが一致する。**該当設問は **No.169 が vol1・2・3・4・5 の5巻**、
**No.147 が vol3・4・5 の3巻**。設問スロットの役割が巻をまたいで同じなので、
**書き癖が同じ番号に着地する。**利用者が複数巻を解くと「169番は and が入っているものを選ぶ」と学習できる。

> **是正は「誤答側にも複合を作る」方向で行うこと。**正解から `and` を削って単一要素にすると、
> 今度は「正解だけが単純」という逆の指紋になる。

`tools/validate.mjs` の**検査H**が複合軸と先頭語軸を見る（1巻×1パート単位、該当8件以上で z>=2.0 なら WARN）。

#### 語数の漏れは全パートで測った（2026-08-28）

「**一番長い選択肢を選ぶ**」だけの的中率（偶然は4択25%・Part2 の3択33%）。是正前の実測:

| | P2 | P3 | P4 | P6 | P7 |
|---|---|---|---|---|---|
| vol1 | 75% | 44% | 51% | 67% | 45% |
| vol2 | 63% | 35% | 52% | 36% | 49% |
| vol3 | 63% | 46% | 40% | 39% | 62% |
| vol4 | 69% | 55% | 76% | 60% | 60% |
| vol5 | 55% | 43% | 60% | 56% | 64% |
| vol6 | 37% | 47% | 35% | 63% | 50% |

原因は書き癖——**正解は根拠に忠実に丁寧に書き、誤答は短く済ませる。**
**Part 5 は健全（23%）。**選択肢が1語なので書き癖が乗らない。

**Part 6 は3つに分けて測ること。**混ぜると原因を取り違える（実際に2回取り違えた）:

| 区分 | n | 最長選択 | 判定 |
|---|---|---|---|
| 文挿入（4文から1文を選ぶ。`topics` に `p6ins`） | 24 | 29% | 健全 |
| **語句の4択（選択肢に3語以上を含む）** | 25 | **76%** | **漏れ** |
| 語の4択（全選択肢が2語以下） | 47 | 20% | 健全 |

漏れているのは **`will be relocated` / `must be disclosed` のような助動詞＋受動態が正解**の設問。
**誤答が短い能動形（`relocated` `discloses` `holding`）に揃っている。**
**是正は誤答側も複数語形にする**（`is being relocated` / `has been disclosing`）。文法の論点は損なわれない。
先例が `vol2-r1.js` No.143 にある——
`*will be removed[3] / is removed[2] / was removed[2] / has been removed[3]` と誤答にも3語形がある。

`tools/validate.mjs` の**検査G**が語数の漏れを見る（最長側・最短側の両方。z>=2.0 で WARN）。

#### 受動態の軸は k 別に測る。誤答を能動化すると主語の癖が出る（2026-09-02）

2026-08-28 の是正で「受動態が1本だけ＝正解」を Part 5・6 で 69%→8% にしたら、全パート合算では 23%→15%（z=−3.39）へ
逆に振れていた。今回、**誤答側の受動態を能動態に書き換える**方式（正解は原則触らない）で全パート合算 20%（z=−1.33）に戻した。

- **k（受動態を含む選択肢の本数）ごとに別の母数で測ること。**k=1 だけ見ると Part 7 の k=2（受動態2本→どちらも誤答、31%）、
  Part 3 の k=3（唯一の能動＝正解、54%）が見えない。同じ書き癖の別の現れ方で、`validate.mjs` の**検査I**は k 別に見る。
- **Part 6 の動詞活用問題（`must be disclosed` 型）と Part 1 は、受動態が論点そのもの**なので、この軸の是正対象から外す。
  Part 6 語句の k=2 は 14/14 で残っており検査Iが WARN を出すが、**能動が正解で受動の誤答を持つ問題を作る素材が無い**
  （k=0 の語句問題は全部が接続詞・副詞の語彙問題）。**形の漏れとは数えない。**Part 1 は線画の描写で `is being + 過去分詞` が論点。
- **能動化した誤答の主語は、指示しないと1種類に揃う。**`They` に揃い（27本中20本）、「They を避けよ」と言うと `Someone` に揃い（14本）、
  「動作主を足せ」と言うと Part 3・4・7 の選択肢に**一人称 `We` が13本**入った（HEAD では Part 6 の挿入文1本しか無い）。
  さらに他の3本が `It…` の設問で1本だけ `The company…` にすると、**「1本だけ主語の種類が違う＝誤答」が12/12**で成立した。
  > **Part 3・4・6・7 の選択肢は三人称で書く。`They` `It` は先行詞が本文か stem にあるときだけ。
  > 主語の種類は同じ設問の他の選択肢と揃える（他が `It` なら `It`＋別の動詞、他が名詞句なら名詞句）。**
  動作主を避けるには自動詞・能格動詞（`closes` `arrived` `went up` `came through`）が最も安全。
  ただし **`went in / went out` のような現場口語は書き言葉の選択肢に使わない**（コーパスで確認できない）。
- **Part 2 の応答を能動化するときは、問いの語の反復（deliver→delivery）を残し、動作主で Who 疑問に答えない。**
  `Yes, we ordered them.` は Who 疑問に人を答えており、排除根拠が冒頭の Yes だけになる。
- **計測器の受動態判定に誤検知が2種類あった**：所有格の `'s`＋過去分詞形の語（`the neighbour's shed`、`the client's agreed budget`）と、
  `-en/-ed` で終わる非過去分詞（`is open` `is between` `are even`）。**選定リストの1件が「書き換える対象が存在しない」と報告されて判明。**
  検査Iの `isPassive` は縮約を代名詞形の列挙に限定し、除外語の否定先読みを持つ。**新しい計器は、実装役が手で当たるまで信じないこと。**
- **書き換えの制約は数値で渡す**（語数 ±1、正解の語数順位不変、and/or・フィラー禁止、先頭語は原則維持）。今回はこれで
  語数の順位分布・最長的中率・フィラー率が着手前と一致した。**それでも先頭語軸に3種類の指紋が出た。**制約の外にある軸は必ず動く。

#### 定形／非定形の軸（2026-09-12。6本目の計器＝検査J）

**第二の正解を潰すために誤答の形をそろえると、その形自体が答えを教える。**
要求・提案の that 節の誤答に置いた直説法（英式で第二の正解）を外して**全部非定形**（`to V` / `V-ing` / `having V-en`）に
差し替えたところ、7問で

> **「`to` も `-ing` も `having` も付いていないものを選ぶ」だけで全問当たる**（HEAD 0% → 100%）

状態になった。**既存の検査G（語数）・H（複合/先頭語）・I（受動態）はどれも検出しない。**
語数も複合も先頭語も受動態も動いていないため。→ **検査J** を `validate.mjs` に追加した。

- 軸は「定形かどうか」ではなく **「その位置で述語として使える形か」**。
  **述語になれる側＝定形＋原形＋原形受動**（`be filed`）／**なれない側＝`to V`・`V-ing`・`having V-en`・
  `being V-en`・裸の過去分詞**。**`be V-en` を非定形に数えると7問中3問を取り逃がす**（既存の測定器が実際にそうなっていた）。
  過去形と同形の `-ed`（`checked` `suspended`）は**判別不能として設問ごと母数から外す**。
  不規則動詞で過去形と形が違う過去分詞（`worn` `taken`）は「なれない側」に数える。
- 検査G・Hと同じ粒度（模試1巻×1パート／ドリル1ファイル×1パート、および合算）で、
  **k=1（述語になれる形が1本だけ）と k=3 の両方向**を測り、**該当8件以上・|z|>=2.0 で WARN。**
- **同じ事故が別の論点・別のファイルでも独立に起きていた**（`p6c-06-3r`。第1巡の再監査が
  「誤答を非定形にして構造で閉じよ」と指示した結果）。つまり「要求提案の that 節」固有ではなく、
  **「誤答を構造で閉じようとすると非定形に寄る」という書き手の癖**である。

**「形で解ける」が常に漏れとは限らない — 止め時の判定（2026-09-12。この型で4巡した末の結論）**
`not` 先行型に落ち着いたあとも、**「活用していない1本を選ぶ」だけで mandative 12問が12/12**で当たる。
**これは漏れではない。**この型で問うている知識が**「要求・提案の that 節の述語は原形」そのもの**だからで、
本番でも原形が正解になる以上、この方略は**そのまま通用する正しい技能**である。
一方、誤答を全部非定形にした①の状態は、**「空所には述語が要る」という別の知識だけで解けてしまう**——
論点を知らなくても当たるので、あれは漏れだった。

> **検査Jが見ているのは「論点を知らなくても形で解けるか」であって、「形＝論点」の設問は対象外。**
> **軸が残っていても、その軸を使うのに論点の知識が要るなら、そこで止める。**

止めないと終わらない：**正解の形を見れば分かることが論点である型では、
どう組んでも「正解の形を選ぶ」戦略が成立する。**

**曖昧な `-ed` 形を誤答に足して逃げないこと。**検査Jはその設問を母数から外すので、
**警告は消えるが漏れは残る。計器を避ける修正は禁止。**

**閾値の下限に注意。**検査G〜Jは該当8件未満の区画を判定しない。
**7問のうち3問を直した時点で警告は消えるが、残り4問の漏れはそのまま残っている。**
**「警告が消えた」を「直し終えた」と読まないこと。**設問ごとに確かめる。

**検査Jを入れると、HEAD の状態でも警告が3件になった**（Part1・Part6 の受動態＋ Part6 の定形 k=3）。
Part6 の「述語になれる形が3/4本のとき定形側が100%正解」は **HEAD で 12/12・z=2.00** の既存の癖で、
今回の是正で **17/17・z=2.38 に強めてしまった**（構造で閉じ直すときに誤答が定形寄りになるため）。
抜け道は「非定形の1本を選ばない」だけで **25% → 33%**。直すには「Part 6 で非定形が正解になる設問」を
新しく書く必要があり、**新規作問は6回中5回で新しい欠陥を生む**ので、今回は直さず申し送りにした。

#### 否定語の軸（2026-09-12。7本目の計器＝検査K）

**「否定語を持つ選択肢が k 本あるとき、正解がその側に来る割合」**を測る。
判定語は `not` / `never` / `n't` / `no` / `none` / `nothing` / `nobody` / `neither` / `nor` / `without` / `cannot`
（`No. 4` のような略記だけ否定先読みで弾く）。実測（k=1 の区画が中心）:

| 区画 | 該当 | 正解が否定側 | 偶然 | z |
|---|---|---|---|---|
| **模試+ドリル Part2** | 26問 | **22問 (85%)** | 33% | **+5.55** |
| **模試+ドリル Part4** | 31問 | **19問 (61%)** | 25% | **+4.67** |
| 模試6巻 Part7 | 69問 | 28問 (41%) | 25% | +2.99 |
| 模試+ドリル Part3 | 38問 | 17問 (45%) | 25% | +2.81 |

**検査Kを入れると警告が 4件 → 16件になる**（検査Kが12件）。
**HEAD でも同じ12件・同じ区画**なので、**データ全体に元からある指紋**である。
**Part 2 がいちばん重い（85%）。**正解が否定の応答（`No, they pushed it back to April.`）になりやすく、
誤答は肯定形で無関係な事実を言う、という書き癖。
**検査Jの「形＝論点なら対象外」は Part 2 には当てはまらない**——
Part 2 が問うているのは応答の適切さであって否定形そのものではないので、
**「否定の選択肢を選ぶ」は論点の知識を使わない方略**である。

> **是正は誤答側に否定語を配る方向で行うこと。**正解から否定を外すと「正解だけが肯定形」という逆の指紋になる。

**`n't` の単語境界に注意。**`\bn't\b` と書くと `doesn't` の `n` の前が `s` なので**1本も拾えない**
（実装時に実データ12本を取り逃がしていて、手で当たって発見した）。`n['’]t\b` と書くこと。

#### 否定語の軸の是正でわかったこと（2026-09-12。128問に手を入れ、4体の監査で致命的13本）

**選定は機械で、書き換えは「正解を触らず誤答1本にだけ否定を足す」で行った。分布は予測どおりに着地した**
（k=1 の正解率 43/30/32/31%、検査Kの警告12→0）。**しかし内容は4体中3体で壊れた。**

- **否定を足せる誤答は、本文と矛盾して閉じているものだけ。「本文に言及が無い」で閉じている誤答に否定を足すと、ほぼ必ず真になる**
  （偽の文を否定すると真になる）。推測（`p7inf`）・意図（`p3int`）の誤答は元来「触れていないこと」で作るので、この型の設問は対象にできない。
  **機械選定は設問の型でしか選べない。誤答がどう閉じているかは人が1本ずつ見てから書かせること。**
- **「本文が肯定している事実を否定せよ」だけ渡すと、実装役は最も鮮明な肯定文＝正解の根拠を選ぶ。**
  結果、正解と真っ向から矛盾する対（`cannot arrive before seven` ⇔ `will never arrive after seven`）ができ、**本文なしで50%**になる（Vol.5-6 で9問）。
  > **正解の根拠になっている文は使わない**、を制約に足すこと。
- **否定形の誤答を配れない stem がある**：`Why…?` `What mistake…?` `What is the stated reason…?` `What is she unable to do?`、営業時間など相互排他の一覧。
  答えの形をしていない1本（25%→33%）か、正解の補集合になる。
- **名詞句・動詞句を並べる選択肢に否定形の定形節を1本入れると、統語範疇だけで捨てられる**（検査Hの軸そのもの）。
  `No + 名詞句` で始まる1本も同じ。**品詞・主語の種類は4本で揃える。**
- **否定語の種類は担当ごとに1語へ収束する。**Vol.5-6 は `never` に39本中17本（既存の `never` は8本すべて誤答側だったので「`never` があれば誤答」が 29/29 に）。
  Vol.1-2 は `n't`、Vol.3-4 は `not`。**分布目標を渡すだけでは足りない。担当ごとに数えさせ、メインが合算して偏りを戻すこと。**
- **「真だが論点外」は排除根拠にならない。**3体がこれを「安全策」として選び、`why` に「この否定文は事実として正しいが」と**穴を自認**して書いた。
  規則を文で渡しても、実装役が安全だと感じた方に負ける。**禁じる形を名指しして渡すこと**（「本文に記載がない」型の否定形は禁止、と書いたのに逃げた）。
- 数値制約（語数±1・先頭語・受動態）の逸脱は128本中53本。大半は `No`/`Not`/`There` で文を始めたもので、それ自体は自然な否定文。
  **機械的に不合格にせず、レビューに回して形の浮きだけを判定させる**のが正しかった。
- **「変化・理由・方法・問題」を問う設問**（`What change…?` `Why…?` `How does…?` `What problem…?`）に否定を足すと、
  否定文が自動的に「変わっていない／問題ない」になって**設問の前提と衝突**し、本文なしで落とせる（25%→33%）。
  否定を足す先は**命題を述べる設問**（詳細・推測・意図）に限る。stem の先頭語で機械的に除外できる。
- **図表のセットでは、表の列見出し・セル値を選択肢に持ち込まない**（Q54 の誤答に `north block` が入り、Q53 の表が先読みで2行に落ちた）。
- **矛盾の確認と「正解と同じ軸の裏返しでないか」の確認は対で行う。**矛盾だけ確かめた差し替えが、正解の軸の裏返しになっていた（雨で危険 ⇔ 雨は無関係）。

> **是正の手順（次にこの軸を触るときはこの順で）**：
> ①機械選定は「否定なし・完全文の選択肢・命題を述べる stem・図表でない」で絞る
> ②人が1本ずつ「矛盾で閉じている誤答があるか」を見て、無い設問は落とす
> ③実装役には「本文の別の肯定文（正解の根拠でないもの）を否定せよ。既存の誤答は否定するな。真だが論点外は禁止」を**禁じる形を名指しで**渡す
> ④否定語の種類・位置・先頭語を担当ごとに数えさせ、メインが合算する
> ⑤監査は「真になっていないか」「正解の裏返しでないか」「stem の型と合うか」「形で浮かないか」「他設問の正解語を持ち込まないか」の5点

#### 検査G〜J が見ていない軸は、まだある（2026-09-12）

**vol3 の No.161 は、5つの計器を全部通過したうえで100%漏れていた。**
語数 8/9/9/8・複合0・受動0・全部定形なので、**検査G〜J はどれも発火しない。**漏れていたのは:

1. **時制** — 誤答3本が過去完了（`had formed` / `had corroded` / `had failed`）、**正解だけが単純過去**（`let`）
2. **他動性** — **正解だけが目的語を取る**（`let in moisture`）。他3本は自動詞
3. **論理的に対立する2本** — `They have all tested clear` と `They have not all been cleared` のように
   **一方が真なら他方が偽**の2本を置くと、**「対のどちらかが答え」で4択が2択に落ちる**
4. **最小対** — 内容語の重なりが突出して高い2本があると同じことが起きる。
   全1,200問で内容語 Jaccard >= 0.45 の9問のうち**8問（89%、z=2.33）で正解がペアに入っていた**

> **選択肢は、語数・複合・先頭語・受動態・定形性だけでなく、時制・他動性・否定の有無も4本で揃えること。**
> **論理的に対立する2本／語がほとんど同じ2本を、同じ設問に置かないこと。**4本は互いに独立した次元にする。

#### 並べ替えられない選択肢は C に偏っている（2026-08-28）

金額・数値・時刻・日付・`[1]`〜`[4]` のように**順序に意味があり並べ替えられない**4択だけを抽出すると:

```
        A   B   C   D    n
合計   10  17  27   6   60
割合   17% 28% 45% 10%      （偶然 各25%。カイ二乗 16.93 / 自由度3 → p<0.001）
```
**「金額・時刻・日付を答える設問は C を選ぶ」で 45%、「一番大きい値は選ばない」で 90% 正しい。**
vol4 と vol6 は13問中7問が C。

原因は誤答の作り方が「①足し忘れ ②少し違う ③正解 ④二重計上・最大」になっていること。
**昇順に並べると正解が必ず3番目に落ちる。**2026-08-25 の
「極端値を誤答に置くを徹底すると補集合が指紋になる」が、図表の行だけでなく**数値の選択肢そのもの**にも起きていた。

**`balance2.mjs` は `classifyEntry()` でこの種を構造的に除外する**（並べ替えると数値の順序が壊れるため、
これは正しい挙動）。**検査A〜F もパート単位の並びを見るので素通りする。
`graphic`・`p7ins` と同じ「人が見るしかない」区画がもう一つある。**

是正は**並べ替えではなく数値そのものの組み替え**で行う。正解を D に置くには誤答3つを**すべて過少計上**に
（「割増を足し忘れ」「2人目を数え忘れ」）、A に置くには**すべて過大計上**にすればよい。
**全巻まとめて判断すること。**単巻だけ直すと別の指紋になる。

### 正解位置は「件数が均等」では足りない（2026-08-18）

**均等な件数は、正解位置が適切であることを意味しない。**`A8/B8/C7/D7` は完璧な分布だが、
その並びが `ABCDABCD…` でも同じ数字が出る。**本文を読まずに全問当たる。**

Vol.6 で**3回**起きた（Part 5・Part 2・Part 7 の一部）。既存5巻には一度も無い。
原因は指示の側にある——**「A/B/C/D をほぼ均等にせよ」と言われた担当は、輪番で均等を達成する。**
均等の最も簡単な作り方が循環だから。**指示するときは「均等かつ規則性が読めないこと」まで言うこと。**

**指示を出すたびに、別の規則性が生まれた。同じ失敗を3回している。**

| 指示 | 生まれた規則性 |
|---|---|
| 「A/B/C/D を均等にせよ」 | **輪番** `ABCDABCD…`（+1 の循環） |
| 「循環を崩せ」 | **降順** `DCBADCBA…`（−1 の循環）と、**同じ記号を隣り合わせない癖** |

実測（2026-08-18、Vol.6）：4択パートで「前問と同じ位置」が **11%**（他巻23〜31%、偶然25%）、
Part 5 の降順が **52%**、**Part 6 は4文書とも A/B/C/D が1回ずつ**（他5巻は 0/4）、
Part 3・4 は **セット内3問が全部別文字**が 77%・90%。
**3問埋めれば4問目が消去法で分かる。**

> **「均等」「規則性が読めない」だけでは足りない。「偶然に見えること」を要求すること。**
> 偶然なら、同じ記号は 25%（3択は33%）の確率で隣り合う。**隣り合わないほうが不自然。**

`tools/validate.mjs` に検査を追加済み（**A〜E の5つ。すべて `--extra` にも効く**）:
- **A 循環（昇順）**：「次の正解が +1（mod k）」が **60%以上で ERROR**
- **B 連続**：同じ位置が **6問以上続いたら WARN**
- **C 循環（降順）**：「次の正解が −1（mod k）」が **60%以上で ERROR**
- **D 隣接同一率が低すぎる**：「前問と同じ位置」が 4択で **12%未満なら WARN**
- **E まとまり単位の使い切り**：Part 6 の1文書（4問）で A〜D が1回ずつ → 75%以上で WARN。
  Part 3・4 の1セット（3問）が全部別文字 → 70%以上で WARN

**A と C は設問10問未満のパートを判定しない**（Part 1 は6問＝5組しかなく、
3組が降順なら自動的に60%になって誤検知する。実際 vol1・vol2 も引っかかった）。
**Part 1 の6問は機械では判定できないので、人が見ること。**

`balance2.mjs` は件数の平準化ツールなので、循環も連続も崩さない。**別の検査が要る。**

**検査を足したら、わざと壊したデータで発火を確かめること。**
この検査は当初 `registry.js` 登録済みの模試にしか効かず、**`--extra` で渡す未登録ファイルを素通り**していた。
作業中の Vol.6 がちょうどその穴に入っており、担当が「エラー0・警告0」と報告し続けていた期間、
循環は一度も見られていなかった。**壊したデータで試して初めて分かった。**

#### 4回目・5回目の規則性（2026-08-25）

**「規則性を消せ」という指示は、必ず別の規則性を作る。**

| 指示 | 生まれた規則性 |
|---|---|
| 「A/B/C/D を均等にせよ」 | **輪番** `ABCDABCD…` |
| 「循環を崩せ」 | **降順** `DCBADCBA…` と、同じ記号を隣り合わせない癖 |
| （ツールの副作用） | **前半に D・C、後半に A・B というドリフト** |
| 「ドリフトを消せ」 | **全区画が閾値の直下に並ぶ** |

**4回目のドリフトは `balance2.mjs` 自身が作っていた。**`balancePart()` が過剰な位置を減らすとき
`entries.find()` で**常にファイル内で最も早い設問**を選んでいたため、書き手が多用した文字が
前半から順に剥がされ**末尾にだけ残る**。件数は均等になるので、ツールの報告も検査A〜Eも通る。
実測：35区画中30区画で同じ向き、最大で前半平均 2.37 → 後半平均 0.47（4σ超）。
**修正済み**（設問 id の FNV-1a ハッシュで選ぶ決定的な方式。「末尾から選ぶ」に変えると逆向きの偏りが出る）。

**5回目は、メインの指示が作った。**「過剰に0へ寄せないこと」とだけ書いたため実装役が
「閾値の直下で止める」と解釈し、**36区画中21区画が |z| = 1.74〜1.96 に固まった**
（同じ値が7区画・5区画・3区画…と繰り返した）。ドリフトという指紋が別の指紋に置き換わっただけ。

> **消す対象を名指しするのではなく、「偶然そうなった分布」を目標として与えること。**
> 是正後：|z|<1.0 が26区画、1.0〜1.6 が9区画、1.6以上が1区画。同値が3区画以上で繰り返さない。

**閾値の検査には副作用がある。**帰無分布に忠実なデータなら |z| ≥ 2.0 は36区画中1〜2件は出るはず。
つまり**「警告0件」を合格線にすると、警告を避けるために不自然に整えた状態へ引き寄せられる。**

#### 検査F（2026-08-25 に追加）

**F 前半・後半の系統的ドリフト**：1つの巻×パートで、`no` 昇順の前半と後半の正解位置（A=0…）の
平均差を帰無分布の標準偏差で割った **z スコア**。**|z| ≥ 2.0 で WARN。10問未満は判定しない。**
3択（Part 2）と4択を同じ閾値で扱うために正規化してある。閾値は実データで決めた——
健全な Vol.6 は最大 |z|=0.82、重症の Vol.3 は 2.68〜5.22 で、その間に3.3倍の余裕がある。

**`balance2.mjs` に `--by drift` を追加した。**前半の高い位置と後半の低い位置を**対で交換**するので
件数は1件も動かない。`applyMoves()` / `classifyEntry()` を再利用しており、除外規則も配列 `ja` の
並べ替えもそのまま効く。**A〜Eを新規に発火させる候補は事前に弾く**（弾かないと、ドリフトを直した
拍子に検査D（隣接同一率の**下限**）と検査Eに引っかかる。実際に起きた）。
**複数巻を一度に渡すこと。**単一巻だけだと、巻をまたいで同じ z に着地する衝突を避けられない。

**`graphic` と `p7ins` は構造的に除外されるので、この2つの論点の正解位置はどの自動ツールでも直らない。
人が見ること。**

#### 文挿入（`p7ins`）の設計（2026-09-12 に確立）

**閉じ方は照応ではなく、算術・時系列・初出違反で設計する**（Vol.6 の作成時に照応で閉じようとして
2問とも2〜3回失敗した）。ただし**この3つには原理的な上限がある:**

> **「まだ出ていない」で排除する装置（初出違反・算術・時系列）は、正解より“前”のマーカーしか落とせない。**
> **正解より“後ろ”のマーカーは、情報が出そろっているので隣接（＝照応）でしか落とせない。**

だから**正解を `[4]` に置く設問だけが、全マーカーを強い型で閉じられる。**
`[1]` や `[2]` に置きたいときは、次の型を使う:

> **逆向きの初出違反。**挿入文の側が**初出の形**（フルネーム・不定冠詞・略語の展開）で導入し、
> **本文の後方がそれを既出として受けている**状態を作る。後ろに置くと
> 「**既出のものを、後からもう一度初めて導入する**」違反になるので、後方のマーカーが構造で落ちる。
> **固有名は役割名詞と違ってブリッジング推論が効かない**ので、フルネーム → 裸の短縮形が最も確実。

**正解位置を前方にしたい巻には、この型を名指しで指定すること。**
`[1]` を「初出違反」で閉じよと指示すると**原理的に成立しない**（2026-09-12 にメインが実際に誤指示し、
`[2]` が第二の正解になった）。

**排除根拠に算術を使うときは、その算術が挿入文の内部で完結していないか確かめる。**
`That is a rise of £36 on a unit that previously cost £80.` は 36/80=45% が本文の
`45 percent higher per unit` と噛み合うが、**£80 は本文に無い**ので、
「挿入文の数値を本文のどの割合と突き合わせてもよい」と読めると別の位置でも算術が通ってしまう。
**解説が自分で第二の正解への道を開く**ことがある。

**裸の `That` は距離を跨げない。**`That gap of 24` のように**自分で先行詞を言い直す**形にすると、
離れた位置からでも受けられてしまい、隣接による排除が効かなくなる。
離れた先行詞を受けさせたいときは `The fall of 153 incidents` のような**定名詞句**にする
（記述内容で同定するので、隣接を要求しない）。

**挿入文は「5つ目の選択肢群」として先読みされる（2026-09-12）。**
`quiz.js` の `renderDocUnit()` は **doc ユニットの4問すべての stem と16の選択肢を本文と同一画面に出す。**
文挿入の `stem` には**挿入文が丸ごと引用される**ので、挿入文も先読みの対象になる。
vol4 の No.174 の挿入文が**報告書の結論そのもの**だったため、その1文から
No.175 は誤答2本が矛盾で脱落して正解が決まり、No.173 も誤答2本が脱落し、No.172 の正解まで決まった。
**4問中3問が本文を読まずに解けていた。**

> **挿入文に、同じ文書の他の設問の答えになる命題を書かないこと。**
> 書いたら、**挿入文だけを見せて他の3問を解かせてみること。**

### 文書の見出し（`title` / `head`）から正解が漏れる（2026-09-12 に発見）

**3人の実装役が、別々の題材・別々のファイルで、独立に同じ事故を起こした。**
Part 7 の文書を新しく書き下ろしたところ、**3巻とも見出しがそのユニットの第1問の答えを名指ししていた。**

| 見出し | 第1問 | 漏れ方 |
|---|---|---|
| `How a Damp Seal Cost Cairnholm Its Clearest Nights` | 何が湿気を主鏡に届かせたか | 選択肢で `seal` を含むのは正解だけ |
| `Change in Stone-Cutting Method on the Northern Face` | 北面で何をすることに決めたか | **見出しがそのまま答え** |
| `Cutting Seedling Losses in the Propagation Range` | 2台のベンチの共通点は何か | `losses` に触れるのは正解だけ |

`assets/js/render.js` の `renderDoc()` は `doc.head` → `doc.title` の順に**実際に画面へ描画する。受験者に見える。**

**なぜどの計器も捕まえなかったか**：`setleak.mjs` も `dupcheck.mjs` も「設問と設問のあいだ」しか見ず、
`validate.mjs` の検査G〜Jは「1つの設問の中」しか見ない。**見出しはどの計器の視野にも入っていなかった。**

> **`title` と `head` に、そのユニットのどの設問の正解の内容語も書かないこと。**
> **書き終えたら、見出しだけを読んで4問を解いてみること。1問でも絞れたら書き直す。**

`tools/titleleak.mjs` を追加した（`setleak` と同じ**誤検知が大半の一次ふるい**。HEAD で47件出るので
`validate.mjs` には入れていない。入れると「エラー0・警告0」の運用が壊れる）。

**メモ文書に `title` と `head` を両方持たせないこと。**`head` の `Re:` 行の直下に同内容の見出しが出る。
実データのメモ9件はすべて `head` のみ。2026-09-12 に `Report` を `Memo` に作り替えたとき、
**`title` だけが Report 時代の形で残り**この事故になった。
**そして `title` を消しても `head` の `Re:` 行に同じ語が残っていて漏れは消えていなかった。両方見ること。**

### Part 7 の誤答は「本文の中の事物」で作る（2026-09-12）

vol3 の No.164 は**誤答3本が本文にも他の設問にも一語も接点を持たず**、
**正解だけが文書の主題語（`dome` / `shutter`）を持っていた。**
4問を先読みすると主題が分かるので、**主題語を持つ1本を選ぶだけで当たる。**

> **Part 7 の誤答は、本文に出てくる語・事物を使って作り、本文と矛盾させて閉じること。**
> **「本文に記載がない」だけで閉じる誤答は1問につき1本まで。**

これは `setleak.mjs` の裏返し（**正解だけが主題語を持っていないか**）である。

**文書の唯一の目玉事実は、他の設問が指していると4択にできない（2026-09-12。3巡失敗した）。**
vol3 の No.172 は「北面で何をすることに決めたか」を問うていたが、
**タイトル・stem・No.174 の挿入文・No.173 の stem が揃って文書の目玉事実（発破→ワイヤー切断の切替）を
指していた**ため、誤答をどう作っても閉じなかった:

| 誤答の作り方 | 塞がる | 開く |
|---|---|---|
| 範囲を変える | 主題語 | **stem とタイトルが範囲を無償で与える** |
| 北面で発破を続ける | 範囲 | **No.174 の挿入文（苦情がほぼゼロ）で落ちる** |
| 別工法を出す | 範囲・174 | **主題語で落ちる** |

**3周して同じ3択を回った。**本文の未使用部分（苦情と保険会社）を問う設問に**作り替えて**決着した。
**これは誤答の書き方の問題ではなく、設問が何を問うかの問題で、CLAUDE.md の
「型がそもそも4択に乗るかは、作問を始める前に判断すること。これは指示を出す側の責任」に当たる。**
**1文書の中で、目玉事実を問うのは1問だけにすること。**

### 実在の固有名詞の扱い（2026-08-18）

- **実在の製品・サービス・企業を、優劣や評価の対象として英文本文に書かないこと。**
  Vol.6 で `Freshdesk` `Zendesk` `HelpScout` を「A や B に慣れていれば評価される」という文脈に置いた例が出た。
  `assets/data/` 全体でも英文に実在ブランドが入っていたのはこの1ファイルだけで、他は全部架空名。
- **メールドメイン（`gmail.com` `fastmail.com`）は個人アドレスとしてなら可。**
  既存3ファイルで使っており、実在製品を評価するのとは別。

## 問題を差し替えるときの決まり

- **中身を実質的に変えたら、設問の id を必ず新規採番する。**id を使い回すと、別問題なのに
  「以前正解した」履歴が SRS に引き継がれて復習間隔が壊れる（`store.js` は設問 id をキーにしている）。
- **id を採番し直す基準（2026-08-28 に確定）：stem と正解が変わっていなければ据え置く。
  stem か正解を変えたら新規採番する。**
  SRS の記録は「この設問に正解できたか」なので、**stem と正解が同じならその記録は依然として有効**。
  誤答の文言が変わっても測っている力は同じ。
  逆に「誤答を1本でも差し替えたら採番し直す」と読むと、2026-08-28 の語数是正だけで
  **400問超の履歴が消える**——規則が防ごうとしている害そのものになる。
  **模試の設問も SRS に載る**（`quiz.js` の `finish()` が全モードで `recordItem(q.id, …)` を呼ぶ）。
  「模試はドリル画面に出ない」は「SRS に載らない」ではない。
  過去に `v4q10r` `v4q22r` を誤答差し替えだけで採番し直した例があるが、そのままでよい（害はない）。
  - **例外として新規採番する場合**：**第二の正解を閉じたとき**（その誤答を選んで「不正解」と
    記録された履歴が誤りになるため）、および**誤答の過半を差し替えたとき**（設問の実体が変わるため）。
    この2つは件数が少ないので履歴を捨てる害が小さい。
    先例：`v3q23r`（3択のうち誤答2本を差し替え）、`p2w-11r`（第二の正解を閉じた）。
  - **迷ったら据え置く。**採番し直すと履歴は必ず消えるが、据え置いて困るのは
    「その誤答を選んだ人の1回分の記録」だけで、次の出題で上書きされる。
  旧 id の履歴は孤児として残るが `registry.js` が読み飛ばすので実害はない。
- **模試の `p5()` / `p2()` ヘルパーは設問 id を通し番号 `no` から自動生成する。**
  `no` を保ったまま中身を差し替えると**意識しないと自動的に id を使い回す。**
  その1ユニットだけヘルパーを使わず直接オブジェクトを書いて採番すること
  （先例が `vol1-r1.js` `vol3-r1.js` `vol5-r1.js` の設問120・116 にコメント付きで残っている）。
  **`no` は絶対に変えない**（1〜200 の連番が必須）。
- **問題を削除したら、他の問題の解説がそれを参照していないか grep する。**
  「この論点の別問題で扱う」と書いた解説が、参照先の削除で宙に浮く事故が実際に起きた。
- **選択肢を変えたら `assets/data` 全体と機械照合すること**（2026-09-12。「新規作問のとき」では足りない）。
  誤答を1本替えただけで、`grammar5.js` の `pron-13` が `vol1-r1.js` の `v1q117` と
  **4語・正解・枠組みまで一致する完全重複**になった。**照合する軸は2つ:**
  1. **選択肢の集合そのもの** — 完全一致と「4本中3本一致かつ正解も同一」の両方を見る
  2. **装置**（答えの根拠が同じか）— `avoid ------- changes` と `postponed ------- a decision` は
     選択肢4本と正解が一致し、どちらも「動名詞のみを取る動詞」という同じ装置だった
  **実測の基準線**（全1,838問）：選択肢集合の完全一致 33組・4本中3本一致 83組。
  大半は Part 5 の語形変化のパラダイム（`is/was/were/has been` 等）で、これは重複と数えない。
  **問題になるのは内容語の選択肢**で、そちらは993問中5組。
- **並行して走る実装役は、互いの成果を照合できない**（2026-09-12）。
  2人が同時に `["is","was","be","has been"]`（正解 `be`）を作った。**どちらも全体照合を実行しているが、
  相手の変更がまだ書かれていない時点で照合している。**別の2人は、同じ設問の直し方として
  **独立に同じ `off` を思いついて**枠まで一致させた。
  **メインが、全員の作業が終わったあとに選択肢集合の照合を1回かけること。**
- **解説に内部 ID・順序表現（「前問の」「1問目」）を書かない。**ドリルの出題順は SRS で入れ替わる。
  ただし Part 6/7 の1文書内と模試の通し番号は順序が固定なので可。
